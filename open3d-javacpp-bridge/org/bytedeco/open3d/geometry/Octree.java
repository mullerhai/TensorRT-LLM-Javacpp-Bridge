// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.geometry;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DGeometry.*;


/** \class Octree
 * 
 *  \brief Octree datastructure. */
@Namespace("open3d::geometry") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DGeometryConfig.class)
public class Octree extends Geometry3D {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public Octree(Pointer p) { super(p); }
    public IJsonConvertible asIJsonConvertible() { return asIJsonConvertible(this); }
    @Namespace public static native @Name("static_cast<utility::IJsonConvertible*>") IJsonConvertible asIJsonConvertible(Octree pointer);

    /** \brief Default Constructor. */
    
    ///
    public Octree() { super((Pointer)null); allocate(); }
    private native void allocate();
    /** \brief Parameterized Constructor.
     * 
     *  @param max_depth Sets the value of the max depth of the Octree. */
    
    ///
    public Octree(long max_depth) { super((Pointer)null); allocate(max_depth); }
    private native void allocate(long max_depth);
    /** \brief Parameterized Constructor.
     * 
     *  @param max_depth Sets the value of the max depth of the Octree.
     *  @param origin Sets the global min bound of the Octree.
     *  @param size Sets the outer bounding box edge size for the whole octree. */
    public Octree(long max_depth,
               @Const @ByRef Vector3d origin,
               double size) { super((Pointer)null); allocate(max_depth, origin, size); }
    private native void allocate(long max_depth,
               @Const @ByRef Vector3d origin,
               double size);
    public Octree(@Const @ByRef Octree src_octree) { super((Pointer)null); allocate(src_octree); }
    private native void allocate(@Const @ByRef Octree src_octree);
    public native @ByRef Octree Clear();
    public native @Cast("bool") boolean IsEmpty();
    public native @ByVal Vector3d GetMinBound();
    public native @ByVal Vector3d GetMaxBound();
    public native @ByVal Vector3d GetCenter();

    /** Creates the axis-aligned bounding box around the object.
     *  Further details in AxisAlignedBoundingBox::AxisAlignedBoundingBox() */
    public native @ByVal AxisAlignedBoundingBox GetAxisAlignedBoundingBox();

    /** Creates an oriented bounding box that is identical to the
     *  axis-aligned bounding from GetAxisAlignedBoundingBox(). */
    public native @ByVal OrientedBoundingBox GetOrientedBoundingBox(
                @Cast("bool") boolean robust/*=false*/);
    public native @ByVal OrientedBoundingBox GetOrientedBoundingBox();

    /** Creates an oriented bounding box that is identical to the
     *  axis-aligned bounding from GetAxisAlignedBoundingBox(). */
    public native @ByVal OrientedBoundingBox GetMinimalOrientedBoundingBox(
                @Cast("bool") boolean robust/*=false*/);
    public native @ByVal OrientedBoundingBox GetMinimalOrientedBoundingBox();

    public native @ByRef Octree Transform(@Const @ByRef Matrix4d transformation);
    public native @ByRef Octree Translate(@Const @ByRef Vector3d translation,
                          @Cast("bool") boolean relative/*=true*/);
    public native @ByRef Octree Translate(@Const @ByRef Vector3d translation);
    public native @ByRef Octree Scale(double scale, @Const @ByRef Vector3d center);
    public native @ByRef Octree Rotate(@Const @ByRef Matrix3d R,
                       @Const @ByRef Vector3d center);
    public native @Cast("bool") boolean ConvertToJsonValue(@ByRef Value value);
    public native @Cast("bool") boolean ConvertFromJsonValue(@Const @ByRef Value value);
    /** \brief Convert octree from point cloud.
     * 
     *  @param point_cloud Input point cloud.
     *  @param size_expand A small expansion size such that the octree is
     *  slightly bigger than the original point cloud bounds to accommodate all
     *  points. */
    public native void ConvertFromPointCloud(@Const @ByRef PointCloud point_cloud,
                                   double size_expand/*=0.01*/);
    public native void ConvertFromPointCloud(@Const @ByRef PointCloud point_cloud);

    /** Root of the octree. */
    public native @SharedPtr OctreeNode root_node_(); public native Octree root_node_(OctreeNode setter);

    /** Global min bound (include). A point is within bound iff
     *  origin_ <= point < origin_ + size_. */
    public native @ByRef Vector3d origin_(); public native Octree origin_(Vector3d setter);

    /** Outer bounding box edge size for the whole octree. A point is within
     *  bound iff origin_ <= point < origin_ + size_. */
    public native double size_(); public native Octree size_(double setter);

    /** Max depth of octree. The depth is defined as the distance from the
     *  deepest leaf node to root. A tree with only the root node has depth 0. */
    
    ///
    public native long max_depth_(); public native Octree max_depth_(long setter);

    /** \brief Insert a point to the octree.
     * 
     *  @param point Coordinates of the point.
     *  @param fl_init Initialization fcn used to create new leaf node
     *  (if needed) associated with the point.
     *  @param fl_update Update fcn used to update the leaf node
     *  associated with the point.
     *  @param fi_init Initialize fcn used to create a new internal node
     *  (if needed) which is an ancestor of the point's leaf node. If omitted,
     *  the default OctreeInternalNode function is used.
     *  @param fi_update Update fcn used to update the internal node
     *  which is an ancestor of the point's leaf node. If omitted, the default
     *  OctreeInternalNode function is used. */

    /** \brief DFS traversal of Octree from the root, with callback function
     *  called for each node.
     * 
     *  @param f  Callback which fires with each traversed internal/leaf node.
     *  Arguments supply information about the node being traversed and other
     *  node-specific data. A Boolean return value is used for early-stopping.
     *  If f returns true, children of this node will not be traversed. */

    /** \brief Const version of Traverse. DFS traversal of Octree from the root,
     *  with callback function called for each node.
     * 
     *  @param f  Callback which fires with each traversed internal/leaf node.
     *  Arguments supply information about the node being traversed and other
     *  node-specific data. A Boolean return value is used for early-stopping.
     *  If f returns true, children of this node will not be traversed. */

    
    ///
    public native @ByVal std::pair<std::shared_ptr<open3d::geometry::OctreeLeafNode>,std::shared_ptr<open3d::geometry::OctreeNodeInfo> > LocateLeafNode(@Const @ByRef Vector3d point);

    /** \brief Return true if point within bound, that is,
     *  origin <= point < origin + size.
     * 
     *  @param point Coordinates of the point.
     *  @param origin Origin coordinates.
     *  @param size Size of the Octree. */
    public static native @Cast("bool") boolean IsPointInBound(@Const @ByRef Vector3d point,
                                   @Const @ByRef Vector3d origin,
                                   double size);

    /** Returns true if the Octree is completely the same, used for testing. */
    public native @Cast("bool") @Name("operator ==") boolean equals(@Const @ByRef Octree other);

    /** Convert to VoxelGrid. */
    public native @SharedPtr VoxelGrid ToVoxelGrid();

    /** Convert from voxel grid. */
    public native void CreateFromVoxelGrid(@Const @ByRef VoxelGrid voxel_grid);
}
