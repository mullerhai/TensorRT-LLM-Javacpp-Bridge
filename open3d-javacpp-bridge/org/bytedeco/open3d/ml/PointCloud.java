// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.ml;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DML.*;


@Namespace("open3d::ml::contrib") @Properties(inherit = org.bytedeco.open3d.presets.Open3DMLConfig.class)
public class PointCloud extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public PointCloud() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public PointCloud(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public PointCloud(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public PointCloud position(long position) {
        return (PointCloud)super.position(position);
    }
    @Override public PointCloud getPointer(long i) {
        return new PointCloud((Pointer)this).offsetAddress(i);
    }

    public native @StdVector PointXYZ pts(); public native PointCloud pts(PointXYZ setter);

    // Must return the number of data points
    public native long kdtree_get_point_count();

    // Returns the dim'th component of the idx'th point in the class:
    // Since this is inlined and the "dim" argument is typically an immediate
    // value, the
    //  "if/else's" are actually solved at compile time.
    public native float kdtree_get_pt(long idx, long dim);

    // Optional bounding-box computation: return false to default to a standard
    // bbox computation loop.
    //   Return true if the BBOX was already computed by the class and returned
    //   in "bb" so it can be avoided to redo it again. Look at bb.size() to
    //   find out the expected dimensionality (e.g. 2 or 3 for point clouds)
}
