// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DCore.*;


@Namespace("open3d::core") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class MemoryManagerStatistic extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public MemoryManagerStatistic(Pointer p) { super(p); }

    /** enum class open3d::core::MemoryManagerStatistic::PrintLevel */
    public static final int
        /** Statistics for all used devices are printed. */
        All = 0,
        /** Only devices with unbalanced counts are printed.
         *  This is typically an indicator for memory leaks. */
        Unbalanced = 1,
        /** No statistics are printed. */
        None = 2;

    public static native @ByRef MemoryManagerStatistic GetInstance();

    
    

    /** Sets the level of provided information for printing. */
    public native void SetPrintLevel(@Cast("open3d::core::MemoryManagerStatistic::PrintLevel") int level);

    /** Enables or disables printing at the program end.
     *  Printing at the program end additionally overrides the exit code
     *  to EXIT_FAILURE in the presence of leaks. */
    public native void SetPrintAtProgramEnd(@Cast("bool") boolean print);

    /** Enables or disables printing at each malloc and free. */
    public native void SetPrintAtMallocFree(@Cast("bool") boolean print);

    /** Prints statistics for all recorded devices depending on the print level. */
    public native void Print();

    /** Returns true if any recorded device has unbalanced counts, false
     *  otherwise. */
    public native @Cast("bool") boolean HasLeaks();

    /** Adds the given allocation to the statistics. */
    public native void CountMalloc(Pointer ptr, long byte_size, @Const @ByRef Device device);

    /** Adds the given deallocations to the statistics.
     *  Counts for previously recorded allocations after a reset are ignored for
     *  consistency. */
    public native void CountFree(Pointer ptr, @Const @ByRef Device device);

    /** Resets the statistics. */
    public native void Reset();
}
