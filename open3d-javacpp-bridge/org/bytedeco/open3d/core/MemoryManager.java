// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DCore.*;


/** Top-level memory interface. Calls to any of the member functions will
 *  automatically dispatch the appropriate MemoryManagerDevice instance based on
 *  the provided device which is used to execute the requested functionality.
 * 
 *  The memory managers are dispatched as follows:
 * 
 *  DeviceType = CPU : MemoryManagerCPU
 *  DeviceType = CUDA :
 *    ENABLE_CACHED_CUDA_MANAGER = ON : MemoryManagerCached w/ MemoryManagerCUDA
 *    Otherwise :                      MemoryManagerCUDA
 *  */
@Namespace("open3d::core") @Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class MemoryManager extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public MemoryManager() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public MemoryManager(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public MemoryManager(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public MemoryManager position(long position) {
        return (MemoryManager)super.position(position);
    }
    @Override public MemoryManager getPointer(long i) {
        return new MemoryManager((Pointer)this).offsetAddress(i);
    }

    /** Allocates memory of \p byte_size bytes on device \p device and returns a
     *  pointer to the beginning of the allocated memory block. */
    public static native Pointer Malloc(long byte_size, @Const @ByRef Device device);

    /** Frees previously allocated memory at address \p ptr on device \p device. */
    public static native void Free(Pointer ptr, @Const @ByRef Device device);

    /** Copies \p num_bytes bytes of memory at address \p src_ptr on device
     *  \p src_device to address \p dst_ptr on device \p dst_device. */
    public static native void Memcpy(Pointer dst_ptr,
                           @Const @ByRef Device dst_device,
                           @Const Pointer src_ptr,
                           @Const @ByRef Device src_device,
                           long num_bytes);

    /** Same as Memcpy, but with host (CPU:0) as default src_device. */
    public static native void MemcpyFromHost(Pointer dst_ptr,
                                   @Const @ByRef Device dst_device,
                                   @Const Pointer host_ptr,
                                   long num_bytes);

    /** Same as Memcpy, but with host (CPU:0) as default dst_device. */
    public static native void MemcpyToHost(Pointer host_ptr,
                                 @Const Pointer src_ptr,
                                 @Const @ByRef Device src_device,
                                 long num_bytes);
}
