// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.pipelines;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DPipelines.*;


/** \class CorrespondenceChecker
 * 
 *  \brief Base class that checks if two (small) point clouds can be aligned.
 * 
 *  This class is used in feature based matching algorithms (such as RANSAC and
 *  FastGlobalRegistration) to prune out outlier correspondences.
 *  The virtual function Check() must be implemented in subclasses. */
@Namespace("open3d::pipelines::registration") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DPipelinesConfig.class)
public class CorrespondenceChecker extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public CorrespondenceChecker(Pointer p) { super(p); }

    /** \brief Default Constructor.
     * 
     *  @param require_pointcloud_alignment Specifies whether point cloud
     *  alignment is required. */
    /** \brief Function to check if two points can be aligned.
     * 
     *  @param source Source point cloud.
     *  @param target Target point cloud.
     *  @param corres Correspondence set between source and target point cloud.
     *  @param transformation The estimated transformation (inplace). */
    public native @Cast("bool") boolean Check(@Const @ByRef PointCloud source,
                           @Const @ByRef PointCloud target,
                           @Const @ByRef CorrespondenceSet corres,
                           @Const @ByRef Matrix4d transformation);
    /** Some checkers do not require point clouds to be aligned, e.g., the edge
     *  length checker. Some checkers do, e.g., the distance checker. */
    public native @Cast("bool") boolean require_pointcloud_alignment_(); public native CorrespondenceChecker require_pointcloud_alignment_(boolean setter);
}
