// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.pipelines;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DPipelines.*;


/** \class L1Loss
 * 
 *  L1 loss function used for outlier rejection.
 * 
 *  The loss p(r) for a given residual 'r' is computed as follow: */
//    p(r) = abs(r)
@Namespace("Pointer") @Properties(inherit = org.bytedeco.open3d.presets.Open3DPipelinesConfig.class)
public class L1Loss extends RobustKernel {
    static { Loader.load(); }
    /** Default native constructor. */
    public L1Loss() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public L1Loss(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public L1Loss(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public L1Loss position(long position) {
        return (L1Loss)super.position(position);
    }
    @Override public L1Loss getPointer(long i) {
        return new L1Loss((Pointer)this).offsetAddress(i);
    }

    /** The weight w(r) for a given residual 'r' is computed as follow:
     *    w(r) = 1.0 / abs(r), for all r
     * 
     *  @param residual Residual value obtained during the optimization step. */
    public native double Weight(double residual);
}
