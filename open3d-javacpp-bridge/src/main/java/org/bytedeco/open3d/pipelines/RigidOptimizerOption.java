// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.pipelines;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DPipelines.*;


@Namespace("Pointer") @Properties(inherit = org.bytedeco.open3d.presets.Open3DPipelinesConfig.class)
public class RigidOptimizerOption extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public RigidOptimizerOption() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public RigidOptimizerOption(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public RigidOptimizerOption(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public RigidOptimizerOption position(long position) {
        return (RigidOptimizerOption)super.position(position);
    }
    @Override public RigidOptimizerOption getPointer(long i) {
        return new RigidOptimizerOption((Pointer)this).offsetAddress(i);
    }

    /** Number of iterations for optimization steps. */
    public native int maximum_iteration_(); public native RigidOptimizerOption maximum_iteration_(int setter);

    /** Parameter to check the visibility of a point. Points with depth larger
     *  than maximum_allowable_depth in a RGB-D will be marked as invisible for
     *  the camera producing that RGB-D image. Select a proper value to include
     *  necessary points while ignoring unwanted points such as the background. */
    public native double maximum_allowable_depth_(); public native RigidOptimizerOption maximum_allowable_depth_(double setter);

    /** Parameter to check the visibility of a point. When the difference of a
     *  point’s depth value in the RGB-D image and the point’s depth value in
     *  the 3D mesh is greater than depth_threshold_for_visibility_check, the
     *  point is marked as invisible to the camera producing the RGB-D image. */
    public native double depth_threshold_for_visibility_check_(); public native RigidOptimizerOption depth_threshold_for_visibility_check_(double setter);

    /** Parameter to check the visibility of a point. It’s often desirable to
     *  ignore points where there is an abrupt change in depth value. First the
     *  depth gradient image is computed, points are considered to be invisible
     *  if the depth gradient magnitude is larger than
     *  depth_threshold_for_discontinuity_check. */
    public native double depth_threshold_for_discontinuity_check_(); public native RigidOptimizerOption depth_threshold_for_discontinuity_check_(double setter);

    /** Parameter to check the visibility of a point. Related to
     *  depth_threshold_for_discontinuity_check, when boundary points are
     *  detected, dilation is performed to ignore points near the object
     *  boundary. half_dilation_kernel_size_for_discontinuity_map specifies the
     *  half-kernel size for the dilation applied on the visibility mask image. */
    public native int half_dilation_kernel_size_for_discontinuity_map_(); public native RigidOptimizerOption half_dilation_kernel_size_for_discontinuity_map_(int setter);

    /** If a projected 3D point onto a 2D image lies in the image border within
     *  image_boundary_margin, the 3D point is considered invisible from the
     *  camera producing the image. This parameter is not used for visibility
     *  check, but used when computing the final color assignment after color
     *  map optimization. */
    public native int image_boundary_margin_(); public native RigidOptimizerOption image_boundary_margin_(int setter);

    /** If a vertex is invisible from all images, we assign the averaged color
     *  of the k nearest visible vertices to fill the invisible vertex. Set to
     *  0 to disable this feature and all invisible vertices will be black. */
    public native int invisible_vertex_color_knn_(); public native RigidOptimizerOption invisible_vertex_color_knn_(int setter);

    /** If specified, the intermediate results will be stored in in the debug
     *  output dir. Existing files will be overwritten if the names are the
     *  same. */
    public native @StdString BytePointer debug_output_dir_(); public native RigidOptimizerOption debug_output_dir_(BytePointer setter);
}
