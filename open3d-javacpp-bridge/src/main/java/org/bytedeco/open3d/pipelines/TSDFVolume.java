// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.pipelines;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DPipelines.*;


/** \class TSDFVolume
 * 
 *  \brief Base class of the Truncated Signed Distance Function (TSDF) volume.
 * 
 *  This volume is usually used to integrate surface data (e.g., a series of
 *  RGB-D images) into a Mesh or PointCloud. The basic technique is presented in
 *  the following paper:
 * 
 *  B. Curless and M. Levoy
 *  A volumetric method for building complex models from range images
 *  In SIGGRAPH, 1996 */
@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DPipelinesConfig.class)
public class TSDFVolume extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public TSDFVolume(Pointer p) { super(p); }

    /** \brief Default Constructor.
     * 
     *  @param voxel_length Length of the voxel in meters.
     *  @param sdf_trunc Truncation value for signed distance function (SDF).
     *  @param color_type Color type of the TSDF volume. */
    /** Function to reset the TSDFVolume. */
    public native void Reset();

    /** Function to integrate an RGB-D image into the volume. */
    public native void Integrate(@Const @ByRef RGBDImage image,
                               @Const @ByRef PinholeCameraIntrinsic intrinsic,
                               @Const @ByRef Matrix4d extrinsic);

    /** Function to extract a point cloud with normals. */
    public native @SharedPtr PointCloud ExtractPointCloud();

    /** \brief Function to extract a triangle mesh, using the marching cubes
     *  algorithm. (https://en.wikipedia.org/wiki/Marching_cubes) */
    public native @SharedPtr TriangleMesh ExtractTriangleMesh();
    /** Length of the voxel in meters. */
    public native double voxel_length_(); public native TSDFVolume voxel_length_(double setter);
    /** Truncation value for signed distance function (SDF). */
    public native double sdf_trunc_(); public native TSDFVolume sdf_trunc_(double setter);
    /** Color type of the TSDF volume. */
    public native @Cast("Pointer") int color_type_(); public native TSDFVolume color_type_(int setter);
}
