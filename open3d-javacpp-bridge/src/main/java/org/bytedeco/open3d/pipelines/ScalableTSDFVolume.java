// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.pipelines;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DPipelines.*;


/** The ScalableTSDFVolume implements a more memory efficient data structure for
 *  volumetric integration.
 * 
 *  This implementation is based on the following repository:
 *  https://github.com/qianyizh/ElasticReconstruction/tree/master/Integrate
 *  The reference is:
 *  Q.-Y. Zhou and V. Koltun
 *  Dense Scene Reconstruction with Points of Interest
 *  In SIGGRAPH 2013
 * 
 *  An observed depth pixel gives two types of information: (a) an approximation
 *  of the nearby surface, and (b) empty space from the camera to the surface.
 *  They induce two core concepts of volumetric integration: weighted average of
 *  a truncated signed distance function (TSDF), and carving. The weighted
 *  average of TSDF is great in addressing the Gaussian noise along surface
 *  normal and producing a smooth surface output. The carving is great in
 *  removing outlier structures like floating noise pixels and bumps along
 *  structure edges. */
@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DPipelinesConfig.class)
public class ScalableTSDFVolume extends TSDFVolume {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ScalableTSDFVolume(Pointer p) { super(p); }

    @NoOffset public static class VolumeUnit extends Pointer {
        static { Loader.load(); }
        /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
        public VolumeUnit(Pointer p) { super(p); }
        /** Native array allocator. Access with {@link Pointer#position(long)}. */
        public VolumeUnit(long size) { super((Pointer)null); allocateArray(size); }
        private native void allocateArray(long size);
        @Override public VolumeUnit position(long position) {
            return (VolumeUnit)super.position(position);
        }
        @Override public VolumeUnit getPointer(long i) {
            return new VolumeUnit((Pointer)this).offsetAddress(i);
        }
    
        public VolumeUnit() { super((Pointer)null); allocate(); }
        private native void allocate();
        public native @SharedPtr UniformTSDFVolume volume_(); public native VolumeUnit volume_(UniformTSDFVolume setter);
        public native @ByRef Vector3i index_(); public native VolumeUnit index_(Vector3i setter);
    }
    public ScalableTSDFVolume(double voxel_length,
                           double sdf_trunc,
                           @ByVal TSDFVolumeColorType color_type,
                           int volume_unit_resolution/*=16*/,
                           int depth_sampling_stride/*=4*/) { super((Pointer)null); allocate(voxel_length, sdf_trunc, color_type, volume_unit_resolution, depth_sampling_stride); }
    private native void allocate(double voxel_length,
                           double sdf_trunc,
                           @ByVal TSDFVolumeColorType color_type,
                           int volume_unit_resolution/*=16*/,
                           int depth_sampling_stride/*=4*/);
    public ScalableTSDFVolume(double voxel_length,
                           double sdf_trunc,
                           @ByVal TSDFVolumeColorType color_type) { super((Pointer)null); allocate(voxel_length, sdf_trunc, color_type); }
    private native void allocate(double voxel_length,
                           double sdf_trunc,
                           @ByVal TSDFVolumeColorType color_type);
    public native void Reset();
    public native void Integrate(@Const @ByRef RGBDImage image,
                       @Const @ByRef PinholeCameraIntrinsic intrinsic,
                       @Const @ByRef Matrix4d extrinsic);
    public native @SharedPtr PointCloud ExtractPointCloud();
    public native @SharedPtr TriangleMesh ExtractTriangleMesh();
    /** Debug function to extract the voxel data into a point cloud. */
    public native @SharedPtr PointCloud ExtractVoxelPointCloud();
    public native int volume_unit_resolution_(); public native ScalableTSDFVolume volume_unit_resolution_(int setter);
    public native double volume_unit_length_(); public native ScalableTSDFVolume volume_unit_length_(double setter);
    public native int depth_sampling_stride_(); public native ScalableTSDFVolume depth_sampling_stride_(int setter);

    /** Assume the index of the volume unit is (x, y, z), then the unit spans
     *  from (x, y, z) * volume_unit_length_
     *  to (x + 1, y + 1, z + 1) * volume_unit_length_ */
}
