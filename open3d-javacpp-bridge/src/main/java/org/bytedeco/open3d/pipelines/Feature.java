// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.pipelines;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DPipelines.*;


/** \class Feature
 * 
 *  \brief Class to store featrues for registration. */
@Namespace("Pointer") @Properties(inherit = org.bytedeco.open3d.presets.Open3DPipelinesConfig.class)
public class Feature extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public Feature() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public Feature(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public Feature(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public Feature position(long position) {
        return (Feature)super.position(position);
    }
    @Override public Feature getPointer(long i) {
        return new Feature((Pointer)this).offsetAddress(i);
    }

    /** Resize feature data buffer to {@code dim x n}.
     * 
     *  @param dim Feature dimension per point.
     *  @param n Number of points. */
    public native void Resize(int dim, int n);
    /** Returns feature dimensions per point. */
    public native long Dimension();
    /** Returns number of points. */
    
    ///
    public native long Num();

    /** \brief Selects features from \p input Feature group, with indices in \p
     *  indices, and returns a new Feature group with selected features.
     * 
     *  @param indices Indices of features to be selected.
     *  @param invert Set to {@code True} to invert the selection of indices. */
    public native @SharedPtr Feature SelectByIndex(@StdVector SizeTPointer indices,
                                               @Cast("bool") boolean invert/*=false*/);
    public native @SharedPtr Feature SelectByIndex(@StdVector SizeTPointer indices);
    /** Data buffer storing features. */
    public native @ByRef MatrixXd data_(); public native Feature data_(MatrixXd setter);
}
