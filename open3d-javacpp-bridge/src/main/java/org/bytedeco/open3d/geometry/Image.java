// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.geometry;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DGeometry.*;


/** \class Image
 * 
 *  \brief The Image class stores image with customizable width, height, num of
 *  channels and bytes per channel. */
@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DGeometryConfig.class)
public class Image extends Geometry2D {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public Image(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public Image(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public Image position(long position) {
        return (Image)super.position(position);
    }
    @Override public Image getPointer(long i) {
        return new Image((Pointer)this).offsetAddress(i);
    }

    /** \enum ColorToIntensityConversionType
     * 
     *  \brief Specifies whether R, G, B channels have the same weight when
     *  converting to intensity. Only used for Image with 3 channels.
     * 
     *  When {@code Weighted} is used R, G, B channels are weighted according to the
     *  Digital ITU BT.601 standard: I = 0.299 * R + 0.587 * G + 0.114 * B. */
    /** enum class Pointer */
    public static final int
        /** R, G, B channels have equal weights. */
        Equal = 0,
        /** Weighted R, G, B channels: I = 0.299 * R + 0.587 * G + 0.114 * B. */
        Weighted = 1;

    /** \enum FilterType
     * 
     *  \brief Specifies the Image filter type. */
    /** enum class Pointer */
    public static final int
        /** Gaussian filter of size 3 x 3. */
        Gaussian3 = 0,
        /** Gaussian filter of size 5 x 5. */
        Gaussian5 = 1,
        /** Gaussian filter of size 7 x 7. */
        Gaussian7 = 2,
        /** Sobel filter along X-axis. */
        Sobel3Dx = 3,
        /** Sobel filter along Y-axis. */
        Sobel3Dy = 4;
    /** \brief Default Constructor. */
    public Image() { super((Pointer)null); allocate(); }
    private native void allocate();
    public native @ByRef Image Clear();
    public native @Cast("bool") boolean IsEmpty();
    public native @ByVal Vector2d GetMinBound();
    
    ///
    public native @ByVal Vector2d GetMaxBound();

    /** \brief Test if coordinate {@code (u, v)} is located in the inner_marge of the
     *  image.
     * 
     *  @param u Coordinate along the width dimension.
     *  @param v Coordinate along the height dimension.
     *  @param inner_margin The inner margin from the image boundary.
     *  @return Returns {@code true} if coordinate {@code (u, v)} is located in the
     *  inner_marge of the image. */
    public native @Cast("bool") boolean TestImageBoundary(double u, double v, double inner_margin/*=0.0*/);
    public native @Cast("bool") boolean TestImageBoundary(double u, double v);
    /** Returns {@code true} if the Image has valid data. */
    public native @Cast("bool") boolean HasData();

    /** \brief Prepare Image properties and allocate Image buffer. */
    public native @ByRef Image Prepare(int width,
                       int height,
                       int num_of_channels,
                       int bytes_per_channel);

    /** \brief Returns data size per line (row, or the width) in bytes. */
    public native int BytesPerLine();

    /** Function to access the bilinear interpolated float value of a
     *  (single-channel) float image.
     *  Returns a tuple, where the first bool indicates if the u,v coordinates
     *  are within the image dimensions, and the second double value is the
     *  interpolated pixel value. */
    public native @ByVal Pointer FloatValueAt(double u, double v);

    /** Factory function to create a float image composed of multipliers that
     *  convert depth values into camera distances (ImageFactory.cpp)
     *  The multiplier function M(u,v) is defined as:
     *  M(u, v) = sqrt(1 + ((u - cx) / fx) ^ 2 + ((v - cy) / fy) ^ 2)
     *  This function is used as a convenient function for performance
     *  optimization in volumetric integration (see
     *  Core/Integration/TSDFVolume.h). */
    public static native @SharedPtr Image CreateDepthToCameraDistanceMultiplierFloatImage(
                @Const @ByRef PinholeCameraIntrinsic intrinsic);

    /** Return a gray scaled float type image. */
    public native @SharedPtr Image CreateFloatImage(
                @Cast("Pointer") int type/*=Pointer*/);
    public native @SharedPtr Image CreateFloatImage();

    /** Function to access the raw data of a single-channel Image. */

    /** Function to access the raw data of a multi-channel Image. */

    /** Reinterpret the internal data buffer. The resulting type's size must be
     *  the same as bytes_per_channel_. This is similar to PointerAt<T>(0, 0). */

    public native @SharedPtr Image ConvertDepthToFloatImage(
                double depth_scale/*=1000.0*/, double depth_trunc/*=3.0*/);
    public native @SharedPtr Image ConvertDepthToFloatImage();

    public native @SharedPtr Image Transpose();

    /** Function to flip image horizontally (from left to right). */
    public native @SharedPtr Image FlipHorizontal();
    /** Function to flip image vertically (upside down). */
    public native @SharedPtr Image FlipVertical();

    /** Function to filter image with pre-defined filtering type. */
    public native @SharedPtr Image Filter(@Cast("Pointer") int type);

    /** Function to filter image with arbitrary dx, dy separable filters. */
    public native @SharedPtr Image Filter(@StdVector DoublePointer dx,
                                      @StdVector DoublePointer dy);
    public native @SharedPtr Image Filter(@StdVector DoubleBuffer dx,
                                      @StdVector DoubleBuffer dy);
    public native @SharedPtr Image Filter(@StdVector double[] dx,
                                      @StdVector double[] dy);

    public native @SharedPtr Image FilterHorizontal(
                @StdVector DoublePointer kernel);
    public native @SharedPtr Image FilterHorizontal(
                @StdVector DoubleBuffer kernel);
    public native @SharedPtr Image FilterHorizontal(
                @StdVector double[] kernel);

    /** Function to 2x image downsample using simple 2x2 averaging. */
    public native @SharedPtr Image Downsample();

    /** Function to dilate 8bit mask map. */
    public native @SharedPtr Image Dilate(int half_kernel_size/*=1*/);
    public native @SharedPtr Image Dilate();

    /** Function to linearly transform pixel intensities
     *  image_new = scale * image + offset. */
    
    ///
    public native @ByRef Image LinearTransform(double scale/*=1.0*/, double offset/*=0.0*/);
    public native @ByRef Image LinearTransform();

    /** Function to clipping pixel intensities.
     * 
     *  @param min is lower bound.
     *  @param max is upper bound. */
    public native @ByRef Image ClipIntensity(double min/*=0.0*/, double max/*=1.0*/);
    public native @ByRef Image ClipIntensity();

    /** Function to change data types of image
     *  crafted for specific usage such as
     *  single channel float image -> 8-bit RGB or 16-bit depth image. */

    /** Function to filter image pyramid. */
    public static native Pointer FilterPyramid(Pointer input,
                                          @Cast("Pointer") int type);

    /** Function to create image pyramid. */
    public native Pointer CreatePyramid(long num_of_levels,
                                   @Cast("bool") boolean with_gaussian_filter/*=true*/);
    public native Pointer CreatePyramid(long num_of_levels);

    /** Function to create a depthmap boundary mask from depth image. */
    public native @SharedPtr Image CreateDepthBoundaryMask(
                double depth_threshold_for_discontinuity_check/*=0.1*/,
                int half_dilation_kernel_size_for_discontinuity_map/*=3*/);
    public native @SharedPtr Image CreateDepthBoundaryMask();
    /** Width of the image. */
    public native int width_(); public native Image width_(int setter);
    /** Height of the image. */
    public native int height_(); public native Image height_(int setter);
    /** Number of channels in the image. */
    public native int num_of_channels_(); public native Image num_of_channels_(int setter);
    /** Number of bytes per channel. */
    public native int bytes_per_channel_(); public native Image bytes_per_channel_(int setter);
    /** Image storage buffer. */
    public native @Cast("uint8_t*") @StdVector BytePointer data_(); public native Image data_(BytePointer setter);
}
