// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.ml;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DML.*;


@NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DMLConfig.class)
public class RoiPoolOpKernel extends OpKernel {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public RoiPoolOpKernel(Pointer p) { super(p); }


    public native void Compute(OpKernelContext context);

    public native void Kernel(OpKernelContext context,
                            int batch_size,
                            int pts_num,
                            int boxes_num,
                            int feature_in_len,
                            int sampled_pts_num,
                            @Const FloatPointer xyz,
                            @Const FloatPointer boxes3d,
                            @Const FloatPointer pts_feature,
                            FloatPointer pooled_features,
                            IntPointer pooled_empty_flag);
    public native void Kernel(OpKernelContext context,
                            int batch_size,
                            int pts_num,
                            int boxes_num,
                            int feature_in_len,
                            int sampled_pts_num,
                            @Const FloatBuffer xyz,
                            @Const FloatBuffer boxes3d,
                            @Const FloatBuffer pts_feature,
                            FloatBuffer pooled_features,
                            IntBuffer pooled_empty_flag);
    public native void Kernel(OpKernelContext context,
                            int batch_size,
                            int pts_num,
                            int boxes_num,
                            int feature_in_len,
                            int sampled_pts_num,
                            @Const float[] xyz,
                            @Const float[] boxes3d,
                            @Const float[] pts_feature,
                            float[] pooled_features,
                            int[] pooled_empty_flag);
}
