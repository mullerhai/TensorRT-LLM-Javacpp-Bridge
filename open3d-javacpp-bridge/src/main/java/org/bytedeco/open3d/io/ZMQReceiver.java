// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.io;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DIO.*;
  // namespace messages

/** Class for the server side receiving requests from a client. */
@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DIOConfig.class)
public class ZMQReceiver extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ZMQReceiver(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public ZMQReceiver(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public ZMQReceiver position(long position) {
        return (ZMQReceiver)super.position(position);
    }
    @Override public ZMQReceiver getPointer(long i) {
        return new ZMQReceiver((Pointer)this).offsetAddress(i);
    }

    /** Constructs a receiver listening on the specified address.
     *  @param address  Address to listen on.
     *  @param timeout       Timeout in milliseconds for sending the reply. */
    public ZMQReceiver(@StdString BytePointer _address/*="tcp://127.0.0.1:51454"*/,
                    int timeout/*=10000*/) { super((Pointer)null); allocate(_address, timeout); }
    private native void allocate(@StdString BytePointer _address/*="tcp://127.0.0.1:51454"*/,
                    int timeout/*=10000*/);
    public ZMQReceiver() { super((Pointer)null); allocate(); }
    private native void allocate();
    public ZMQReceiver(@StdString String _address/*="tcp://127.0.0.1:51454"*/,
                    int timeout/*=10000*/) { super((Pointer)null); allocate(_address, timeout); }
    private native void allocate(@StdString String _address/*="tcp://127.0.0.1:51454"*/,
                    int timeout/*=10000*/);

    
    

    /** Starts the receiver mainloop in a new thread. */
    public native void Start();

    /** Stops the receiver mainloop and joins the thread.
     *  This function blocks until the mainloop is done with processing
     *  messages that have already been received. */
    public native void Stop();

    /** Returns the last error from the mainloop thread. */
    public native @ByVal runtime_error GetLastError();

    /** Sets the message processor object which will process incoming messages. */
    public native void SetMessageProcessor(@SharedPtr MessageProcessorBase processor);
}
