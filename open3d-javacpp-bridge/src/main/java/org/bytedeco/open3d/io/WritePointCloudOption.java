// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.io;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DIO.*;


/** \struct WritePointCloudOption
 *  \brief Optional parameters to WritePointCloud */
@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DIOConfig.class)
public class WritePointCloudOption extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public WritePointCloudOption(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public WritePointCloudOption(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public WritePointCloudOption position(long position) {
        return (WritePointCloudOption)super.position(position);
    }
    @Override public WritePointCloudOption getPointer(long i) {
        return new WritePointCloudOption((Pointer)this).offsetAddress(i);
    }

    /** enum class Pointer */
    public static final boolean Binary = false, Ascii = true;
    /** enum class Pointer */
    public static final boolean Uncompressed = false, Compressed = true;
    public WritePointCloudOption() { super((Pointer)null); allocate(); }
    private native void allocate();
    // for compatibility
    public WritePointCloudOption(@Cast("bool") boolean write_ascii) { super((Pointer)null); allocate(write_ascii); }
    private native void allocate(@Cast("bool") boolean write_ascii);
    // for compatibility
    public WritePointCloudOption(@StdString BytePointer format,
                              @Cast("bool") boolean write_ascii) { super((Pointer)null); allocate(format, write_ascii); }
    private native void allocate(@StdString BytePointer format,
                              @Cast("bool") boolean write_ascii);
    public WritePointCloudOption(@StdString String format,
                              @Cast("bool") boolean write_ascii) { super((Pointer)null); allocate(format, write_ascii); }
    private native void allocate(@StdString String format,
                              @Cast("bool") boolean write_ascii);
    /** Specifies what format the contents of the file are (and what writer to
     *  use), default "auto" means to go off of file extension.
     *  Note: "auto" is incompatible when reading directly from memory. */
    public native @StdString BytePointer format(); public native WritePointCloudOption format(BytePointer setter);
    /** Whether to save in Ascii or Binary.  Some savers are capable of doing
     *  either, other ignore this. */
    public native @Cast("Pointer") boolean write_ascii(); public native WritePointCloudOption write_ascii(boolean setter);
    /** Whether to save Compressed or Uncompressed.  Currently, only PCD is
     *  capable of compressing, and only if using IsAscii::Binary, all other
     *  formats ignore this. */
    public native @Cast("Pointer") boolean compressed(); public native WritePointCloudOption compressed(boolean setter);
    /** Print progress to stdout about loading progress.  Also see
     *  \p update_progress if you want to have your own progress indicators or
     *  to be able to cancel loading. */
    public native @Cast("bool") boolean print_progress(); public native WritePointCloudOption print_progress(boolean setter);
    /** Callback to invoke as reading is progressing, parameter is percentage
     *  completion (0.-100.) return true indicates to continue loading, false
     *  means to try to stop loading and cleanup */
}
