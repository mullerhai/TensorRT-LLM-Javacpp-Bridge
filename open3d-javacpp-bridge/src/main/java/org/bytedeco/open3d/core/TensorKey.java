// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DCore.*;


/** \brief TensorKey is used to represent single index, slice or advanced
 *  indexing on a Tensor.
 * 
 *  See https://numpy.org/doc/stable/reference/arrays.indexing.html for details. */
@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class TensorKey extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public TensorKey(Pointer p) { super(p); }

    /** Instantiates a TensorKey with single index mode.
     * 
     *  <pre>{@code
     *  t[0]   : t.GetItem({TensorKey::Index(0)})
     *  t[2]   : t.GetItem({TensorKey::Index(2)})
     *  t[2, 3]: t.GetItem({TensorKey::Index(2), TensorKey::Index(3)})
     *  }</pre>
     * 
     *  @param index: Index to the tensor. */
    
    ///
    ///
    public static native @ByVal TensorKey Index(@Cast("int64_t") long index);

    /** Instantiates a TensorKey with slice mode.
     * 
     *  <pre>{@code
     *  t[0:10:2]: t.GetItem({TensorKey::Slice(0    , 10  , 2   )})
     *  t[:-1]   : t.Getitem({TensorKey::Slice(None , None, -1  )})
     *  t[3:]    : t.GetItem({TensorKey::Slice(3    , None, None)})
     *  }</pre>
     * 
     *  @param start: Start index. None means starting from the 0-th element.
     *  @param stop: Stop index. None means stopping at the last element.
     *  @param step: Step size. None means step size 1. */
    
    ///
    ///
    public static native @ByVal TensorKey Slice(@Cast("int64_t*") @Optional LongPointer start,
                               @Cast("int64_t*") @Optional LongPointer stop,
                               @Cast("int64_t*") @Optional LongPointer step);
    public static native @ByVal TensorKey Slice(@Cast("int64_t*") @Optional LongBuffer start,
                               @Cast("int64_t*") @Optional LongBuffer stop,
                               @Cast("int64_t*") @Optional LongBuffer step);
    public static native @ByVal TensorKey Slice(@Cast("int64_t*") @Optional long[] start,
                               @Cast("int64_t*") @Optional long[] stop,
                               @Cast("int64_t*") @Optional long[] step);

    /** Instantiates a TensorKey with tensor-index (advanced indexing) mode.
     * 
     *  <pre>{@code
     *  [[1, 2], [3:]]: advanced indexing on dim-0, slicing on dim-1.
     *  t.GetItem({
     *      TensorKey::IndexTensor(Tensor::Init<int64_t>({1, 2}, device),
     *      TensorKey::Slice(3, None, None),
     *  });
     *  }</pre>
     * 
     *  @param index_tensor: Indexing tensor of dtype int64_t or bool. */
    public static native @ByVal TensorKey IndexTensor(@Const @ByRef Tensor index_tensor);

    /** enum class Pointer */
    public static final int Index = 0, Slice = 1, IndexTensor = 2;

    /** Returns TensorKey mode. */
    public native @Cast("Pointer") int GetMode();

    /** Convert TensorKey to a string representation. */
    public native @StdString BytePointer ToString();
    /** Get (single) index.
     *  For TensorKeyMode::Index only. */
    public native @Cast("int64_t") long GetIndex();

    /** Get start index. Throws exception if start is None.
     *  For TensorKeyMode::Slice only. */
    public native @Cast("int64_t") long GetStart();

    /** Get stop index. Throws exception if stop is None.
     *  For TensorKeyMode::Slice only. */
    public native @Cast("int64_t") long GetStop();

    /** Get step index. Throws exception if step is None.
     *  For TensorKeyMode::Slice only. */
    public native @Cast("int64_t") long GetStep();

    /** When dim_size is know, convert the None values in start, stop, step with
     *  to concrete values and returns a new TensorKey.
     *  <pre>{@code
     *  E.g. if t.shape == (5,), t[:4]:
     *       before compute: Slice(None,    4, None)
     *       after compute : Slice(   0,    4,    1)
     *  E.g. if t.shape == (5,), t[1:]:
     *       before compute: Slice(   1, None, None)
     *       after compute : Slice(   1,    5,    1)
     *  }</pre>
     *  For TensorKeyMode::Slice only. */
    public native @ByVal TensorKey InstantiateDimSize(@Cast("int64_t") long dim_size);

    /** Get advanced indexing tensor.
     *  For TensorKeyMode::IndexTensor only. */
    public native @ByVal Tensor GetIndexTensor();
}
