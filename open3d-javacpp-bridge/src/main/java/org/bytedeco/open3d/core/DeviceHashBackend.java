// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DCore.*;


@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class DeviceHashBackend extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public DeviceHashBackend(Pointer p) { super(p); }


    /** Reserve expects a lot of extra memory space at runtime,
     *  since it consists of
     *  1) dumping all key value pairs to a buffer
     *  2) creating a new hash table
     *  3) parallel inserting dumped key value pairs
     *  4) deallocating old hash table */
    public native void Reserve(@Cast("int64_t") long _capacity);

    /** Parallel insert contiguous arrays of keys and values. */
    public native void Insert(@Const Pointer input_keys,
                            @Cast("const void**") PointerPointer input_values,
                            buf_index_t output_buf_indices,
                            @Cast("bool*") BoolPointer output_masks,
                            @Cast("int64_t") long count);
    public native void Insert(@Const Pointer input_keys,
                            @Cast("const void**") @StdVector @ByPtrPtr Pointer input_values,
                            buf_index_t output_buf_indices,
                            @Cast("bool*") BoolPointer output_masks,
                            @Cast("int64_t") long count);
    public native void Insert(@Const Pointer input_keys,
                            @Cast("const void**") @StdVector @ByPtrPtr Pointer input_values,
                            buf_index_t output_buf_indices,
                            @Cast("bool*") boolean[] output_masks,
                            @Cast("int64_t") long count);

    /** Parallel find a contiguous array of keys. */
    public native void Find(@Const Pointer input_keys,
                          buf_index_t output_buf_indices,
                          @Cast("bool*") BoolPointer output_masks,
                          @Cast("int64_t") long count);
    public native void Find(@Const Pointer input_keys,
                          buf_index_t output_buf_indices,
                          @Cast("bool*") boolean[] output_masks,
                          @Cast("int64_t") long count);

    /** Parallel erase a contiguous array of keys. */
    public native void Erase(@Const Pointer input_keys,
                           @Cast("bool*") BoolPointer output_masks,
                           @Cast("int64_t") long count);
    public native void Erase(@Const Pointer input_keys,
                           @Cast("bool*") boolean[] output_masks,
                           @Cast("int64_t") long count);

    /** Parallel collect all iterators in the hash table */
    public native @Cast("int64_t") long GetActiveIndices(buf_index_t output_buf_indices);

    /** Clear stored map without reallocating memory. */
    public native void Clear();

    /** Get the size (number of valid entries) of the hash map. */
    public native @Cast("int64_t") long Size();

    /** Get the number of buckets of the hash map. */
    public native @Cast("int64_t") long GetBucketCount();

    /** Get the current load factor, defined as size / bucket count. */
    public native float LoadFactor();

    /** Get the maximum capacity of the hash map. */
    public native @Cast("int64_t") long GetCapacity();

    /** Get the current device. */
    public native @ByVal Device GetDevice();

    /** Get the number of entries per bucket. */
    public native @Cast("int64_t*") @StdVector LongPointer BucketSizes();

    /** Get the key buffer that stores actual keys. */
    public native @ByVal Tensor GetKeyBuffer();

    /** Get the value buffers that store actual array of values. */
    public native @StdVector Tensor GetValueBuffers();

    /** Get the i-th value buffer that store an actual value array. */
    public native @ByVal Tensor GetValueBuffer(long i/*=0*/);
    public native @ByVal Tensor GetValueBuffer();

    public native void Allocate(@Cast("int64_t") long _capacity);
    public native void Free();
    public native @Cast("int64_t") long capacity_(); public native DeviceHashBackend capacity_(long setter);

    public native @Cast("int64_t") long key_dsize_(); public native DeviceHashBackend key_dsize_(long setter);
    public native @Cast("int64_t*") @StdVector LongPointer value_dsizes_(); public native DeviceHashBackend value_dsizes_(LongPointer setter);

    public native @ByRef Device device_(); public native DeviceHashBackend device_(Device setter);

    public native @SharedPtr HashBackendBuffer buffer_(); public native DeviceHashBackend buffer_(HashBackendBuffer setter);
}
