// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DCore.*;


/** \class Dataset
 *  \brief Base Open3D dataset class.
 * 
 *  The Dataset classes in Open3D are designed for convenient access to
 *  "built-in" example and test data. You'll need internet access to use the
 *  dataset classes. The downloaded data will be stored in the Open3D's data
 *  root directory.
 * 
 *  - A dataset class locates the data root directory in the following order:
 *    (a) User-specified by {@code data_root} when instantiating a dataset object.
 *    (b) OPEN3D_DATA_ROOT environment variable.
 *    (c) $HOME/open3d_data.
 *    By default, (c) will be used, and it is also the recommended way.
 *  - When a dataset object is instantiated, the corresponding data will be
 *    downloaded in {@code ${data_root}/download/prefix/} and extracted or copied to
 *    {@code ${data_root}/extract/prefix/}. If the extracted data directory exists,
 *    the files will be used without validation. If it does not exists, and the
 *    valid downloaded file exists, the data will be extracted from the
 *    downloaded file. If downloaded file does not exists, or validates against
 *    the provided MD5, it will be re-downloaded.
 *  - After the data is downloaded and extracted, the dataset object will NOT
 *    load the data for you. Instead, you will get the paths to the data files
 *    and use Open3D's I/O functions to load the data. This design exposes where
 *    the data is stored and how the data is loaded, allowing users to modify
 *    the code and load their own data in a similar way. Please check the
 *    documentation of the specific dataset to know more about the specific
 *    functionalities provided for it. */
@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class Dataset extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public Dataset(Pointer p) { super(p); }

    /** \brief Parameterized Constructor.
     * 
     *  @param prefix Prefix of the dataset. The data is downloaded in
     *  {@code ${data_root}/download/${prefix}/} and extracted in
     *  {@code ${data_root}/extract/${prefix}/}.
     *  @param data_root Path to {@code ${data_root}}, which contains all the
     *  downloaded and extracted files.
     *  The data root directory is located in the following order:
     *    (a) User-specified by {@code data_root} when instantiating a dataset object.
     *    (b) OPEN3D_DATA_ROOT environment variable.
     *    (c) $HOME/open3d_data.
     *    By default, (c) will be used, and it is also the recommended way. */
    public Dataset(@StdString BytePointer prefix, @StdString BytePointer data_root/*=""*/) { super((Pointer)null); allocate(prefix, data_root); }
    private native void allocate(@StdString BytePointer prefix, @StdString BytePointer data_root/*=""*/);
    public Dataset(@StdString BytePointer prefix) { super((Pointer)null); allocate(prefix); }
    private native void allocate(@StdString BytePointer prefix);
    public Dataset(@StdString String prefix, @StdString String data_root/*=""*/) { super((Pointer)null); allocate(prefix, data_root); }
    private native void allocate(@StdString String prefix, @StdString String data_root/*=""*/);
    public Dataset(@StdString String prefix) { super((Pointer)null); allocate(prefix); }
    private native void allocate(@StdString String prefix);

    /** \brief Get data root directory. The data root is set at construction
     *  time or automatically determined. */
    public native @StdString BytePointer GetDataRoot();

    /** \brief Get prefix for the dataset. */
    public native @StdString BytePointer GetPrefix();

    /** \brief Get absolute path to download directory. i.e.
     *  ${data_root}/download/${prefix} */
    public native @StdString BytePointer GetDownloadDir();

    /** \brief Get absolute path to extract directory. i.e.
     *  ${data_root}/extract/${prefix} */
    public native @StdString BytePointer GetExtractDir();
}
