// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DCore.*;


/**
 * \brief Framework-specific function pointers table for DLPack exchange.
 *
 * Additionally to {@code __dlpack__()} we define a C function table sharable by
 * Python implementations via {@code __c_dlpack_exchange_api__}.
 * This attribute must be set on the type as a Python integer compatible
 * with {@code PyLong_FromVoidPtr}/{@code PyLong_AsVoidPtr}.
 *
 * A consumer library may use a pattern such as:
 *
 * <pre>{@code
 *
 * PyObject *api_obj = type(tensor_obj).__c_dlpack_exchange_api__;  // as C-code
 * MyDLPackExchangeAPI *api = PyLong_AsVoidPtr(api_obj);
 * if (api == NULL && PyErr_Occurred()) { goto handle_error; }
 *
 * }</pre>
 *
 * Note that this must be defined on the type. The consumer should look up the
 * attribute on the type and may cache the result for each unique type.
 *
 * The precise API table is given by:
 * <pre>{@code
 * struct MyDLPackExchangeAPI : public DLPackExchangeAPI {
 *   MyDLPackExchangeAPI() {
 *     header.version.major = DLPACK_MAJOR_VERSION;
 *     header.version.minor = DLPACK_MINOR_VERSION;
 *     header.prev_version_api = nullptr;
 *
 *     managed_tensor_allocator = MyDLPackManagedTensorAllocator;
 *     managed_tensor_from_py_object_no_sync =
 * MyDLPackManagedTensorFromPyObjectNoSync; managed_tensor_to_py_object_no_sync
 * = MyDLPackManagedTensorToPyObjectNoSync; dltensor_from_py_object_no_sync =
 * MyDLPackDLTensorFromPyObjectNoSync; current_work_stream =
 * MyDLPackCurrentWorkStream;
 *  }
 *
 *  static const DLPackExchangeAPI* Global() {
 *     static MyDLPackExchangeAPI inst;
 *     return &inst;
 *  }
 * };
 * }</pre>
 *
 * Guidelines for leveraging DLPackExchangeAPI:
 *
 * There are generally two kinds of consumer needs for DLPack exchange:
 * - N0: library support, where consumer.kernel(x, y, z) would like to run a
 * kernel with the data from x, y, z. The consumer is also expected to run the
 * kernel with the same stream context as the producer. For example, when x, y,
 * z is torch.Tensor, consumer should query exchange_api->current_work_stream to
 * get the current stream and launch the kernel with the same stream. This setup
 * is necessary for no synchronization in kernel launch and maximum
 * compatibility with CUDA graph capture in the producer. This is the desirable
 * behavior for library extension support for frameworks like PyTorch.
 * - N1: data ingestion and retention
 *
 * Note that obj.__dlpack__() API should provide useful ways for N1.
 * The primary focus of the current DLPackExchangeAPI is to enable faster
 * exchange N0 with the support of the function pointer current_work_stream.
 *
 * Array/Tensor libraries should statically create and initialize this structure
 * then return a pointer to DLPackExchangeAPI as an int value in Tensor/Array.
 * The DLPackExchangeAPI* must stay alive throughout the lifetime of the
 * process.
 *
 * One simple way to do so is to create a static instance of DLPackExchangeAPI
 * within the framework and return a pointer to it. The following code
 * shows an example to do so in C++. It should also be reasonably easy
 * to do so in other languages.
 */
@Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class DLPackExchangeAPI extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public DLPackExchangeAPI() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public DLPackExchangeAPI(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public DLPackExchangeAPI(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public DLPackExchangeAPI position(long position) {
        return (DLPackExchangeAPI)super.position(position);
    }
    @Override public DLPackExchangeAPI getPointer(long i) {
        return new DLPackExchangeAPI((Pointer)this).offsetAddress(i);
    }

    /**
     * \brief The header that remains stable across versions.
     */
    public native @ByRef DLPackExchangeAPIHeader header(); public native DLPackExchangeAPI header(DLPackExchangeAPIHeader setter);
    /**
     * \brief Producer function pointer for DLPackManagedTensorAllocator
     *        This function must not be NULL.
     * @see DLPackManagedTensorAllocator
     */
    public native DLPackManagedTensorAllocator managed_tensor_allocator(); public native DLPackExchangeAPI managed_tensor_allocator(DLPackManagedTensorAllocator setter);
    /**
     * \brief Producer function pointer for DLPackManagedTensorFromPyObject
     *        This function must be not NULL.
     * @see DLPackManagedTensorFromPyObject
     */
    public native DLPackManagedTensorFromPyObjectNoSync managed_tensor_from_py_object_no_sync(); public native DLPackExchangeAPI managed_tensor_from_py_object_no_sync(DLPackManagedTensorFromPyObjectNoSync setter);
    /**
     * \brief Producer function pointer for DLPackManagedTensorToPyObject
     *        This function must be not NULL.
     * @see DLPackManagedTensorToPyObject
     */
    public native DLPackManagedTensorToPyObjectNoSync managed_tensor_to_py_object_no_sync(); public native DLPackExchangeAPI managed_tensor_to_py_object_no_sync(DLPackManagedTensorToPyObjectNoSync setter);
    /**
     * \brief Producer function pointer for DLPackDLTensorFromPyObject
     *        This function can be NULL when the producer does not support this
     * function. @see DLPackDLTensorFromPyObjectNoSync
     */
    public native DLPackDLTensorFromPyObjectNoSync dltensor_from_py_object_no_sync(); public native DLPackExchangeAPI dltensor_from_py_object_no_sync(DLPackDLTensorFromPyObjectNoSync setter);
    /**
     * \brief Producer function pointer for DLPackCurrentWorkStream
     *        This function must be not NULL.
     * @see DLPackCurrentWorkStream
     */
    public native DLPackCurrentWorkStream current_work_stream(); public native DLPackExchangeAPI current_work_stream(DLPackCurrentWorkStream setter);
}
