// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DCore.*;


// #ifdef BUILD_CUDA_MODULE

/** \class CUDAScopedDevice
 * 
 *  Switch CUDA device id in the current scope. The device id will be reset
 *  once leaving the scope.
 * 
 *  CUDAScopedDevice provides an
 *  [RAII-style](https://en.wikipedia.org/wiki/Resource_acquisition_is_initialization)
 *  mechanism for setting and resetting CUDA devices of a scoped block.
 * 
 *  Example:
 *  <pre>{@code cpp
 *  void my_func() {
 *      // The scoped wrapper records the previous device when it is
 *      constructed.
 *      // Let's assume the current device is 0 initially.
 *      CUDAScopedDevice scoped_device(1);
 * 
 *      // Now the current device is 1.
 *      // Make cuda calls here for device 1.
 * 
 *      // After `my_func` returns, `scoped_device` goes out-of-scope,
 *      // so the global device will be reset back to 0.
 *  } */
@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class CUDAScopedDevice extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public CUDAScopedDevice(Pointer p) { super(p); }

    public CUDAScopedDevice(int device_id) { super((Pointer)null); allocate(device_id); }
    private native void allocate(int device_id);

    public CUDAScopedDevice(@Const @ByRef Device device) { super((Pointer)null); allocate(device); }
    private native void allocate(@Const @ByRef Device device);

    
    
}
