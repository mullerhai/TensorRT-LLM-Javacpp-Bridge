// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DCore.*;


/** \class CUDAScopedStream
 * 
 *  Switch CUDA stream in the current scope. The stream will be reset
 *  once leaving the scope.
 * 
 *  CUDAScopedStream provides an
 *  [RAII-style](https://en.wikipedia.org/wiki/Resource_acquisition_is_initialization)
 *  mechanism for setting and resetting CUDA streams of a scoped block.
 * 
 *  Example:
 *  <pre>{@code cpp
 *  void my_func(cudaStream_t stream) {
 *      // The scoped wrapper records the previous stream when it is
 *      constructed.
 *      // Let's assume the current stream is 0 initially.
 *      CUDAScopedStream scoped_stream(stream);
 * 
 *      // Now the current stream is 1.
 *      // Make cuda calls here for stream 1.
 * 
 *      // After `my_func` returns, `scoped_stream` goes out-of-scope,
 *      // so the global stream will be reset back to 0.
 *  }
 *  }</pre>
 *  If a new stream shall be used, the following constructor can be used:
 *  <pre>{@code cpp
 *  void my_func() {
 *      // The scoped wrapper records the previous stream when it is
 *      constructed.
 *      // Let's assume the current stream is 0 initially.
 *      CUDAScopedStream scoped_stream(CUDAScopedStream::CreateNewStream);
 * 
 *      // Now the current stream is 1.
 *      // Make cuda calls here for stream 1.
 * 
 *      // After `my_func` returns, `scoped_stream` goes out-of-scope,
 *      // so the global stream will be reset back to 0.
 *  }
 *  }</pre> */
@Namespace("Pointer") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class CUDAScopedStream extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public CUDAScopedStream(Pointer p) { super(p); }

    @MemberGetter public static native @Const @ByRef CreateNewStreamTag CreateNewStream();

    public CUDAScopedStream(@Const @ByRef CreateNewStreamTag arg0) { super((Pointer)null); allocate(arg0); }
    private native void allocate(@Const @ByRef CreateNewStreamTag arg0);

    public CUDAScopedStream(@ByVal cudaStream_t stream) { super((Pointer)null); allocate(stream); }
    private native void allocate(@ByVal cudaStream_t stream);

    
    
}
