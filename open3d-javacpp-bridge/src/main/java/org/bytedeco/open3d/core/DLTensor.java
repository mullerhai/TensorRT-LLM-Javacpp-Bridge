// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DCore.*;


/**
 * \brief Plain C Tensor object, does not manage memory.
 */
@Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class DLTensor extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public DLTensor() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public DLTensor(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public DLTensor(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public DLTensor position(long position) {
        return (DLTensor)super.position(position);
    }
    @Override public DLTensor getPointer(long i) {
        return new DLTensor((Pointer)this).offsetAddress(i);
    }

    /**
     * \brief The data pointer points to the allocated data. This will be CUDA
     * device pointer or cl_mem handle in OpenCL. It may be opaque on some
     * device types. This pointer is always aligned to 256 bytes as in CUDA. The
     * {@code byte_offset} field should be used to point to the beginning of the data.
     *
     * Note that as of Nov 2021, multiple libraries (CuPy, PyTorch, TensorFlow,
     * TVM, perhaps others) do not adhere to this 256 byte alignment requirement
     * on CPU/CUDA/ROCm, and always use {@code byte_offset=0}.  This must be fixed
     * (after which this note will be updated); at the moment it is recommended
     * to not rely on the data pointer being correctly aligned.
     *
     * For given DLTensor, the size of memory required to store the contents of
     * data is calculated as follows:
     *
     * <pre>{@code {.c}
     * static inline size_t GetDataSize(const DLTensor* t) {
     *   size_t size = 1;
     *   for (tvm_index_t i = 0; i < t->ndim; ++i) {
     *     size *= t->shape[i];
     *   }
     *   size *= (t->dtype.bits * t->dtype.lanes + 7) / 8;
     *   return size;
     * }
     * }</pre>
     *
     * Note that if the tensor is of size zero, then the data pointer should be
     * set to {@code NULL}.
     */
    public native Pointer data(); public native DLTensor data(Pointer setter);
    /** \brief The device of the tensor */
    public native @ByRef DLDevice device(); public native DLTensor device(DLDevice setter);
    /** \brief Number of dimensions */
    public native @Cast("int32_t") int ndim(); public native DLTensor ndim(int setter);
    /** \brief The data type of the pointer*/
    public native @ByRef DLDataType dtype(); public native DLTensor dtype(DLDataType setter);
    /**
     * \brief The shape of the tensor
     *
     *  When ndim == 0, shape can be set to NULL.
     */
    public native @Cast("int64_t*") LongPointer shape(); public native DLTensor shape(LongPointer setter);
    /**
     * \brief strides of the tensor (in number of elements, not bytes),
     *  can not be NULL if ndim != 0, must points to
     *  an array of ndim elements that specifies the strides,
     *  so consumer can always rely on strides[dim] being valid for 0 <= dim <
     * ndim.
     *
     *  When ndim == 0, strides can be set to NULL.
     *
     *  \note Before DLPack v1.2, strides can be NULL to indicate contiguous
     * data. This is not allowed in DLPack v1.2 and later. The rationale is to
     * simplify the consumer handling.
     */
    public native @Cast("int64_t*") LongPointer strides(); public native DLTensor strides(LongPointer setter);
    /** \brief The offset in bytes to the beginning pointer to data */
    public native @Cast("uint64_t") long byte_offset(); public native DLTensor byte_offset(long setter);
}
