// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.utility;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.bytedeco.open3d.Vector3d;
import org.bytedeco.open3d.Vector3i;
import org.bytedeco.open3d.Vector2d;
import org.bytedeco.open3d.Vector2i;
import org.bytedeco.open3d.Vector4i;
import org.bytedeco.open3d.Matrix3d;
import org.bytedeco.open3d.Matrix4d;
import org.bytedeco.open3d.SizeVector;

import static org.bytedeco.open3d.global.Open3DUtility.*;


/** Logger class should be used as a global singleton object (GetInstance()). */
@Namespace("Pointer") @Properties(inherit = org.bytedeco.open3d.presets.Open3DUtilityConfig.class)
public class Logger extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public Logger() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public Logger(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public Logger(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public Logger position(long position) {
        return (Logger)super.position(position);
    }
    @Override public Logger getPointer(long i) {
        return new Logger((Pointer)this).offsetAddress(i);
    }

    
    

    /** Get Logger global singleton instance. */
    
    ///
    public static native @ByRef Logger GetInstance();

    /** Overwrite the default print function, this is useful when you want to
     *  redirect prints rather than printing to stdout. For example, in Open3D's
     *  python binding, the default print function is replaced with py::print().
     * 
     *  @param print_fcn The function for printing. It should take a string
     *  input and returns nothing. */

    /** Reset the print function to the default one (print to console). */
    public native void ResetPrintFunction();

    /** Get the print function used by the Logger. */

    /** Set global verbosity level of Open3D.
     * 
     *  @param verbosity_level Messages with equal or less than verbosity_level
     *  verbosity will be printed. */
    public native void SetVerbosityLevel(@Cast("Pointer") int verbosity_level);

    /** Get global verbosity level of Open3D. */
    public native @Cast("Pointer") int GetVerbosityLevel();
        }
