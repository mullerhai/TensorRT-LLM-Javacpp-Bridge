// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DCore.*;


/** A tensorlist is a list of Tensors of the same shape, similar to
 *  std::vector<Tensor>. Internally, a tensorlist stores the Tensors in one
 *  bigger internal tensor, where the begin dimension of the internal tensor is
 *  extendable.
 * 
 *  Examples:
 *  - A 3D point cloud with N points:
 *    - element_shape        : (3,)
 *    - reserved_size        : M, where M >= N
 *    - internal_tensor.shape: (M, 3)
 *  - Sparse voxel grid of N voxels:
 *    - element_shape        : (8, 8, 8)
 *    - reserved_size        : M, where M >= N
 *    - internal_tensor.shape: (M, 8, 8, 8) */
@Namespace("open3d::core") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DCoreConfig.class)
public class TensorList extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public TensorList(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public TensorList(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public TensorList position(long position) {
        return (TensorList)super.position(position);
    }
    @Override public TensorList getPointer(long i) {
        return new TensorList((Pointer)this).offsetAddress(i);
    }

    /** Useful to support operator[] in a map. */
    
    ///
    public TensorList() { super((Pointer)null); allocate(); }
    private native void allocate();

    /** Constructs an empty tensorlist.
     * 
     *  @param element_shape Shape of the contained tensors, e.g. {3,}. 0-sized
     *  and scalar element_shape are allowed.
     *  @param dtype Data type of the contained tensors. e.g. core::Float32.
     *  @param device Device of the contained tensors. e.g. Device("CPU:0"). */
    
    ///
    public TensorList(@Const @ByRef SizeVector element_shape,
                   @ByVal Dtype dtype,
                   @Const @ByRef(nullValue = "open3d::core::Device(\"CPU:0\")") Device device) { super((Pointer)null); allocate(element_shape, dtype, device); }
    private native void allocate(@Const @ByRef SizeVector element_shape,
                   @ByVal Dtype dtype,
                   @Const @ByRef(nullValue = "open3d::core::Device(\"CPU:0\")") Device device);
    public TensorList(@Const @ByRef SizeVector element_shape,
                   @ByVal Dtype dtype) { super((Pointer)null); allocate(element_shape, dtype); }
    private native void allocate(@Const @ByRef SizeVector element_shape,
                   @ByVal Dtype dtype);

    /** Constructs a tensorlist from a vector of Tensors. The tensors must have
     *  the same shape, dtype and device. Values will be copied.
     * 
     *  @param tensors A vector of tensors. The tensors must have common shape,
     *  dtype and device. */
    
    ///
    public TensorList(@StdVector Tensor tensors) { super((Pointer)null); allocate(tensors); }
    private native void allocate(@StdVector Tensor tensors);

    /** Constructs a tensorlist with specified size.
     * 
     *  @param size Size of the tensorlist.
     *  @param element_shape Shape of the contained tensors, e.g. {3,}. 0-sized
     *  and scalar element_shape are allowed.
     *  @param dtype Data type of the contained tensors. e.g. core::Float32.
     *  @param device Device of the contained tensors. e.g. Device("CPU:0"). */
    
    ///
    public TensorList(@Cast("int64_t") long size,
                   @Const @ByRef SizeVector element_shape,
                   @ByVal Dtype dtype,
                   @Const @ByRef(nullValue = "open3d::core::Device(\"CPU:0\")") Device device) { super((Pointer)null); allocate(size, element_shape, dtype, device); }
    private native void allocate(@Cast("int64_t") long size,
                   @Const @ByRef SizeVector element_shape,
                   @ByVal Dtype dtype,
                   @Const @ByRef(nullValue = "open3d::core::Device(\"CPU:0\")") Device device);
    public TensorList(@Cast("int64_t") long size,
                   @Const @ByRef SizeVector element_shape,
                   @ByVal Dtype dtype) { super((Pointer)null); allocate(size, element_shape, dtype); }
    private native void allocate(@Cast("int64_t") long size,
                   @Const @ByRef SizeVector element_shape,
                   @ByVal Dtype dtype);

    /** Constructs a tensorlist from a list of Tensors. The tensors must have
     *  the same shape, dtype and device. Values will be copied.
     * 
     *  @param tensors A list of tensors. The tensors must have common shape,
     *  dtype and device. */
    
    ///
    public TensorList(@Const @ByRef std::initializer_list<open3d::core::Tensor> tensors) { super((Pointer)null); allocate(tensors); }
    private native void allocate(@Const @ByRef std::initializer_list<open3d::core::Tensor> tensors);

    /** Constructs a tensorlist from Tensor iterator. The tensors must have
     *  the same shape, dtype and device. Values will be copied.
     * 
     *  @param begin Begin iterator.
     *  @param end End iterator. */

    /** Factory function to create tensorlist from a Tensor.
     * 
     *  @param tensor The input tensor. The tensor must have at least one
     *  dimension (tensor.NumDims() >= 1). The first dimension of the tensor
     *  will be used as the "size" dimension of the tensorlist, while the
     *  remaining dimensions will be used as the element shape of the tensor
     *  list. For example, if the input tensor has shape (2, 3, 4), the
     *  resulting tensorlist will have size 2 and element shape (3, 4).
     * 
     *  @param inplace If {@code inplace == true}, the tensorlist shares the same
     *  memory with the input tensor. The input tensor must be contiguous. The
     *  resulting tensorlist cannot be extended. If {@code inplace == false}, the
     *  tensor values will be copied when creating the tensorlist. */
    public static native @ByVal TensorList FromTensor(@Const @ByRef Tensor tensor, @Cast("bool") boolean inplace/*=false*/);
    public static native @ByVal TensorList FromTensor(@Const @ByRef Tensor tensor);

    /** Copy constructor for tensorlist. The internal tensor will share the same
     *  memory as the input. Also see: the copy constructor for Tensor. */
    public TensorList(@Const @ByRef TensorList other) { super((Pointer)null); allocate(other); }
    private native void allocate(@Const @ByRef TensorList other);

    /** Move constructor for tensorlist. The internal tensor will share the same
     *  memory as the input. Also see: the move constructor for Tensor. */

    /** Copy assignment operator. The internal tensor will share the same memory
     *  as the input. */
    public native @ByRef @Name("operator =") TensorList put(@Const @ByRef TensorList other);

    /** Move assignment operator. The internal tensor will share the same memory
     *  as the input. */

    /** Performs actual copy from another tensorlist. The internal tensor will
     *  be explicitly copied. All attributes will be copied and replaced. The
     *  returned tensor will always be resizable. */
    public native void CopyFrom(@Const @ByRef TensorList other);

    /** Duplicate the current tensorlist. Values will be copied. The returned
     *  tensor will always be resizable. */
    public native @ByVal TensorList Clone();

    /** Return the reference of the contained valid tensors with shared memory. */
    public native @ByVal Tensor AsTensor();

    /** Resize tensorlist.
     *  If the size increases, the increased part will be initialized with 0.
     *  If the size decreases, the reserved_size_ remain unchanged. This
     *  operation is only valid for resizable tensorlist. */
    
    ///
    public native void Resize(@Cast("int64_t") long new_size);

    /** Push back a tensor to the tensorlist. The values will be copied. This
     *  operation is only valid for resizable tensorlist.
     * 
     *  @param tensor The tensor to to be copied to the end of the tensorlist.
     *  The tensor must be of the same shape, dtype and device as the tensot
     *  list. */
    public native void PushBack(@Const @ByRef Tensor tensor);

    /** Extend the current tensorlist with another tensorlist appended to the
     *  end. The data is copied. The two tensorlists must have the same
     *  element_shape, dtype, and device. This operation is only valid for
     *  resizable tensorlist. */
    public native void Extend(@Const @ByRef TensorList other);

    /** Concatenate two tensorlists.
     *  Return a new tensorlists with data copied.
     *  Two tensorlists must have the same element_shape, type, and device. */
    public static native @ByVal TensorList Concatenate(@Const @ByRef TensorList a, @Const @ByRef TensorList b);

    /** Concatenate two tensorlists. */
    public native @ByVal @Name("operator +") TensorList add(@Const @ByRef TensorList other);

    /** Inplace concatenate with another tensorlist. This operation is only
     *  valid for resizable tensorlist. */
    public native @ByRef @Name("operator +=") TensorList addPut(@Const @ByRef TensorList other);

    /** Extract the i-th Tensor along the begin axis, returning a new view.
     *  For advanced indexing like Slice, use tensorlist.AsTensor().Slice(). */
    public native @ByVal @Name("operator []") Tensor get(@Cast("int64_t") long index);

    /** Clear the tensorlist by disgarding the internal tensor and resetting the
     *  size to 0. This operation is only valid for resizable tensorlist. */
    public native void Clear();

    public native @StdString BytePointer ToString();

    public native @ByVal SizeVector GetElementShape();

    public native void AssertElementShape(@Const @ByRef SizeVector expected_element_shape);

    public native void AssertDevice(@Const @ByRef Device expected_device);

    public native @ByVal Device GetDevice();

    public native @ByVal Dtype GetDtype();

    public native @Cast("int64_t") long GetSize();

    public native @Cast("int64_t") long GetReservedSize();

    public native @Const @ByRef Tensor GetInternalTensor();

    public native @Cast("bool") boolean IsResizable();
}
