// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.geometry;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DGeometry.*;


/** \class OrientedBoundingBox
 * 
 *  \brief A bounding box oriented along an arbitrary frame of reference.
 * 
 *  The oriented bounding box is defined by its center position, rotation
 *  matrix and extent. */
@Namespace("open3d::geometry") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DGeometryConfig.class)
public class OrientedBoundingBox extends Geometry3D {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public OrientedBoundingBox(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public OrientedBoundingBox(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public OrientedBoundingBox position(long position) {
        return (OrientedBoundingBox)super.position(position);
    }
    @Override public OrientedBoundingBox getPointer(long i) {
        return new OrientedBoundingBox((Pointer)this).offsetAddress(i);
    }

    /** \brief Default constructor.
     * 
     *  Creates an empty Oriented Bounding Box. */
    
    ///
    public OrientedBoundingBox() { super((Pointer)null); allocate(); }
    private native void allocate();
    /** \brief Parameterized constructor.
     * 
     *  @param center Specifies the center position of the bounding box.
     *  @param R The rotation matrix specifying the orientation of the
     *  bounding box with the original frame of reference.
     *  @param extent The extent of the bounding box. */
    public OrientedBoundingBox(@Const @ByRef Vector3d center,
                            @Const @ByRef Matrix3d R,
                            @Const @ByRef Vector3d extent) { super((Pointer)null); allocate(center, R, extent); }
    private native void allocate(@Const @ByRef Vector3d center,
                            @Const @ByRef Matrix3d R,
                            @Const @ByRef Vector3d extent);
    public native @ByRef OrientedBoundingBox Clear();
    public native @Cast("bool") boolean IsEmpty();
    public native @ByVal Vector3d GetMinBound();
    public native @ByVal Vector3d GetMaxBound();
    public native @ByVal Vector3d GetCenter();

    /** Creates an axis-aligned bounding box around the
     *  points (corners) of the object. */
    public native @ByVal AxisAlignedBoundingBox GetAxisAlignedBoundingBox();

    /** Returns the object itself */
    public native @ByVal OrientedBoundingBox GetOrientedBoundingBox(
                @Cast("bool") boolean robust);

    /** Returns the object itself */
    public native @ByVal OrientedBoundingBox GetMinimalOrientedBoundingBox(
                @Cast("bool") boolean robust);

    public native @ByRef OrientedBoundingBox Transform(
                @Const @ByRef Matrix4d transformation);
    public native @ByRef OrientedBoundingBox Translate(@Const @ByRef Vector3d translation,
                                               @Cast("bool") boolean relative/*=true*/);
    public native @ByRef OrientedBoundingBox Translate(@Const @ByRef Vector3d translation);
    public native @ByRef OrientedBoundingBox Scale(double scale,
                                           @Const @ByRef Vector3d center);
    public native @ByRef OrientedBoundingBox Rotate(@Const @ByRef Matrix3d R,
                                            @Const @ByRef Vector3d center);

    /** Returns the volume of the bounding box. */
    public native double Volume();

    /** Returns the eight points that define the bounding box.
     *  <pre>{@literal
     *       ------- x
     *      /|
     *     / |
     *    /  | z
     *   y
     *       0 ------------------- 1
     *        /|                /|
     *       / |               / |
     *      /  |              /  |
     *     /   |             /   |
     *  2 ------------------- 7  |
     *    |    |____________|____| 6
     *    |   /3            |   /
     *    |  /              |  /
     *    | /               | /
     *    |/                |/
     *  5 ------------------- 4
     *  }</pre> */
    public native @StdVector Vector3d GetBoxPoints();

    /** Return indices to points that are within the bounding box. */
    
    ///
    public native @StdVector SizeTPointer GetPointIndicesWithinBoundingBox(
                @StdVector Vector3d points);

    /** Returns an oriented bounding box from the AxisAlignedBoundingBox.
     * 
     *  @param aabox AxisAlignedBoundingBox object from which
     *  OrientedBoundingBox is created. */
    public static native @ByVal OrientedBoundingBox CreateFromAxisAlignedBoundingBox(
                @Const @ByRef AxisAlignedBoundingBox aabox);

    /** Creates an oriented bounding box using a PCA.
     *  Note, that this is only a coarse approximation to the minimum oriented
     *  bounding box that could be computed for example with O'Rourke's
     *  algorithm (cf. http://cs.smith.edu/~jorourke/Papers/MinVolBox.pdf,
     *  https://www.geometrictools.com/Documentation/MinimumVolumeBox.pdf)
     *  @param points A list of points with data type of float32 or float64 (N x
     *  3 tensor, where N must be larger than 3).
     *  @param robust If set to true uses a more robust method which works
     *                in degenerate cases but introduces noise to the points
     *                coordinates.
     *  \remark
     *      For an exact algorithm use the Tensor version of
     *      open3d::t::geometry::OrientedBoundingBox::CreateFromPoints() and
     *      select the method
     *      open3d::t::geometry::MethodOBBCreate::MINIMAL_JYLANKI. */
    public static native @ByVal OrientedBoundingBox CreateFromPoints(
                @StdVector Vector3d points, @Cast("bool") boolean robust/*=false*/);
    public static native @ByVal OrientedBoundingBox CreateFromPoints(
                @StdVector Vector3d points);

    /** Fast approximation of the oriented bounding box with the smallest
     *  volume. The algorithm makes use of the fact that at least one edge of
     *  the convex hull must be collinear with an edge of the minimum
     *  bounding box: for each triangle in the convex hull, calculate
     *  the minimal axis aligned box in the frame of that triangle.
     *  at the end, return the box with the smallest volume found.
     *  @param points A list of points with data type of float32 or float64 (N x
     *  3 tensor, where N must be larger than 3).
     *  @param robust If set to true uses a more robust method which works
     *                in degenerate cases but introduces noise to the points
     *                coordinates.
     *  \remark
     *      For an exact algorithm use the Tensor version of
     *      open3d::t::geometry::OrientedBoundingBox::CreateFromPoints() and
     *      select the method
     *      open3d::t::geometry::MethodOBBCreate::MINIMAL_JYLANKI. */
    public static native @ByVal OrientedBoundingBox CreateFromPointsMinimal(
                @StdVector Vector3d points, @Cast("bool") boolean robust/*=false*/);
    public static native @ByVal OrientedBoundingBox CreateFromPointsMinimal(
                @StdVector Vector3d points);
    /** The center point of the bounding box. */
    public native @ByRef Vector3d center_(); public native OrientedBoundingBox center_(Vector3d setter);
    /** The rotation matrix of the bounding box to transform the original frame
     *  of reference to the frame of this box. */
    public native @ByRef Matrix3d R_(); public native OrientedBoundingBox R_(Matrix3d setter);
    /** The extent of the bounding box in its frame of reference. */
    public native @ByRef Vector3d extent_(); public native OrientedBoundingBox extent_(Vector3d setter);
    /** The color of the bounding box in RGB. */
    public native @ByRef Vector3d color_(); public native OrientedBoundingBox color_(Vector3d setter);
}
