// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3D.*;


/** \class LineSet
 * 
 *  \brief LineSet define a sets of lines in 3D. A typical application is to
 *  display the point cloud correspondence pairs. */
@Namespace("open3d::geometry") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DConfig.class)
public class LineSet extends Geometry3D {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public LineSet(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public LineSet(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public LineSet position(long position) {
        return (LineSet)super.position(position);
    }
    @Override public LineSet getPointer(long i) {
        return new LineSet((Pointer)this).offsetAddress(i);
    }

    /** \brief Default Constructor. */
    
    ///
    ///
    public LineSet() { super((Pointer)null); allocate(); }
    private native void allocate();
    /** \brief Parameterized Constructor.
     * 
     *   Create a LineSet from given points and line indices
     * 
     *  @param points Point coordinates.
     *  @param lines Lines denoted by the index of points forming the line. */
    public LineSet(@StdVector Vector3d points,
                @StdVector Vector2i lines) { super((Pointer)null); allocate(points, lines); }
    private native void allocate(@StdVector Vector3d points,
                @StdVector Vector2i lines);
    public native @ByRef LineSet Clear();
    public native @Cast("bool") boolean IsEmpty();
    public native @ByVal Vector3d GetMinBound();
    public native @ByVal Vector3d GetMaxBound();
    public native @ByVal Vector3d GetCenter();

    /** Creates the axis-aligned bounding box around the points of the object.
     *  Further details in AxisAlignedBoundingBox::CreateFromPoints() */
    public native @ByVal AxisAlignedBoundingBox GetAxisAlignedBoundingBox();

    /** Creates an oriented bounding box around the points of the object.
     *  Further details in OrientedBoundingBox::CreateFromPoints()
     *  @param robust If set to true uses a more robust method which works
     *                in degenerate cases but introduces noise to the points
     *                coordinates. */
    public native @ByVal OrientedBoundingBox GetOrientedBoundingBox(
                @Cast("bool") boolean robust/*=false*/);
    public native @ByVal OrientedBoundingBox GetOrientedBoundingBox();

    /** Creates the minimal oriented bounding box around the points of the
     *  object. Further details in
     *  OrientedBoundingBox::CreateFromPointsMinimal()
     *  @param robust If set to true uses a more robust method which works
     *                in degenerate cases but introduces noise to the points
     *                coordinates. */
    public native @ByVal OrientedBoundingBox GetMinimalOrientedBoundingBox(
                @Cast("bool") boolean robust/*=false*/);
    public native @ByVal OrientedBoundingBox GetMinimalOrientedBoundingBox();

    public native @ByRef LineSet Transform(@Const @ByRef Matrix4d transformation);
    public native @ByRef LineSet Translate(@Const @ByRef Vector3d translation,
                           @Cast("bool") boolean relative/*=true*/);
    public native @ByRef LineSet Translate(@Const @ByRef Vector3d translation);
    public native @ByRef LineSet Scale(double scale, @Const @ByRef Vector3d center);
    public native @ByRef LineSet Rotate(@Const @ByRef Matrix3d R,
                        @Const @ByRef Vector3d center);

    public native @ByRef @Name("operator +=") LineSet addPut(@Const @ByRef LineSet lineset);
    public native @ByVal @Name("operator +") LineSet add(@Const @ByRef LineSet lineset);

    /** Returns {@code true} if the object contains points. */
    public native @Cast("bool") boolean HasPoints();

    /** Returns {@code true} if the object contains lines. */
    public native @Cast("bool") boolean HasLines();

    /** Returns {@code true} if the objects lines contains colors. */
    
    ///
    public native @Cast("bool") boolean HasColors();

    /** \brief Returns the coordinates of the line at the given index.
     * 
     *  @param line_index Index of the line. */
    
    ///
    public native @ByVal std::pair<Eigen::Vector3d,Eigen::Vector3d> GetLineCoordinate(
                long line_index);

    /** \brief Assigns each line in the LineSet the same color.
     * 
     *  @param color Specifies the color to be applied. */
    
    ///
    public native @ByRef LineSet PaintUniformColor(@Const @ByRef Vector3d color);

    /** \brief Factory function to create a LineSet from two PointClouds
     *  (\p cloud0, \p cloud1) and a correspondence set.
     * 
     *  @param cloud0 First point cloud.
     *  @param cloud1 Second point cloud.
     *  @param correspondences Set of correspondences. */
    
    ///
    public static native @SharedPtr LineSet CreateFromPointCloudCorrespondences(
                @Const @ByRef PointCloud cloud0,
                @Const @ByRef PointCloud cloud1,
                @StdVector std::pair<int,int> correspondences);

    /** \brief Factory function to create a LineSet from an OrientedBoundingBox.
     * 
     *  @param box The input bounding box. */
    
    ///
    public static native @SharedPtr LineSet CreateFromOrientedBoundingBox(
                @Const @ByRef OrientedBoundingBox box);

    /** \brief Factory function to create a LineSet from an
     *  AxisAlignedBoundingBox.
     * 
     *  @param box The input bounding box. */
    
    ///
    public static native @SharedPtr LineSet CreateFromAxisAlignedBoundingBox(
                @Const @ByRef AxisAlignedBoundingBox box);

    /** Factory function to create a LineSet from edges of a triangle mesh.
     * 
     *  @param mesh The input triangle mesh. */
    
    ///
    public static native @SharedPtr LineSet CreateFromTriangleMesh(
                @Const @ByRef TriangleMesh mesh);

    /** Factory function to create a LineSet from edges of a tetra mesh.
     * 
     *  @param mesh The input tetra mesh. */
    
    ///
    public static native @SharedPtr LineSet CreateFromTetraMesh(@Const @ByRef TetraMesh mesh);

    /** Factory function to create a LineSet from intrinsic and extrinsic
     *  matrices.
     * 
     *  @param view_width_px The width of the view, in pixels
     *  @param view_height_px The height of the view, in pixels
     *  @param intrinsic The intrinsic matrix
     *  @param extrinsic The extrinsic matrix */
    public static native @SharedPtr LineSet CreateCameraVisualization(
                int view_width_px,
                int view_height_px,
                @Const @ByRef Matrix3d intrinsic,
                @Const @ByRef Matrix4d extrinsic,
                double scale/*=1.0*/);
    public static native @SharedPtr LineSet CreateCameraVisualization(
                int view_width_px,
                int view_height_px,
                @Const @ByRef Matrix3d intrinsic,
                @Const @ByRef Matrix4d extrinsic);
    /** Points coordinates. */
    public native @StdVector Vector3d points_(); public native LineSet points_(Vector3d setter);
    /** Lines denoted by the index of points forming the line. */
    public native @StdVector Vector2i lines_(); public native LineSet lines_(Vector2i setter);
    /** RGB colors of lines. */
    public native @StdVector Vector3d colors_(); public native LineSet colors_(Vector3d setter);
}
