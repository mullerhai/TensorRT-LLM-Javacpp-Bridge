// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3D.*;


/** \class TetraMesh
 * 
 *  \brief Tetra mesh contains vertices and tetrahedra represented by the
 *  indices to the vertices. */
@Namespace("open3d::geometry") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DConfig.class)
public class TetraMesh extends MeshBase {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public TetraMesh(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public TetraMesh(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public TetraMesh position(long position) {
        return (TetraMesh)super.position(position);
    }
    @Override public TetraMesh getPointer(long i) {
        return new TetraMesh((Pointer)this).offsetAddress(i);
    }

    /** \brief Default Constructor. */
    
    ///
    public TetraMesh() { super((Pointer)null); allocate(); }
    private native void allocate();
    /** \brief Parameterized Constructor.
     * 
     *  @param vertices Vertex coordinates.
     *  @param tetras List of tetras denoted by the index of points forming the
     *  tetra. */
    public TetraMesh(@StdVector Vector3d vertices,
                  @StdVector Vector4i tetras) { super((Pointer)null); allocate(vertices, tetras); }
    private native void allocate(@StdVector Vector3d vertices,
                  @StdVector Vector4i tetras);
    public native @ByRef TetraMesh Clear();
    public native @ByRef @Name("operator +=") TetraMesh addPut(@Const @ByRef TetraMesh mesh);
    public native @ByVal @Name("operator +") TetraMesh add(@Const @ByRef TetraMesh mesh);

    /** \brief Function that removes duplicated vertices, i.e., vertices that
     *  have identical coordinates. */
    public native @ByRef TetraMesh RemoveDuplicatedVertices();

    /** \brief Function that removes duplicated tetrahedra, i.e., removes
     *  tetrahedra that reference the same four vertices, independent of their
     *  order. */
    public native @ByRef TetraMesh RemoveDuplicatedTetras();

    /** \brief This function removes vertices from the tetra mesh that are not
     *  referenced in any tetrahedron of the mesh. */
    public native @ByRef TetraMesh RemoveUnreferencedVertices();

    /** \brief Function that removes degenerate tetrahedra, i.e., tetrahedra
     *  that reference a single vertex multiple times in a single tetrahedron.
     *  They are usually the product of removing duplicated vertices. */
    public native @ByRef TetraMesh RemoveDegenerateTetras();

    /** Returns {@code true} if the mesh contains tetras. */
    
    ///
    public native @Cast("bool") boolean HasTetras();

    /** \brief Function to extract a triangle mesh of the specified iso-surface
     *  at a level This method applies primal contouring and generates triangles
     *  for each tetrahedron.
     * 
     *  @param level specifies the level.
     *  @param values specifies values per-vertex. */
    
    ///
    public native @SharedPtr TriangleMesh ExtractTriangleMesh(
                @StdVector DoublePointer values, double level);
    public native @SharedPtr TriangleMesh ExtractTriangleMesh(
                @StdVector DoubleBuffer values, double level);
    public native @SharedPtr TriangleMesh ExtractTriangleMesh(
                @StdVector double[] values, double level);

    /** \brief Function that creates a tetrahedral mesh (TetraMeshFactory.cpp).
     *  from a point cloud.
     * 
     *  The method creates the Delaunay triangulation
     *  using the implementation from Qhull. */
    public static native @ByVal std::tuple<std::shared_ptr<open3d::geometry::TetraMesh>,std::vector<size_t> > CreateFromPointCloud(@Const @ByRef PointCloud point_cloud);
    /** List of tetras denoted by the index of points forming the tetra. */
    public native @StdVector Vector4i tetras_(); public native TetraMesh tetras_(Vector4i setter);
}
