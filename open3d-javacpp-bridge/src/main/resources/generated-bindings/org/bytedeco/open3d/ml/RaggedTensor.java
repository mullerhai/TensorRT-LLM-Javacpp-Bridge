// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.ml;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DML.*;


/** A RaggedTensor is a tensor with ragged dimensions, whose slice
 *  may have different lengths. We define a container for ragged tensor
 *  to support operations involving batches whose elements may have different
 *  shape. */
@NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DMLConfig.class)
public class RaggedTensor extends CustomClassHolder {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public RaggedTensor(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public RaggedTensor(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public RaggedTensor position(long position) {
        return (RaggedTensor)super.position(position);
    }
    @Override public RaggedTensor getPointer(long i) {
        return new RaggedTensor((Pointer)this).offsetAddress(i);
    }

    public RaggedTensor() { super((Pointer)null); allocate(); }
    private native void allocate();

    /** Constructor for creating RaggedTensor with values and row_splits. */
    
    ///
    public RaggedTensor(@ByVal Tensor values, @ByVal Tensor row_splits) { super((Pointer)null); allocate(values, row_splits); }
    private native void allocate(@ByVal Tensor values, @ByVal Tensor row_splits);

    /** Creates a RaggedTensor with rows partitioned by row_splits.
     * 
     *  The returned {@code RaggedTensor} corresponds with the python list defined by:
     *  <pre>{@code python
     *  result = [values[row_splits[i]:row_splits[i + 1]]
     *            for i in range(len(row_splits) - 1)]
     *  }</pre> */
    public native @ByVal c10::intrusive_ptr<RaggedTensor> FromRowSplits(@ByVal Tensor values,
                                                       @ByVal Tensor row_splits,
                                                       @Cast("bool") boolean validate/*=true*/);
    public native @ByVal c10::intrusive_ptr<RaggedTensor> FromRowSplits(@ByVal Tensor values,
                                                       @ByVal Tensor row_splits);

    /** Returns _values tensor. */
    public native @ByVal Tensor GetValues();

    /** Returns _row_splits tensor. */
    public native @ByVal Tensor GetRowSplits();

    /** Returns string representation. */
    
    ///
    public native @StdString BytePointer ToString();

    /** Pythonic __getitem__ for RaggedTensor.
     * 
     *  Returns a slice of values based on row_splits. It can be
     *  used to retrieve i'th batch element. Currently it
     *  only supports a single integer index. */
    
    ///
    public native @ByVal Tensor GetItem(int key);

    /** Pythonic __len__ for RaggedTensor.
     * 
     *  Returns number of batch elements. */
    public native @Cast("int64_t") long Len();

    /** Copy Tensor to the same device. */
    public native @ByVal c10::intrusive_ptr<RaggedTensor> Clone();

    public native @ByVal c10::intrusive_ptr<RaggedTensor> Concat(
                @ByVal c10::intrusive_ptr<RaggedTensor> r_tensor, @Cast("int64_t") long axis);
}
