// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.pipelines;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DPipelines.*;


/** \class HuberLoss
 * 
 *  HuberLoss loss function used for outlier rejection.
 * 
 *  The loss p(r) for a given residual 'r' is computed as follow:
 *    p(r) = r^2                   for abs(r) <= k,
 *    p(r) = k^2 * (abs(r) - k/2)  for abs(r) > k
 * 
 *  For more information: http://en.wikipedia.org/wiki/Huber_Loss_Function */
@Namespace("open3d::pipelines::registration") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DPipelinesConfig.class)
public class HuberLoss extends RobustKernel {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public HuberLoss(Pointer p) { super(p); }

    /** \brief Parametrized Constructor.
     * 
     *  @param k Is the scaling parameter of the huber loss function. 'k'
     *  corresponds to 'delta' on this page:
     *  http://en.wikipedia.org/wiki/Huber_Loss_Function */
    
    ///
    public HuberLoss(double k) { super((Pointer)null); allocate(k); }
    private native void allocate(double k);

    /** The weight w(r) for a given residual 'r' is computed as follow:
     *    w(r) = 1.0         for abs(r) <= k,
     *    w(r) = k / abs(r)  for abs(r) > k
     *  Where k Is the scaling parameter of the loss function.
     * 
     *  @param residual Residual value obtained during the optimization step. */
    public native double Weight(double residual);
    /** Scaling parameter. */
    public native double k_(); public native HuberLoss k_(double setter);
}
