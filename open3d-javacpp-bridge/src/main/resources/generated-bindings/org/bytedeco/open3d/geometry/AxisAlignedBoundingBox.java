// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.open3d.geometry;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.open3d.global.Open3DGeometry.*;


/** \class AxisAlignedBoundingBox
 * 
 *  \brief A bounding box that is aligned along the coordinate axes and defined
 *  by the min_bound and max_bound.
 * 
 *   The AxisAlignedBoundingBox uses the coordinate axes for bounding box
 *   generation. This means that the bounding box is oriented along the
 *   coordinate axes. */
@Namespace("open3d::geometry") @NoOffset @Properties(inherit = org.bytedeco.open3d.presets.Open3DGeometryConfig.class)
public class AxisAlignedBoundingBox extends Geometry3D {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public AxisAlignedBoundingBox(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public AxisAlignedBoundingBox(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public AxisAlignedBoundingBox position(long position) {
        return (AxisAlignedBoundingBox)super.position(position);
    }
    @Override public AxisAlignedBoundingBox getPointer(long i) {
        return new AxisAlignedBoundingBox((Pointer)this).offsetAddress(i);
    }

    /** \brief Default constructor.
     * 
     *  Creates an empty Axis Aligned Bounding Box. */
    
    ///
    public AxisAlignedBoundingBox() { super((Pointer)null); allocate(); }
    private native void allocate();
    /** \brief Parameterized constructor.
     * 
     *  @param min_bound Lower bounds of the bounding box for all axes.
     *  @param max_bound Upper bounds of the bounding box for all axes. */
    public AxisAlignedBoundingBox(@Const @ByRef Vector3d min_bound,
                               @Const @ByRef Vector3d max_bound) { super((Pointer)null); allocate(min_bound, max_bound); }
    private native void allocate(@Const @ByRef Vector3d min_bound,
                               @Const @ByRef Vector3d max_bound);
    public native @ByRef AxisAlignedBoundingBox Clear();
    public native @Cast("bool") boolean IsEmpty();
    public native @ByVal Vector3d GetMinBound();
    public native @ByVal Vector3d GetMaxBound();
    public native @ByVal Vector3d GetCenter();

    /** Returns the object itself */
    public native @ByVal AxisAlignedBoundingBox GetAxisAlignedBoundingBox();

    /** Returns an oriented bounding box with the same dimensions
     *  and orientation as the object */
    public native @ByVal OrientedBoundingBox GetOrientedBoundingBox(
                @Cast("bool") boolean robust/*=false*/);
    public native @ByVal OrientedBoundingBox GetOrientedBoundingBox();

    /** Returns an oriented bounding box with the same dimensions
     *  and orientation as the object */
    public native @ByVal OrientedBoundingBox GetMinimalOrientedBoundingBox(
                @Cast("bool") boolean robust/*=false*/);
    public native @ByVal OrientedBoundingBox GetMinimalOrientedBoundingBox();
    public native @ByRef AxisAlignedBoundingBox Transform(
                @Const @ByRef Matrix4d transformation);
    
    ///
    public native @ByRef AxisAlignedBoundingBox Translate(
                @Const @ByRef Vector3d translation, @Cast("bool") boolean relative/*=true*/);
    public native @ByRef AxisAlignedBoundingBox Translate(
                @Const @ByRef Vector3d translation);

    /** \brief Scales the axis-aligned bounding boxes.
     *  If {@code mi} is the min_bound and {@code ma} is the max_bound of
     *  the axis aligned bounding box, and {@code s} and {@code c} are the
     *  provided scaling factor and center respectively, then the new
     *  min_bound and max_bound are given by {@code mi = c + s (mi - c)}
     *  and {@code ma = c + s (ma - c)}.
     * 
     *  @param scale The scale parameter.
     *  @param center Center used for the scaling operation. */
    public native @ByRef AxisAlignedBoundingBox Scale(
                double scale, @Const @ByRef Vector3d center);

    /** \brief an AxisAlignedBoundingBox can not be rotated. This method
     *  will throw an error. */
    public native @ByRef AxisAlignedBoundingBox Rotate(
                @Const @ByRef Matrix3d R, @Const @ByRef Vector3d center);

    public native @ByRef @Name("operator +=") AxisAlignedBoundingBox addPut(@Const @ByRef AxisAlignedBoundingBox other);

    /** Get the extent/length of the bounding box in x, y, and z dimension. */
    public native @ByVal Vector3d GetExtent();

    /** Returns the half extent of the bounding box. */
    public native @ByVal Vector3d GetHalfExtent();

    /** Returns the maximum extent, i.e. the maximum of X, Y and Z axis'
     *  extents. */
    public native double GetMaxExtent();

    /** Calculates the percentage position of the given x-coordinate within
     *  the x-axis range of this AxisAlignedBoundingBox. */
    public native double GetXPercentage(double x);

    /** Calculates the percentage position of the given y-coordinate within
     *  the y-axis range of this AxisAlignedBoundingBox. */
    public native double GetYPercentage(double y);

    /** Calculates the percentage position of the given z-coordinate within
     *  the z-axis range of this AxisAlignedBoundingBox. */
    public native double GetZPercentage(double z);

    /** Returns the volume of the bounding box. */
    public native double Volume();
    /** Returns the eight points that define the bounding box. */
    
    ///
    public native @StdVector Vector3d GetBoxPoints();

    /** Return indices to points that are within the bounding box.
     * 
     *  @param points A list of points. */
    public native @StdVector SizeTPointer GetPointIndicesWithinBoundingBox(
                @StdVector Vector3d points);

    /** Returns the 3D dimensions of the bounding box in string format. */
    
    ///
    public native @StdString BytePointer GetPrintInfo();

    /** Creates the bounding box that encloses the set of points.
     * 
     *  @param points A list of points. */
    public static native @ByVal AxisAlignedBoundingBox CreateFromPoints(
                @StdVector Vector3d points);
    /** The lower x, y, z bounds of the bounding box. */
    public native @ByRef Vector3d min_bound_(); public native AxisAlignedBoundingBox min_bound_(Vector3d setter);
    /** The upper x, y, z bounds of the bounding box. */
    public native @ByRef Vector3d max_bound_(); public native AxisAlignedBoundingBox max_bound_(Vector3d setter);
    /** The color of the bounding box in RGB. */
    public native @ByRef Vector3d color_(); public native AxisAlignedBoundingBox color_(Vector3d setter);
}
