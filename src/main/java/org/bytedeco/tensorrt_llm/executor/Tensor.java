// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.tensorrt_llm.executor;

import org.bytedeco.tensorrt_llm.*;
import org.bytedeco.tensorrt_llm.executor.*;
import org.bytedeco.tensorrt_llm.executor.kvcache.*;
import org.bytedeco.tensorrt_llm.runtime.*;
import org.bytedeco.tensorrt_llm.batch_manager.*;
import org.bytedeco.tensorrt_llm.batch_manager.kvcache.*;
import org.bytedeco.tensorrt_llm.common.*;
import org.bytedeco.tensorrt_llm.mpi.*;
import org.bytedeco.tensorrt_llm.nvinfer.*;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.tensorrt_llm.global.TRTLLM.*;


@Namespace("tensorrt_llm::executor") @NoOffset @Properties(inherit = tensorrt_llm.presets.TRTLLMFullConfig.class)
public class Tensor extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public Tensor(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public Tensor(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public Tensor position(long position) {
        return (Tensor)super.position(position);
    }
    @Override public Tensor getPointer(long i) {
        return new Tensor((Pointer)this).offsetAddress(i);
    }

    
    //!

    /** Allocate a cpu tensor with the given shape and data type.
     * 
     *  @param shape The shape of the tensor.
     *  @param dataType The data type of the tensor. */
    public static native @ByVal Tensor cpu(DataType dataType, @ByVal(nullValue = "tensorrt_llm::executor::Shape{}") Shape shape);
    public static native @ByVal Tensor cpu(DataType dataType);

    
    //!
    public native @ByVal Tensor copyToCpu(@Cast("tensorrt_llm::executor::Tensor::CudaStreamPtr") Pointer stream/*=nullptr*/);
    public native @ByVal Tensor copyToCpu();

    /** Allocate a cpu tensor in pinned memory with the given shape and data type.
     * 
     *  @param shape The shape of the tensor.
     *  @param dataType The data type of the tensor. */
    public static native @ByVal Tensor pinned(DataType dataType, @ByVal(nullValue = "tensorrt_llm::executor::Shape{}") Shape shape);
    public static native @ByVal Tensor pinned(DataType dataType);

    
    //!
    public native @ByVal Tensor copyToPinned(@Cast("tensorrt_llm::executor::Tensor::CudaStreamPtr") Pointer stream/*=nullptr*/);
    public native @ByVal Tensor copyToPinned();

    /** Allocate a cpu tensor in pooled pinned memory with the given shape and data type.
     * 
     *  @param shape The shape of the tensor.
     *  @param dataType The data type of the tensor. */
    public static native @ByVal Tensor pooledPinned(DataType dataType, @ByVal(nullValue = "tensorrt_llm::executor::Shape{}") Shape shape);
    public static native @ByVal Tensor pooledPinned(DataType dataType);

    
    //!
    public native @ByVal Tensor copyToPooledPinned(@Cast("tensorrt_llm::executor::Tensor::CudaStreamPtr") Pointer stream/*=nullptr*/);
    public native @ByVal Tensor copyToPooledPinned();

    /** Allocate a tensor in managed memory (UVM) with the given shape and data type.
     * 
     *  @param shape The shape of the tensor.
     *  @param dataType The data type of the tensor. */
    public static native @ByVal Tensor managed(DataType dataType, @ByVal(nullValue = "tensorrt_llm::executor::Shape{}") Shape shape);
    public static native @ByVal Tensor managed(DataType dataType);

    
    //!
    public native @ByVal Tensor copyToManaged(@Cast("tensorrt_llm::executor::Tensor::CudaStreamPtr") Pointer stream/*=nullptr*/);
    public native @ByVal Tensor copyToManaged();

    /** Allocate a gpu tensor with the given shape and data type on a particular cuda stream.
     * 
     *  @param shape The shape of the tensor.
     *  @param stream Specifies the CUDA stream on which to allocate the tensor for GPU memory.
     *  @param dataType The data type of the tensor. */
    public static native @ByVal Tensor gpu(DataType dataType, @Cast("tensorrt_llm::executor::Tensor::CudaStreamPtr") Pointer stream, @ByVal(nullValue = "tensorrt_llm::executor::Shape{}") Shape shape);
    public static native @ByVal Tensor gpu(DataType dataType, @Cast("tensorrt_llm::executor::Tensor::CudaStreamPtr") Pointer stream);

    
    //!
    public native @ByVal Tensor copyToGpu(@Cast("tensorrt_llm::executor::Tensor::CudaStreamPtr") Pointer stream);

    /** Wrap a data pointer into a tensor without taking ownership.
     * 
     *  @param shape The shape of the tensor.
     *  @param dataType The data type of the tensor.
     *  @param stream Specifies the CUDA stream on which to allocate the tensor for GPU memory. */
    
    //!
    public static native @ByVal Tensor of(DataType dataType, Pointer data, @ByVal Shape shape);

    /** Wrap a data pointer into a tensor without taking ownership.
     * 
     *  @param shape The shape of the tensor.
     *  @param dataType The data type of the tensor.
     *  @param stream Specifies the CUDA stream on which to allocate the tensor for GPU memory. */

    /** Wrap any container into a tensor without taking ownership.
     * 
     *  @param shape The shape of the tensor.
     *  @param dataType The data type of the tensor.
     *  @param stream Specifies the CUDA stream on which to allocate the tensor for GPU memory. */

    public Tensor() { super((Pointer)null); allocate(); }
    @NoException(true) private native void allocate();

    public Tensor(@Const @ByRef Tensor other) { super((Pointer)null); allocate(other); }
    @NoException(true) private native void allocate(@Const @ByRef Tensor other);

    public native @ByRef @Name("operator =") @NoException(true) Tensor put(@Const @ByRef Tensor other);

    /**
     *  \brief Returns a pointer to underlying array.
     *  */
    
    
    //!
    //!
    public native Pointer getData();

    /**
     *  \brief Returns a pointer to underlying array.
     *  */

    /**
     *  \brief Returns the data type of the buffer.
     *  */
    
    
    //!
    //!
    public native DataType getDataType();

    /**
     *  \brief Returns the memory type of the buffer.
     *  */
    
    
    //!
    //!
    public native MemoryType getMemoryType();

    /**
     *  \brief Returns the tensor dimensions.
     *  */
    
    
    //!
    //!
    public native @ByVal Shape getShape();

    /**
     *  \brief Returns the number of elements in the tensor.
     *  */
    
    
    //!
    //!
    public native @Cast("std::size_t") long getSize();

    /**
     *  \brief Returns the size of the tensor in bytes.
     *  */
    
    
    //!
    //!
    public native @Cast("std::size_t") long getSizeInBytes();

    /**
     *  \brief Set the entire memory to zero.
     * 
     *  @param stream Must be a valid CUDA stream if the memory type is GPU. */
    
    
    //!
    //!
    public native void setZero(@Cast("tensorrt_llm::executor::Tensor::CudaStreamPtr") Pointer stream/*=nullptr*/);
    public native void setZero();

    /**
     *  \brief Copy the data and shape from another tensor.
     * 
     *  @param other A tensor to copy from.
     *  @param stream Must be a valid CUDA stream if the memory type is GPU. */
    public native void setFrom(@Const @ByRef Tensor other, @Cast("tensorrt_llm::executor::Tensor::CudaStreamPtr") Pointer stream/*=nullptr*/);
    public native void setFrom(@Const @ByRef Tensor other);

    public native @Cast("bool") @Name("operator bool") boolean asBoolean();

    public native @Cast("bool") @Name("operator ==") boolean equals(@Const @ByRef Tensor rhs);

    public native @Cast("bool") @Name("operator !=") boolean notEquals(@Const @ByRef Tensor rhs);
}
