// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package org.bytedeco.tensorrt_llm;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.tensorrt_llm.global.TRTLLM.*;


@Namespace("tensorrt_llm::runtime") @Properties(inherit = tensorrt_llm.presets.TRTLLMFullConfig.class)
public class IBuffer extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public IBuffer(Pointer p) { super(p); }
    public IBuffer asIBuffer() { return this; }

    
    
    //!
    //!

    /**
     *  \brief Returns a pointer to underlying array.
     *  */
    
    
    //!
    //!
    public Pointer data() { return asIBuffer()._data(); }
    private native @Name("data") Pointer _data();

    /**
     *  \brief Returns a pointer to underlying array.
     *  */

    /**
     *  \brief Returns a pointer to the underlying array at a given element index.
     *  */
    
    
    //!
    //!
    public Pointer data(long index) { return asIBuffer()._data(index); }
    private native @Name("data") Pointer _data(@Cast("std::size_t") long index);

    /**
     *  \brief Returns a pointer to the underlying array at a given element index.
     *  */

    /**
     *  \brief Returns the size (in number of elements) of the buffer.
     *  */
    
    
    //!
    //!
    public long getSize() { return asIBuffer()._getSize(); }
    private native @Cast("std::size_t") @Name("getSize") long _getSize();

    /**
     *  \brief Returns the size (in bytes) of the buffer.
     *  */
    
    
    //!
    //!
    public long getSizeInBytes() { return asIBuffer()._getSizeInBytes(); }
    private native @Cast("std::size_t") @Name("getSizeInBytes") long _getSizeInBytes();

    /**
     *  \brief Returns the capacity of the buffer.
     *  */
    
    
    //!
    //!
    public long getCapacity() { return asIBuffer()._getCapacity(); }
    private native @Cast("std::size_t") @Name("getCapacity") long _getCapacity();

    /**
     *  \brief Returns the data type of the buffer.
     *  */
    public DataType getDataType() { return asIBuffer()._getDataType(); }
    private native @ByVal @Name("getDataType") DataType _getDataType();

    public static native @Cast("const char*") BytePointer getDataTypeName(@ByVal DataType dataType);

    
    
    //!
    //!
    public BytePointer getDataTypeName() { return asIBuffer()._getDataTypeName(); }
    private native @Name("getDataTypeName") @Cast("const char*") BytePointer _getDataTypeName();

    /**
     *  \brief Returns the memory type of the buffer.
     *  */
    public int getMemoryType() { return asIBuffer()._getMemoryType(); }
    private native @Cast("tensorrt_llm::runtime::MemoryType") @Name("getMemoryType") int _getMemoryType();

    
    
    //!
    //!
    public BytePointer getMemoryTypeName() { return asIBuffer()._getMemoryTypeName(); }
    private native @Name("getMemoryTypeName") @Cast("const char*") BytePointer _getMemoryTypeName();

    /**
     *  \brief Resizes the buffer. This is a no-op if the new size is smaller than or equal to the current capacity.
     *  */
    
    
    //!
    //!
    public void resize(long newSize) { asIBuffer()._resize(newSize); }
    private native @Name("resize") void _resize(@Cast("std::size_t") long newSize);

    /**
     *  \brief Releases the buffer. It will be reset to nullptr.
     *  */
    public void release() { asIBuffer()._release(); }
    private native @Name("release") void _release();

    /**
     *  \brief Not allowed to copy.
     *  */
    

    /**
     *  \brief Not allowed to copy.
     *  */
    

    /**
     *  \brief Creates a sliced view on the underlying {@code buffer}. The view will have the same data type as {@code buffer}.
     * 
     *  @param buffer The buffer to view.
     *  @param offset The offset of the view.
     *  @param size The size of the view.
     *  @return A view on the {@code buffer}.
     *  */
    public static IBuffer slice(IBuffer buffer, long offset, long size) { return _slice(buffer.asIBuffer(), offset, size); }
    private static native @ByVal @Name("slice") IBuffer _slice(@ByVal IBuffer buffer, @Cast("std::size_t") long offset, @Cast("std::size_t") long size);

    public static IBuffer slice(IBuffer buffer, long offset) { return _slice(buffer.asIBuffer(), offset); }
    private static native @ByVal @Name("slice") IBuffer _slice(@ByVal IBuffer buffer, @Cast("std::size_t") long offset);

    /**
     *  \brief Returns a view on the underlying {@code tensor} which can be independently resized.
     * 
     *  @param tensor The tensor to view.
     *  @return A view on the {@code tensor}.
     *  */
    
    
    //!
    //!
    //!
    public static IBuffer view(IBuffer tensor) { return _view(tensor.asIBuffer()); }
    private static native @ByVal @Name("view") IBuffer _view(@ByVal IBuffer tensor);

    /**
     *  \brief Returns a view on the underlying {@code tensor} with a different size.
     * 
     *  @param tensor The tensor to view.
     *  @param size The size of the view.
     *  @return A view on the {@code tensor}.
     *  */
    public static IBuffer view(IBuffer tensor, long size) { return _view(tensor.asIBuffer(), size); }
    private static native @ByVal @Name("view") IBuffer _view(@ByVal IBuffer tensor, @Cast("std::size_t") long size);

    /**
     *  \brief Wraps the given {@code data} in an {@code IBuffer}. The {@code IBuffer} will not own the underlying {@code data} and cannot
     *  be resized beyond {@code capacity}.
     * 
     *  @param data The data to wrap.
     *  @param type The data type of the {@code data}.
     *  @param size The size of the buffer.
     *  @param capacity The capacity of the buffer.
     *  @return An {@code IBuffer}. */
    public static native @ByVal IBuffer wrap(Pointer data, @ByVal DataType type, @Cast("std::size_t") long size, @Cast("std::size_t") long _capacity);

    public static native @ByVal IBuffer wrap(Pointer data, @ByVal DataType type, @Cast("std::size_t") long size);

    /**
     *  \brief Determine the memory type of a pointer.
     *  */
    public static native @Cast("tensorrt_llm::runtime::MemoryType") int memoryType(@Const Pointer data);
}
