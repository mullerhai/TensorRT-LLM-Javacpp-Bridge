// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.kernels;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Kernels.*;


@Name("tensorrt_llm::kernels::TopKSamplingKernelParams<float>") @Properties(inherit = tensorrt_llm.presets.KernelsConfig.class)
public class TopKSamplingKernelParams extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public TopKSamplingKernelParams() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public TopKSamplingKernelParams(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public TopKSamplingKernelParams(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public TopKSamplingKernelParams position(long position) {
        return (TopKSamplingKernelParams)super.position(position);
    }
    @Override public TopKSamplingKernelParams getPointer(long i) {
        return new TopKSamplingKernelParams((Pointer)this).offsetAddress(i);
    }


    /** Input buffer [batchSize, maxTokensPerStep, vocabSizePadded].
     *  Log probabilities of each token in the vocab. If logitsHasProbs is true,
     *  logProbs must contain **just** probabilities instead of log probabilities. */
    public native @Const FloatPointer logProbs(); public native TopKSamplingKernelParams logProbs(FloatPointer setter);
    /** input buffer [batchSize][tokensPerStep, vocabSizePadded] array of pointers to logits.
     *  If nullptr, logProbs is used. */
    @MemberGetter public native @Const FloatPointer logProbsPtrs(int i);
    @MemberGetter public native @Cast("const float*const*") PointerPointer logProbsPtrs();

    /** output buffer [maxBatchSize][maxSeqLen], optional. Contains pointers to rows
     *  with output tokens per request. If nullptr, outputIds must be provided. */
    public native IntPointer outputIdsPtrs(int i); public native TopKSamplingKernelParams outputIdsPtrs(int i, IntPointer setter);
    public native @Cast("tensorrt_llm::runtime::TokenIdType**") PointerPointer outputIdsPtrs(); public native TopKSamplingKernelParams outputIdsPtrs(PointerPointer setter);
    /** output buffer [maxBatchSize, maxSeqLen], optional. Tensor to store output tokens.
     *  Not used if outputIdsPtrs != nullptr */
    public native IntPointer outputIds(); public native TopKSamplingKernelParams outputIds(IntPointer setter);

    /** Required. Pointer to the workspace of size returned by getTopKWorkspaceSize.
     *  Has to be pre-allocated by caller.
     *  Function does not take ownership of the buffer */
    public native Pointer workspace(); public native TopKSamplingKernelParams workspace(Pointer setter);

    /** input buffer [maxBatchSize], optional. EOS token ids per request */
    public native @Const IntPointer endIds(); public native TopKSamplingKernelParams endIds(IntPointer setter);

    /** input/output buffer [maxBatchSize], optional. If nullptr, seqLen is 0
     *  Current sequence length of the request. Set up to, but excluding endId token. */
    public native IntPointer sequenceLengths(); public native TopKSamplingKernelParams sequenceLengths(IntPointer setter);
    /** input buffer[batchSize], optional. Indices of rows of data in memory pool.
     *  Linear indexing (batchIdx) is used if nullptr. */
    public native @Const IntPointer batchSlots(); public native TopKSamplingKernelParams batchSlots(IntPointer setter);
    /** input buffer [maxBatchSize], optional. Number of tokens per step for each request.
     *  It is assumed that all requests have maxTokensPerStep tokens per step if nullptr. */
    public native @Const IntPointer tokensPerStep(); public native TopKSamplingKernelParams tokensPerStep(IntPointer setter);

    /** input buffer [maxBatchSize], optional. If true, request exits early. */
    public native @Const FinishedState finishedInput(); public native TopKSamplingKernelParams finishedInput(FinishedState setter);
    /** output buffer [maxBatchSize], optional.
     *  Set to true if sequence has finished (if finished || outputId == endId). */
    public native FinishedState finishedOutput(); public native TopKSamplingKernelParams finishedOutput(FinishedState setter);
    /** input buffer [maxBatchSize]. Flags whether to skip decoding per request */
    public native @Const BoolPointer skipDecode(); public native TopKSamplingKernelParams skipDecode(BoolPointer setter);

    /** input/output buffer [maxBatchSize], optional.
     *  Cumulative log probability of selected tokens. Ignored if nullptr */
    public native FloatPointer cumLogProbs(); public native TopKSamplingKernelParams cumLogProbs(FloatPointer setter);
    /** output buffer
     *  [maxBatchSize, maxTopK] when returnAllSelectedTokens, otherwise [maxSeqLen, maxBatchSize]
     *  Log probs is the probability induced by the top-k sampling.
     *  If normalizeLogProbs is true, we normalize the probability 'expLogit' of the selected token
     *  by the probability 's_sum' of a set of top-k tokens, meaning the logProb is the probability
     *  of the selected token, conditioned on the event that it is selected,
     *  i.e., log_prob = log P(i | i is in top-k) = log(expLogit / s_sum).
     *  Ignored if nullptr. */
    public native FloatPointer outputLogProbs(); public native TopKSamplingKernelParams outputLogProbs(FloatPointer setter);

    /** input buffer [maxBatchSize], optional. Initialized curand states.
     *  If nullptr, 1 is always used. */
    public native Pointer curandState(); public native TopKSamplingKernelParams curandState(Pointer setter);
    /** input buffer [maxBatchSize]. K for topK sampling per request.
     *  Supported K is in range [1; 1024]. Where K=1 is greedy search.
     *  If nullptr maxTopK is used for all requests. */
    public native @Const IntPointer topKs(); public native TopKSamplingKernelParams topKs(IntPointer setter);
    /** input buffer [maxBatchSize]. Probability for topP sampling per request.
     *  Supported P is in range (0.0, 1.0]. If nullptr, topP is used for all requests */
    public native @Const FloatPointer topPs(); public native TopKSamplingKernelParams topPs(FloatPointer setter);
    /** maximum among all topKs K for topK sampling */
    public native int maxTopK(); public native TopKSamplingKernelParams maxTopK(int setter);
    /** probability for topP sampling. */
    public native float maxTopP(); public native TopKSamplingKernelParams maxTopP(float setter);

    public native int batchSize(); public native TopKSamplingKernelParams batchSize(int setter);
    public native int maxBatchSize(); public native TopKSamplingKernelParams maxBatchSize(int setter);
    public native int vocabSizePadded(); public native TopKSamplingKernelParams vocabSizePadded(int setter);
    public native int maxTokensPerStep(); public native TopKSamplingKernelParams maxTokensPerStep(int setter);
    public native int maxSeqLen(); public native TopKSamplingKernelParams maxSeqLen(int setter);

    /** when set to True outputLogProbs are normalized to TopK */
    public native boolean normalizeLogProbs(); public native TopKSamplingKernelParams normalizeLogProbs(boolean setter);
    /** flag to highlight that logProbs contains probabilities */
    public native boolean logitsHasProbs(); public native TopKSamplingKernelParams logitsHasProbs(boolean setter);
    /** flag to return all selected TopK results */
    public native boolean returnAllSelectedTokens(); public native TopKSamplingKernelParams returnAllSelectedTokens(boolean setter);
    /** flag to set strict TopP boundary.
     *  If true, when randNum <=0.0f, the selection is completed, even if K draft tokens are not reached.
     *  If false, when randNum <=0.0f, the selection will continue until it reaches K tokens. */
    public native boolean strictTopPBoundary(); public native TopKSamplingKernelParams strictTopPBoundary(boolean setter);

    /** flag to return all selected TopK results per request. */
    public native @Const BoolPointer returnAllSelectedTokensPerSlot(); public native TopKSamplingKernelParams returnAllSelectedTokensPerSlot(BoolPointer setter);

    /** output buffer [maxBatchSize], optional.
     *  Store the multinomial sampled target token id in TopK/TopP sampled tokens when returnAllSelectedTokens==True.
     *  Only return when skipOutputIdCurrentStep != nullptr && skipOutputIdCurrentStep == False */
    public native IntPointer outputIdCurrentStep(); public native TopKSamplingKernelParams outputIdCurrentStep(IntPointer setter);
    /** input buffer [maxBatchSize]. Determine if multinomial sampling is required when returnAllSelectedTokens==True. */
    public native @Const BoolPointer skipOutputIdCurrentStep(); public native TopKSamplingKernelParams skipOutputIdCurrentStep(BoolPointer setter);

    public native void checkParams();
}
