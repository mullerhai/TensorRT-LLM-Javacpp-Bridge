// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.kernels;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Kernels.*;

/**
 * Java bindings for {@code tensorrt_llm/kernels/moeAlignKernels.h}.
 * Aligns token distribution across experts for block-size-compatible matrix multiplication.
 */
@Properties(inherit = tensorrt_llm.presets.KernelsConfig.class)
public class MoeAlignKernels {
    static { Loader.load(); }

    /**
     * Aligns token distribution across experts to be compatible with block size for matrix multiplication.
     *
     * @param topk_ids              Input tensor with expert IDs per token [total_tokens, top_k]
     * @param topk_ids_dtype_size   Size of the dtype (e.g. sizeof(int32_t))
     * @param sorted_token_ids      Output tensor for sorted token indices
     * @param expert_ids            Output tensor for expert IDs per block
     * @param num_tokens_post_pad   Output tensor for total tokens after padding (single int32)
     * @param num_experts           Total number of experts
     * @param block_size            Block size for matrix multiplication alignment
     * @param numel                 Total number of elements in topk_ids
     * @param max_num_tokens_padded Maximum number of tokens after padding
     * @param stream                CUDA stream
     */
    @Namespace("tensorrt_llm::kernels")
    public static native void invokeMoeAlignBlockSize(
        @Const Pointer topk_ids,
        int topk_ids_dtype_size,
        IntPointer sorted_token_ids,
        IntPointer expert_ids,
        IntPointer num_tokens_post_pad,
        int num_experts,
        int block_size,
        int numel,
        int max_num_tokens_padded,
        Pointer stream);
}

