// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.kernels;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Kernels.*;

/**
 * Java bindings for {@code tensorrt_llm/kernels/beamSearchKernels.h}.
 *
 * <p>The {@link BeamHypotheses} struct is defined in its own class file.
 * This class groups the free kernel invocation functions.</p>
 */
@Properties(inherit = tensorrt_llm.presets.KernelsConfig.class)
public class BeamSearchKernels {
    static { Loader.load(); }

    // -------------------------------------------------------
    // Constants from beamSearchKernels.h
    // -------------------------------------------------------
    /** Maximum beam width supported in TRT-LLM. */
    public static final long kMaxBeamWidth = 1024;
    /** Maximum beam width for V1 workflow. */
    public static final long kMaxBeamWidthForV1 = 8;
    /** Maximum length of beam-width array per request. */
    public static final long kMaxBeamWidthArrayLength = 8;
    /** Thread count for small beam width stage-1. */
    public static final long kThreadForSmallBeamWidth = 256;
    /** Maximum vocab parts for stage-1 in V1 workflow. */
    public static final long kMaxVPartStage1 = 128;

    // -------------------------------------------------------
    // Kernel functions
    // -------------------------------------------------------

    /**
     * Top-K beam search step.
     *
     * @param logProbs   input log-probabilities [batchSize, beamWidth, vocabSize]
     * @param bias       optional bias pointer (may be null)
     * @param workspace  scratch workspace
     * @param bh         BeamHypotheses reference
     * @param stream     CUDA stream
     */
    @Namespace("tensorrt_llm::kernels")
    public static native void invokeTopkBeamSearch(
        @Const Pointer logProbs,
        @Const Pointer bias,
        Pointer workspace,
        @ByRef BeamHypotheses bh,
        Pointer stream);

    /**
     * Updates the cache indirection buffer for the next decoding step.
     *
     * @param tgtCI  target cache indirection [batchSize, beamWidth, maxSeqLen]
     * @param srcCI  source cache indirection [batchSize, beamWidth, maxSeqLen]
     * @param bh     BeamHypotheses reference
     * @param stream CUDA stream
     */
    @Namespace("tensorrt_llm::kernels")
    public static native void invokeUpdateCacheIndirection(
        IntPointer tgtCI,
        @Const IntPointer srcCI,
        @ByRef BeamHypotheses bh,
        Pointer stream);

    /**
     * Debug helper: prints log-probabilities to stdout.
     *
     * @param x    log-prob buffer
     * @param nBS  batch size
     * @param nBMIn  input beam width
     * @param nBM  beam width
     * @param nV   vocab size
     */
    @Namespace("tensorrt_llm::kernels")
    public static native void printLogProbs(
        @Const FloatPointer x,
        int nBS,
        int nBMIn,
        int nBM,
        int nV);
}

