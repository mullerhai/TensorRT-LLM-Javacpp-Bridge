// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.kernels;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Kernels.*;

/**
 * Java bindings for {@code tensorrt_llm/kernels/globalTimerKernel.h}.
 * Provides access to the global GPU timer kernel.
 */
@Properties(inherit = tensorrt_llm.presets.KernelsConfig.class)
public class GlobalTimerKernel {
    static { Loader.load(); }

    /**
     * Reads the global GPU hardware timer into a device-side uint64_t buffer.
     *
     * @param d_timestamp device pointer to a uint64_t to receive the timestamp
     * @param stream       CUDA stream
     */
    @Namespace("tensorrt_llm::kernels")
    public static native void invokeReadGlobalTimer(LongPointer d_timestamp, Pointer stream);
}

