// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.kernels;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Kernels.*;

/**
 * Java bindings for {@code tensorrt_llm/kernels/layernormKernels.h}.
 *
 * <p>The C++ function is templated on {@code T} (activation type) and
 * {@code QuantT} (quantization output type). The raw Pointer-based overload
 * covers all specialisations available in the native library.</p>
 */
@Properties(inherit = tensorrt_llm.presets.KernelsConfig.class)
public class LayernormKernels {
    static { Loader.load(); }

    /**
     * General LayerNorm kernel (FP16 / BF16 / FP32).
     *
     * @param out            output buffer [tokens, hidden_dim]
     * @param input          input buffer  [tokens, hidden_dim]
     * @param gamma          gamma (scale) [hidden_dim]
     * @param beta           beta  (bias)  [hidden_dim], may be null for RMSNorm
     * @param eps            epsilon for numerical stability
     * @param tokens         number of tokens
     * @param hidden_dim     hidden dimension
     * @param quant_mode     quantisation mode (QuantMode int value)
     * @param stream         CUDA stream (may be null → default stream)
     * @param use_diff_of_squares whether to use difference-of-squares algorithm
     * @param clamp_val      optional per-token clamp value pointer (may be null)
     * @param scale          optional static scale pointer (may be null)
     * @param dynamic_scale  optional output dynamic scale pointer (may be null)
     * @param sum_per_token  optional output sum-per-token pointer (may be null)
     * @param out_quant      optional quantised output pointer (may be null)
     */
    @Namespace("tensorrt_llm::kernels")
    public static native void invokeGeneralLayerNorm(
        Pointer out,
        @Const Pointer input,
        @Const Pointer gamma,
        @Const Pointer beta,
        float eps,
        int tokens,
        int hidden_dim,
        int quant_mode,
        Pointer stream,
        boolean use_diff_of_squares,
        @Const FloatPointer clamp_val,
        @Const FloatPointer scale,
        FloatPointer dynamic_scale,
        FloatPointer sum_per_token,
        Pointer out_quant);
}

