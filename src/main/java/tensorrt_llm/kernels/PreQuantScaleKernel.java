// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.kernels;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Kernels.*;

/**
 * Java bindings for {@code tensorrt_llm/kernels/preQuantScaleKernel.h}.
 *
 * <p>Both functions are templated on {@code T_in} / {@code T_out}. The raw
 * {@link Pointer}-based signatures cover all native library specialisations.</p>
 */
@Properties(inherit = tensorrt_llm.presets.KernelsConfig.class)
public class PreQuantScaleKernel {
    static { Loader.load(); }

    /**
     * Applies a per-channel activation scale (smooth-quant style).
     *
     * @param smoothed_act         output activation buffer
     * @param act                  input  activation buffer
     * @param per_channel_scale    scale factor per column [cols]
     * @param rows                 number of rows (tokens)
     * @param cols                 number of columns (hidden_dim)
     * @param num_valid_tokens_ptr optional pointer to number of valid tokens (may be null)
     * @param stream               CUDA stream (may be null → default stream)
     */
    @Namespace("tensorrt_llm::kernels")
    public static native void apply_per_channel_scale_kernel_launcher(
        Pointer smoothed_act,
        @Const Pointer act,
        @Const Pointer per_channel_scale,
        int rows,
        int cols,
        @Const LongPointer num_valid_tokens_ptr,
        Pointer stream);

    /**
     * Applies a per-channel-per-expert activation scale for MoE.
     *
     * @param smoothed_act              output activation buffer
     * @param act                       input  activation buffer
     * @param per_channel_scale         scale factor [num_experts_per_node, cols]
     * @param rows                      number of rows
     * @param cols                      number of columns
     * @param expert_first_token_offset first-token offset per expert [num_experts_per_node + 1]
     * @param num_experts_per_node      number of local experts
     * @param num_valid_tokens_ptr      pointer to number of valid tokens
     * @param stream                    CUDA stream
     */
    @Namespace("tensorrt_llm::kernels")
    public static native void apply_per_channel_scale_per_expert_kernel_launcher(
        Pointer smoothed_act,
        @Const Pointer act,
        @Const Pointer per_channel_scale,
        int rows,
        int cols,
        LongPointer expert_first_token_offset,
        int num_experts_per_node,
        @Const LongPointer num_valid_tokens_ptr,
        Pointer stream);
}

