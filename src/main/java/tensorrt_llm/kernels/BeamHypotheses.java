// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.kernels;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Kernels.*;
          // Max vocab part count for stage 1 in V1 workflow

@Namespace("tensorrt_llm::kernels") @Properties(inherit = tensorrt_llm.presets.KernelsConfig.class)
public class BeamHypotheses extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public BeamHypotheses() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public BeamHypotheses(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public BeamHypotheses(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public BeamHypotheses position(long position) {
        return (BeamHypotheses)super.position(position);
    }
    @Override public BeamHypotheses getPointer(long i) {
        return new BeamHypotheses((Pointer)this).offsetAddress(i);
    }

    // clang-format off
    // MBS: max_batch_size, BS: batch_size, BM: beam_width, MSL: max_seq_length
    // %%: parameter name in file generation.py (python workflow)
    // Candidate beams: a beam which generates end_id or its sequence length reaches MSL
    // Candidate-Beam-Array (CBA): The arrays to place the candidate beams and related information
    // Variable-Beam-Width-Search (VBWS): A search mode that allows using different beam width for each step

    // Scalar values
    public native boolean bReturnNormedScore(); public native BeamHypotheses bReturnNormedScore(boolean setter);         // Return `normedScore` or `cumLogProbs`, always be `false` now
    public native boolean bVBWS(); public native BeamHypotheses bVBWS(boolean setter);                      // whether to use VBWS for Beam-Search
    public native long nMaxBatchSize(); public native BeamHypotheses nMaxBatchSize(long setter);                // Buildtime max batch size
    public native long nBatchSize(); public native BeamHypotheses nBatchSize(long setter);                   // Runtime batch size
    public native long nBeamWidth(); public native BeamHypotheses nBeamWidth(long setter);                   // Runtime beam width
    public native long nBeamWidthIn(); public native BeamHypotheses nBeamWidthIn(long setter);                 // Scalar value of current input beam width, for VBWS
    public native long nBeamWidthOut(); public native BeamHypotheses nBeamWidthOut(long setter);                // Scalar value of current output beam width, for VBWS
    public native long nMaxSeqLen(); public native BeamHypotheses nMaxSeqLen(long setter);                   //
    public native long nVocabSize(); public native BeamHypotheses nVocabSize(long setter);                   // Vocab Size Padded
    public native long nVPart(); public native BeamHypotheses nVPart(long setter);                       // Count of vocab_size_padded divided
    public native long nByteMaxSharedMemoryPerBlock(); public native BeamHypotheses nByteMaxSharedMemoryPerBlock(long setter); // Device information
    public native long nByteSharedMemoryStage1(); public native BeamHypotheses nByteSharedMemoryStage1(long setter);      // Dynamic shared memory size of stage 1
    public native long nByteSharedMemoryStage3(); public native BeamHypotheses nByteSharedMemoryStage3(long setter);      // Static shared memory size of stage 3

    // Pointers from SamplingConfig
    public native @Const FloatPointer diversityRates(); public native BeamHypotheses diversityRates(FloatPointer setter);           // [BS]
    public native @Const FloatPointer lengthPenalties(); public native BeamHypotheses lengthPenalties(FloatPointer setter);          // [BS]
    public native @Const IntPointer earlyStoppings(); public native BeamHypotheses earlyStoppings(IntPointer setter);             // [BS]
    public native @Const IntPointer beamWidthArraysHost(); public native BeamHypotheses beamWidthArraysHost(IntPointer setter);        // [BS, kMaxBeamWidthArrayLength]                           for VBWS
    public native IntPointer nBeamWidthInHost(); public native BeamHypotheses nBeamWidthInHost(IntPointer setter);                 // [BS], cpu                                                for VBWS, beam width of last forward computation
    public native IntPointer nBeamWidthOutHost(); public native BeamHypotheses nBeamWidthOutHost(IntPointer setter);                // [BS], cpu                                                for VBWS, beam width of next forward computation

    // Pointers from input
    public native @Const IntPointer inputLengths(); public native BeamHypotheses inputLengths(IntPointer setter);               // [BS, BM]         %% context_length
    public native @Const IntPointer endIds(); public native BeamHypotheses endIds(IntPointer setter);                     // [BS, BM]         %% self.end_ids
    public native @Const IntPointer batchSlots(); public native BeamHypotheses batchSlots(IntPointer setter); // [BS]

    // Pointers for output
    public native IntPointer outputIds(); public native BeamHypotheses outputIds(IntPointer setter);                        // [BS, BM, MSL]    %% self.output_ids                      only used in gather_tree
    public native FloatPointer logProbs(); public native BeamHypotheses logProbs(FloatPointer setter);                       // [BS, BM, MSL]    %% self.log_probs                       only used in gather_tree
    public native FloatPointer logProbsTiled(); public native BeamHypotheses logProbsTiled(FloatPointer setter);                  // [MSL, MBS, BM]   %% self.log_probs_tiled
    public native IntPointer sequenceLengths(); public native BeamHypotheses sequenceLengths(IntPointer setter);                  // [BS, BM]         %% self.sequence_length_buffer
    public native FloatPointer cumLogProbs(); public native BeamHypotheses cumLogProbs(FloatPointer setter);                    // [BS, BM]         %% self.cum_log_probs

    // Pointers of CBA
    public native IntPointer outputIdsCBA(); public native BeamHypotheses outputIdsCBA(IntPointer setter);                     // [BS, BM*2, MSL]  %% self.beam_hyps_output_ids_cba
    public native FloatPointer logProbsCBA(); public native BeamHypotheses logProbsCBA(FloatPointer setter);                    // [BS, BM*2, MSL]  %% self.beam_hyps_log_probs_cba
    public native IntPointer sequenceLengthsCBA(); public native BeamHypotheses sequenceLengthsCBA(IntPointer setter);               // [BS, BM*2]       %% self.beam_hyps_seq_len_cba
    public native FloatPointer cumLogProbsCBA(); public native BeamHypotheses cumLogProbsCBA(FloatPointer setter);                 // [BS, BM*2]       %% self.beam_hyps_cum_log_probs_cba
    public native FloatPointer normedScoresCBA(); public native BeamHypotheses normedScoresCBA(FloatPointer setter);                // [BS, BM*2]       %% self.beam_hyps_normed_scores_cba
    public native IntPointer numBeamsCBA(); public native BeamHypotheses numBeamsCBA(IntPointer setter);                      // [BS]             %% self.beam_hyps_num_beams             number of beams in CBA
    public native FloatPointer minNormedScoresCBA(); public native BeamHypotheses minNormedScoresCBA(FloatPointer setter);             // [BS]             %% self.beam_hyps_min_normed_scores     worst score in CBA

    // Pointers related to beam search process, they are initialized in those two functions:
    // [gptDecoder.cpp] GptDecoder<T>::forward or [dynamicDecodeOp.cpp] FtDynamicDecode<T>::forward
    public native BoolPointer batchDones(); public native BeamHypotheses batchDones(BoolPointer setter);                      // [BS]             %% self.beam_hyps_is_done               whether a whole batch is finished
    public native FinishedState finished(); public native BeamHypotheses finished(FinishedState setter);               // [BS*BM], uint8   %% self.finished                        whether and how a beam is finished

    // Pointers for backtrack of the beams, they are relocated in [dynamicDecodeLayer.cpp] DynamicDecodeLayer<T>::prepareIdsPtrs
    public native IntPointer outputIdsPtr(int i); public native BeamHypotheses outputIdsPtr(int i, IntPointer setter);
    public native @Cast("int**") PointerPointer outputIdsPtr(); public native BeamHypotheses outputIdsPtr(PointerPointer setter);                    // [BS][BM, MSL]    %% self.output_ids
    public native IntPointer parentIdsPtr(int i); public native BeamHypotheses parentIdsPtr(int i, IntPointer setter);
    public native @Cast("int**") PointerPointer parentIdsPtr(); public native BeamHypotheses parentIdsPtr(PointerPointer setter);                    // [BS][BM, MSL]    %% self.parent_ids

    // Pointers for gather_tree(), read the unfinished beams from them and write to CBA for the final selection
    public native @Const IntPointer outputIdsUnfinish(); public native BeamHypotheses outputIdsUnfinish(IntPointer setter);          // [BS, BM, MSL]   %% self.output_ids
    public native @Const IntPointer parentIdsUnfinish(); public native BeamHypotheses parentIdsUnfinish(IntPointer setter);          // [BS, BM, MSL]   %% self.parent_ids

    // clang-format on

    public native void print();
}
