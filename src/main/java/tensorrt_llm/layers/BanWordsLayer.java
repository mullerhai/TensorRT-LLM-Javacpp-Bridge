// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.layers;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Layers.*;


/** \brief Layer to ban specific words from being sampled.
 *  Supports banning bad words and repeating N grams.
 *  Set badWordsPtr, maxBadWordsLen and badWordsLengths to ban bad words.
 *  Set noRepeatNgramSize in input params to ban repeat Ngrams.
 *  Layer modifies logits in-place. */
@Name("tensorrt_llm::layers::BanWordsLayer<float>") @NoOffset @Properties(inherit = tensorrt_llm.presets.LayersConfig.class)
public class BanWordsLayer extends BaseLayer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public BanWordsLayer(Pointer p) { super(p); }

    public BanWordsLayer(@Const @ByRef Pointer mode, @Const @ByRef DecoderDomain decoderDomain,
            @SharedPtr BufferManager bufferManager) { super((Pointer)null); allocate(mode, decoderDomain, bufferManager); }
    private native void allocate(@Const @ByRef Pointer mode, @Const @ByRef DecoderDomain decoderDomain,
            @SharedPtr BufferManager bufferManager);

    public native void setup(int batchSize, int beamWidth, @ByVal @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr batchSlots,
            @SharedPtr BaseSetupParams baseSetupParams);
            @SharedPtr DecodingLayerWorkspace workspace);

    /** \brief Modifies 'outputs->logits' in-place with -INF for banned words */
    public native void forwardAsync(@SharedPtr BaseDecodingOutputs outputs,
            @SharedPtr BaseDecodingInputs inputs);
            @SharedPtr DecodingLayerWorkspace workspace);
}
