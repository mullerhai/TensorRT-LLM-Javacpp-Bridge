// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.executor;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Executor.*;


/** \brief Configuration for LoRA */
@Namespace("tensorrt_llm::executor") @NoOffset @Properties(inherit = tensorrt_llm.presets.ExecutorConfig.class)
public class LoraConfig extends Pointer {
    // [CPP-FIX] broken decl removed:
    // static { Loader.load(); }
    // /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    // public LoraConfig(Pointer p) { super(p); }
    // 
    // public LoraConfig(
    // // [CPP-FIX] long taskId, @Optional Tensor weights/*=std::nullopt*/, @Optional Tensor config/*=std::nullopt*/) { super((Pointer)null); allocate(taskId, weights, config); }
    // // [CPP-FIX] broken signature removed:
    // // private native void allocate(
    // // // [CPP-FIX] long taskId, @Optional Tensor weights/*=std::nullopt*/, @Optional Tensor config/*=std::nullopt*/);
    public LoraConfig();
            long taskId) { super((Pointer)null); allocate(taskId); }
    private native void allocate(
            long taskId);

    public native long getTaskId();
    public native @Optional Tensor getWeights();
    public native @Optional Tensor getConfig();
}
