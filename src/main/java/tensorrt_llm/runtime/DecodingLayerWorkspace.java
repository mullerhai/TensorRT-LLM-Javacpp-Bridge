// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.runtime;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.TrtllmRuntime.*;


/**\brief A collection of shared resources and data for the decoding layers. */
@Namespace("tensorrt_llm::runtime") @NoOffset @Properties(inherit = tensorrt_llm.presets.RuntimeConfig.class)
public class DecodingLayerWorkspace extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public DecodingLayerWorkspace(Pointer p) { super(p); }


    public DecodingLayerWorkspace(@SharedPtr BufferManager bufferManager, @Const @ByRef DecoderDomain decoderDomain,
            int logitsType, long workspaceBufferSizeInBytes) { super((Pointer)null); allocate(bufferManager, decoderDomain, logitsType, workspaceBufferSizeInBytes); }
    private native void allocate(@SharedPtr BufferManager bufferManager, @Const @ByRef DecoderDomain decoderDomain,
            int logitsType, long workspaceBufferSizeInBytes);

    

    

    /**\brief Gets a pointer to the start of the shared device workspace. */
    public native Pointer getRawWorkspaceDevicePtr();

    /**\brief Gets a pointer to the start of the shared device workspace, as a pointer to the given type. */

    /**\brief Gets a pointer to the buffer backing the device workspace. */
    public native @SharedPtr IBuffer getWorkspaceDeviceBuffer();

    /**\brief Sets the value of the device copy of the batch slots. */
    public native void setDeviceBatchSlots(@Cast("const tensorrt_llm::runtime::DecodingLayerWorkspace::TensorConstPtr") ITensor newBatchSlots);

    /**\brief Gets the pointer to the batch slots on device. */
    public native @Const IntPointer getDeviceBatchSlotsPtr();

    /**\brief Gets the device tensor containing the batch slots. */
    public native @Cast("tensorrt_llm::runtime::DecodingLayerWorkspace::TensorConstPtr") ITensor getDeviceBatchSlots();

    /**\brief Gets the device tensor containing the runtime logits. */
    public native @SharedPtr ITensor getDeviceRuntimeLogits();

    /**\brief Gets a tensor with the given shape and type at the start of the device workspace. */
    public native @SharedPtr ITensor getWorkspaceAsDeviceTensor(@ByVal @Cast("tensorrt_llm::runtime::ITensor::Shape*") Pointer shape, int type);

    /** \brief A convenience function to copy the content of a standard vector to a device workspace. */

    /** \brief A convenience function to copy the content of a standard vector to the workspace. */

    /**\brief Ensures the workspace has at least the provided space in bytes. Does nothing if the workspace is already
     *  at least as large. */
    public native void resize(long minSize);

    /**\brief Given a collection of tuples of tensor shapes and data types, returns the memory aligned size required to
     *  contain those tensors. */

    /**\brief Given a collection of tensors, creates tensors with the same shape and data types in the workspace and
     *  copies the data from the input tensors to their reflection on device. */

    /** \brief A convenience function to initialize curand states from a provided seed. */
    // [CPP-FIX] public native void initializeDeviceCurandStates(@Optional std::vector<uint64_t> randomSeed,
    // [CPP-FIX] int batchSize, @Cast("const tensorrt_llm::runtime::DecodingLayerWorkspace::TensorConstPtr") ITensor batchSlots, @SharedPtr @ByRef PointerPointer statesDevice);
});
