// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.runtime;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.TrtllmRuntime.*;


@Namespace("tensorrt_llm::runtime") @NoOffset @Properties(inherit = tensorrt_llm.presets.RuntimeConfig.class)
public class EagleModule extends SpeculativeDecodingModule {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public EagleModule(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public EagleModule(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public EagleModule position(long position) {
        return (EagleModule)super.position(position);
    }
    @Override public EagleModule getPointer(long i) {
        return new EagleModule((Pointer)this).offsetAddress(i);
    }

    // Number of paths is maxDecodingTokens = maxDecodingDraftTokens + 1 to account for very flat trees with
    // depth 1.
    public EagleModule(int maxDraftPathLen, int maxDecodingDraftTokens, int numTransformersLayer,
            int maxNonLeafNodesPerLayer) { super((Pointer)null); allocate(maxDraftPathLen, maxDecodingDraftTokens, numTransformersLayer, maxNonLeafNodesPerLayer); }
    @NoException(true) private native void allocate(int maxDraftPathLen, int maxDecodingDraftTokens, int numTransformersLayer,
            int maxNonLeafNodesPerLayer);

    public EagleModule() { super((Pointer)null); allocate(); }
    @NoException(true) private native void allocate();

    public native @Cast("const tensorrt_llm::executor::EagleChoices*") @Const Pointer getDefaultEagleChoices();

    public native @NoException(true) int getNumTransformerLayers();

    public native @NoException(true) int getMaxNonLeafNodesPerLayer();
}
