// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.runtime;


import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;


@Namespace("tensorrt_llm::runtime") @NoOffset @Properties(inherit = tensorrt_llm.presets.RuntimeConfig.class)
public class CudaVirtualMemoryAllocator extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public CudaVirtualMemoryAllocator(Pointer p) { super(p); }

    /** enum Pointer */
    public static final int
        NONE = 0,   // The memory is not backed. Upon rematerialize, memory has uninitialized content.
        MEMSET = 1, // The memory is memset to zero upon rematerialize.
        CPU = 2,    // The memory is backed by normal CPU memory. The content is restored upon rematerialize.
        PINNED = 3;  // The memory is backed by pinned CPU memory. The content is restored upon rematerialize.

    @NoOffset public static class Configuration extends Pointer {
        static { Loader.load(); }
        /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
        public Configuration(Pointer p) { super(p); }
    
        /**
         * CudaVirtualMemoryAllocator::Configuration
         * @param manager    Manager used to track and manage virtual memories
         * @param tag        The tag for allocated memories
         * @param mode       Backed storage mode
         * @param backStream The CUDA stream used for restoring memory content
         *                   Note: Virtual Address Allocation is not async. The stream is not used in allocation.
         */
        public Configuration(@ByRef CudaVirtualMemoryManager manager, @StdString BytePointer tag, @Cast("tensorrt_llm::runtime::CudaVirtualMemoryAllocator::RestoreMode") int mode, @SharedPtr CudaStream backStream) { super((Pointer)null); allocate(manager, tag, mode, backStream); }
        private native void allocate(@ByRef CudaVirtualMemoryManager manager, @StdString BytePointer tag, @Cast("tensorrt_llm::runtime::CudaVirtualMemoryAllocator::RestoreMode") int mode, @SharedPtr CudaStream backStream);
        public Configuration(@ByRef CudaVirtualMemoryManager manager, @StdString String tag, @Cast("tensorrt_llm::runtime::CudaVirtualMemoryAllocator::RestoreMode") int mode, @SharedPtr CudaStream backStream) { super((Pointer)null); allocate(manager, tag, mode, backStream); }
        private native void allocate(@ByRef CudaVirtualMemoryManager manager, @StdString String tag, @Cast("tensorrt_llm::runtime::CudaVirtualMemoryAllocator::RestoreMode") int mode, @SharedPtr CudaStream backStream);

        public native long aligned(long n, int device/*=0*/);
        public native long aligned(long n);

        // Background configuration, used to indicate no virtual memory allocator is explicitly configured by the user.
        public static native @ByRef Configuration backgroundConfiguration(); public static native void backgroundConfiguration(Configuration setter);
    }

    public CudaVirtualMemoryAllocator(@SharedPtr Configuration config) { super((Pointer)null); allocate(config); }
    private native void allocate(@SharedPtr Configuration config);

    // Tells if this is the background allocator.
    public native @Name("operator bool") @NoException(true) boolean asBoolean();

    public native @Name("allocate") void _allocate(Pointer ptr, long n, int device);
    public native @Name("deallocate") void _deallocate(@ByVal Pointer ptr, long n);
}
