// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.runtime;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.TrtllmRuntime.*;


/**
 * Holds memory of lora cache pages, and manages allocation and freeing of whole pages.
 * Memory is pre-allocated either on the host or device
 *
 * Note that this class is not thread safe
 */
@Namespace("tensorrt_llm::runtime") @NoOffset @Properties(inherit = tensorrt_llm.presets.RuntimeConfig.class)
public class LoraCachePageManager extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public LoraCachePageManager(Pointer p) { super(p); }


    /**
     * @param config [in] : a LoraCachePageManagerConfig
     * @param bufferManager [in] : a Buffermanager used to allocate page blocks
     */
    public LoraCachePageManager(@Const @ByRef LoraCachePageManagerConfig config, @Const @ByRef BufferManager bufferManager) { super((Pointer)null); allocate(config, bufferManager); }
    private native void allocate(@Const @ByRef LoraCachePageManagerConfig config, @Const @ByRef BufferManager bufferManager);

    /**
     * \brief claim pages
     *
     * @param numPages [in] number of pages to claim
     * @return a tuple, where the first values is a boolean indicating whether pages were claimed.  If the first value
     * is true the second value will have a list of pageIds
     */
    // [CPP-FIX] public native @Optional std::vector<std::size_t> claimPages(int numPages);

    /**
     * \brief get number of available (free) pages in manager
     *
     * @return number of free pages in manager
     */
    public native int numAvailablePages();

    /**
     * \brief release given pages
     *
     * @param pages [in] : list of pages to release (free)
     */
    public native void releasePages(@StdVector LongPointer pages);

    /**
     * \brief return pointer to given page block
     *
     * @param blockIdx [in] ;
     * @return -- pointer to page block
     */
    public native @Cast("tensorrt_llm::runtime::ITensor::SharedConstPtr") ITensor blockPtr(int blockIdx);

    /**
     * \brief return pointer to given page
     *
     * @param pageIdx [in] :
     * @return -- const pointer to page
     */
    public native @Cast("tensorrt_llm::runtime::ITensor::SharedConstPtr") ITensor pagePtr(long pageIdx);

    /**
     * \brief return pointer to given page
     *
     * @param pageIdx [in] :
     * @return -- mutable pointer to page
     */
    public native @SharedPtr ITensor mutablePagePtr(long pageIdx);
}
