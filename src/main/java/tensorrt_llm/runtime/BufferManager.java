// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.runtime;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.TrtllmRuntime.*;


/** \brief A helper class for managing memory on host and device. */
@Namespace("tensorrt_llm::runtime") @NoOffset @Properties(inherit = tensorrt_llm.presets.RuntimeConfig.class)
public class BufferManager extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public BufferManager(Pointer p) { super(p); }

    
    //!

    /** \brief Construct a BufferManager.
     * 
     *  @param cudaStream [in] The cuda stream to use for all operations on GPU (allocation, de-allocation, copying,
     *  etc.). */
    public BufferManager(@SharedPtr CudaStream stream, boolean trimPool/*=false*/) { super((Pointer)null); allocate(stream, trimPool); }
    private native void allocate(@SharedPtr CudaStream stream, boolean trimPool/*=false*/);
    public BufferManager(@SharedPtr CudaStream stream) { super((Pointer)null); allocate(stream); }
    private native void allocate(@SharedPtr CudaStream stream);

    /** \brief Destructor. */

    @MemberGetter public static native int kBYTE_TYPE();
    public static final int kBYTE_TYPE = kBYTE_TYPE();

    /** \brief Allocates an {@code IBuffer} of the given size on the GPU, using cudaMallocAsync. */
    // [CPP-FIX] public native @UniquePtr IBuffer gpu(long size, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public native @UniquePtr IBuffer gpu(long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions on the GPU, using cudaMallocAsync. */
    // [CPP-FIX] public native @UniquePtr ITensor gpu(@ByVal Pointer dims, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public native @UniquePtr ITensor gpu(@ByVal Pointer dims);

    /** \brief Allocates an {@code IBuffer} of the given size on the GPU, using cudaMalloc. */
    // [CPP-FIX] public static native @UniquePtr IBuffer gpuSync(long size, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr IBuffer gpuSync(long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions on the GPU, using cudaMalloc. */
    // [CPP-FIX] public static native @UniquePtr ITensor gpuSync(@ByVal Pointer dims, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr ITensor gpuSync(@ByVal Pointer dims);

    /** \brief Allocates an {@code IBuffer} of the given size on the CPU. */
    // [CPP-FIX] public static native @UniquePtr IBuffer cpu(long size, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr IBuffer cpu(long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions on the CPU. */
    // [CPP-FIX] public static native @UniquePtr ITensor cpu(@ByVal Pointer dims, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr ITensor cpu(@ByVal Pointer dims);

    /** \brief Allocates a pinned {@code IBuffer} of the given size on the CPU. */
    // [CPP-FIX] public static native @UniquePtr IBuffer pinned(long size, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr IBuffer pinned(long size);

    /** \brief Allocates a pinned {@code ITensor} of the given dimensions on the CPU. */
    // [CPP-FIX] public static native @UniquePtr ITensor pinned(@ByVal Pointer dims, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr ITensor pinned(@ByVal Pointer dims);

    /** \brief Allocates a pinned {@code IBuffer} of the given size on the CPU in the default memory pool. */
    // [CPP-FIX] public static native @UniquePtr IBuffer pinnedPool(long size, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr IBuffer pinnedPool(long size);

    /** \brief Allocates a pinned {@code ITensor} of the given dimensions on the CPU in the default memory pool. */
    // [CPP-FIX] public static native @UniquePtr ITensor pinnedPool(@ByVal Pointer dims, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr ITensor pinnedPool(@ByVal Pointer dims);

    /** \brief Allocates an {@code IBuffer} of the given size in UVM. */
    // [CPP-FIX] public static native @UniquePtr IBuffer managed(long size, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr IBuffer managed(long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions in UVM. */
    // [CPP-FIX] public static native @UniquePtr ITensor managed(@ByVal Pointer dims, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public static native @UniquePtr ITensor managed(@ByVal Pointer dims);

    /** \brief Allocates an {@code ITensor} of the given dimensions for NVLS */
    // [CPP-FIX] public static native @UniquePtr ITensor ipcNvls(@ByVal std::set<int> ranks, @ByVal Pointer dims, int type);

    /** \brief Allocates an {@code IBuffer} of the given size and memory type. */
    // [CPP-FIX] broken signature removed:
    // public native @UniquePtr @Name("allocate") IBuffer _allocate(
    // // [CPP-FIX] MemoryType memoryType, long size, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public native @UniquePtr @Name("allocate") IBuffer _allocate(
            MemoryType memoryType, long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions and memory type. */
    // [CPP-FIX] broken signature removed:
    // public native @UniquePtr @Name("allocate") ITensor _allocate(
    // // [CPP-FIX] MemoryType memoryType, @ByVal Pointer dims, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public native @UniquePtr @Name("allocate") ITensor _allocate(
            MemoryType memoryType, @ByVal Pointer dims);

    /** \brief Create an empty {@code IBuffer} of the given memory type. It may be resized later. */
    // [CPP-FIX] public native @UniquePtr IBuffer emptyBuffer(MemoryType memoryType, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public native @UniquePtr IBuffer emptyBuffer(MemoryType memoryType);

    /** \brief Create an empty {@code ITensor} of the given memory type. It may be reshaped later. */
    // [CPP-FIX] public native @UniquePtr ITensor emptyTensor(MemoryType memoryType, int type/*=tensorrt_llm::runtime::BufferManager::kBYTE_TYPE*/);
    public native @UniquePtr ITensor emptyTensor(MemoryType memoryType);

    /** \brief Set the contents of the given {@code buffer} to value. */
    public native void setMem(@ByRef IBuffer buffer, int value);

    /** \brief Set the contents of the given {@code buffer} to zero. */
    public native void setZero(@ByRef IBuffer buffer);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const Pointer src, @ByRef IBuffer dst, MemoryType srcType);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const @ByRef IBuffer src, Pointer dst, MemoryType dstType);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const Pointer src, @ByRef IBuffer dst);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const @ByRef IBuffer src, Pointer dst);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const @ByRef IBuffer src, @ByRef IBuffer dst);

    /** \brief Copy {@code src} into a new {@code IBuffer} with a potentially different memory type. */
    public native @UniquePtr IBuffer copyFrom(@Const @ByRef IBuffer src, MemoryType memoryType);

    /** \brief Copy {@code src} into a new {@code ITensor} with a potentially different memory type. */
    public native @UniquePtr ITensor copyFrom(@Const @ByRef ITensor src, MemoryType memoryType);

    /** \brief Copy {@code src} into a new {@code IBuffer} with a potentially different memory type. */

    /** \brief Copy {@code src} into a new {@code ITensor} with a potentially different memory type. */

    /** \brief Copy {@code src} into a new {@code ITensor} with a potentially different memory type. */

    /** \brief Get the underlying cuda stream. */
    public native @Const @ByRef CudaStream getStream();

    /** \brief The current size of the memory reserved by the memory pool. */
    public native long memoryPoolReserved();

    /** \brief The current size of the memory used by the memory pool. */
    public native long memoryPoolUsed();

    /** \brief The current size of the memory free in the memory pool. */
    public native long memoryPoolFree();

    /** \brief Try to trim the memory reserved by the pool to {@code size} bytes. This synchronizes implicitly with the
     *  stream. */
    public native void memoryPoolTrimTo(long size);
}
