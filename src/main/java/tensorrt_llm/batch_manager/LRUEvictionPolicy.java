// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.batch_manager;


import tensorrt_llm.global.Batchmanager;
import tensorrt_llm.presets.BatchmanagerConfig;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Batchmanager.*;


@Namespace("tensorrt_llm::batch_manager::eviction_policy") @NoOffset @Properties(inherit = tensorrt_llm.presets.BatchmanagerConfig.class)
public class LRUEvictionPolicy extends BaseEvictionPolicy {
    static { Loader.load(); }
    /** Default native constructor. */
    public LRUEvictionPolicy() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public LRUEvictionPolicy(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public LRUEvictionPolicy(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public LRUEvictionPolicy position(long position) {
        return (LRUEvictionPolicy)super.position(position);
    }
    @Override public LRUEvictionPolicy getPointer(long i) {
        return new LRUEvictionPolicy((Pointer)this).offsetAddress(i);
    }

    public native void initialize(@StdVector BlockPtr mAllBlocksById, @Cast("tensorrt_llm::batch_manager::SizeType32*") @StdVector IntPointer sizes,
            @Optional int secondaryOffloadMinPriority);

    public native void releaseBlock(@ByVal BlockPtr block);
    public native void releaseBlock(@ByVal BlockPtr block, boolean toFront);

    public native @Cast("tensorrt_llm::batch_manager::SizeType32") int getNumFreeBlocks(@Cast("tensorrt_llm::batch_manager::SizeType32") int cacheLevel);

    public native void claimBlock(@ByVal BlockPtr block);
    public native void claimBlock(@ByVal BlockPtr block, @Optional int priority,
            @Optional long durationMs);

    // Check the expiring blocks heap, and move expired blocks back to the default queue.
    public native void refresh();

    // Making this public and virtual makes it possible to test.
    public native @ByVal duration getTime();

    public native boolean verifyQueueIntegrity();
}
