// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.batch_manager;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Batchmanager.*;


// Used to support the cache transmission with different layouts and different protocols.
@Namespace("tensorrt_llm::batch_manager::kv_cache_manager") @Properties(inherit = tensorrt_llm.presets.BatchmanagerConfig.class)
public class BaseCacheFormatter extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public BaseCacheFormatter(Pointer p) { super(p); }


    /** \brief Format the cache data into bytes for sending.
     *  @param session The transfer session. */
    public native void format(@ByRef TransferSession session);

    /** \brief Unformat the cache data from received bytes.
     *  @param session The transfer session. */
    public native void unformat(@ByRef TransferSession session);

    /** \brief Determine whether the sender is applicable to the source and target.
     *  @param selfConfig Source data arrangement.
     *  @param destConfig Target data arrangement.
     *  @return Whether the sender is applicable to the source and target. */
    public native boolean inquireSupport(@Const @ByRef CacheState selfConfig, @Const @ByRef CacheState destConfig);

    /** \brief Obtain the indies of the counterparts that need to be actually communicated with.
     *  @param selfConfig Source data arrangement.
     *  @param selfIdx The sequential index of the current executor process within the entire parallel group.
     *  @param destConfig Target data arrangement.
     *  @return The indies of the counterparts. */
    public native @Cast("tensorrt_llm::batch_manager::kv_cache_manager::SizeType32*") @StdVector IntPointer getCounterparts(
            @Const @ByRef CacheState selfConfig, @Cast("tensorrt_llm::batch_manager::kv_cache_manager::SizeType32") int selfIdx, @Const @ByRef CacheState destConfig);

    public native @NoException(true) BaseKVCacheManager getCacheManager();

    /** \brief Pick receive connections and their corresponding local rank indices.
     *  @return Pair of (pickUpConnections, localRankIndices). */
    // [CPP-FIX] public native @ByVal std::pair<std::vector<size_t>,std::vector<size_t> > pickRecvConnections(long numConnections);
    // [CPP-FIX] @Const @ByRef CacheState selfConfig, @Cast("tensorrt_llm::batch_manager::kv_cache_manager::SizeType32") int selfIdx, @Const @ByRef CacheState destConfig,
    // [CPP-FIX] @Cast("tensorrt_llm::batch_manager::kv_cache_manager::SizeType32*") @StdVector IntPointer counterPartRanks);

    /** \brief Destructor. */
}
