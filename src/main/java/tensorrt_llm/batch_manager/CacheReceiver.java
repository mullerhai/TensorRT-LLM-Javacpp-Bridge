// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.batch_manager;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;
import tensorrt_llm.executor.ConnectionManager;

import static tensorrt_llm.global.Batchmanager.*;


@Namespace("tensorrt_llm::batch_manager") @NoOffset @Properties(inherit = tensorrt_llm.presets.BatchmanagerConfig.class)
public class CacheReceiver extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public CacheReceiver(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public CacheReceiver(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public CacheReceiver position(long position) {
        return (CacheReceiver)super.position(position);
    }
    @Override public CacheReceiver getPointer(long i) {
        return new CacheReceiver((Pointer)this).offsetAddress(i);
    }

    /** \brief Constructor.
     *  @param manager The connection manager.
     *  @param selfIndex The sequential index of the current executor process.
     *  @param cacheLayer The cache layer bundling all cache states and formatters. */
    public CacheReceiver(ConnectionManager manager, @Cast("tensorrt_llm::batch_manager::SizeType32") int selfIndex, @ByVal CacheTransferLayer cacheLayer) { super((Pointer)null); allocate(manager, selfIndex, cacheLayer); }
    private native void allocate(ConnectionManager manager, @Cast("tensorrt_llm::batch_manager::SizeType32") int selfIndex, @ByVal CacheTransferLayer cacheLayer);

    public CacheReceiver() { super((Pointer)null); allocate(); }
    private native void allocate();

    /** \brief Asynchronously send a request to receive data.
     *  @param llmRequest Request object. Its data should be in an allocated but unwritten state when called, and the
     *  data for this request should remain intact only after future synchronization.
     *  @return Once the data is fully received, the future object will become valid. */
    // [CPP-FIX] public native @ByVal std::future<void> receiveAsync(@ByRef LlmRequest llmRequest);

    public native @ByVal TransferSession sendRequestInfo(@Const @ByRef LlmRequest llmRequest);

    public native void receiveSync(@ByRef TransferSession session);

    /** \brief Cancel the request.
     *  @param llmRequest Request object.
     *  @return Whether the request is cancelled. */
    public native boolean cancelRequest(@Const @ByRef LlmRequest llmRequest);

    /** \brief Receive ready signal.
     *  @param session The session object.
     *  @return Whether the request is ready to be received. */
    public native boolean receiveReadySignal(@ByRef TransferSession session);

    /** \brief Destructor. */
}
