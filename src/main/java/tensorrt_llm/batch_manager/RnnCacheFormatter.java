// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.batch_manager;


import tensorrt_llm.executor.CacheState;
import tensorrt_llm.global.Batchmanager;
import tensorrt_llm.presets.BatchmanagerConfig;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Batchmanager.*;
 // namespace rnn_state_manager

/** \brief RNN Cache Formatter for formatting/unformatting RNN states during transfer. */
@Namespace("tensorrt_llm::batch_manager") @NoOffset @Properties(inherit = tensorrt_llm.presets.BatchmanagerConfig.class)
public class RnnCacheFormatter extends BaseCacheFormatter {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public RnnCacheFormatter(Pointer p) { super(p); }


    /** \brief Constructor.
     *  @param rnnStateManager Pointer to the RNN state manager.
     *  @param rnnCacheTransBufferManager Pointer to the RNN cache transfer buffer manager. */
    public RnnCacheFormatter(RnnStateManager rnnStateManager,
            RnnCacheTransBufferManager rnnCacheTransBufferManager) { super((Pointer)null); allocate(rnnStateManager, rnnCacheTransBufferManager); }
    private native void allocate(RnnStateManager rnnStateManager,
            RnnCacheTransBufferManager rnnCacheTransBufferManager);

    /** \brief Format RNN states for sending.
     *  @param session The transfer session. */
    public native void format(@ByRef Pointer session);

    /** \brief Unformat received RNN states.
     *  @param session The transfer session. */
    public native void unformat(@ByRef Pointer session);

    /** \brief Check if transfer is supported between two configurations. */
    public native boolean inquireSupport(@Const @ByRef Pointer selfConfig, @Const @ByRef Pointer destConfig);

    /** \brief Get the counterpart ranks for communication. */
    public native @Cast("tensorrt_llm::batch_manager::RnnCacheFormatter::SizeType32*") @StdVector IntPointer getCounterparts(
            @Const @ByRef Pointer selfConfig, @Cast("tensorrt_llm::batch_manager::RnnCacheFormatter::SizeType32") int selfIdx, @Const @ByRef Pointer destConfig);

    /** \brief Pick receive connections and their corresponding local rank indices. */

    /** \brief Returns nullptr since RNN cache formatter doesn't use KV cache manager. */
    public native @NoException(true) BaseKVCacheManager getCacheManager();

    /** \brief Get the RNN state manager.
     *  @return Pointer to the RNN state manager. */
    public native @NoException(true) RnnStateManager getRnnStateManager();
}
