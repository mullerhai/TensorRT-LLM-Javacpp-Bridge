// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.batch_manager;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;
import tensorrt_llm.executor.PeftCacheConfig;

import static tensorrt_llm.global.Batchmanager.*;


@Namespace("tensorrt_llm::batch_manager") @NoOffset @Properties(inherit = tensorrt_llm.presets.BatchmanagerConfig.class)
public class PeftCacheManagerConfig extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public PeftCacheManagerConfig(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public PeftCacheManagerConfig(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public PeftCacheManagerConfig position(long position) {
        return (PeftCacheManagerConfig)super.position(position);
    }
    @Override public PeftCacheManagerConfig getPointer(long i) {
        return new PeftCacheManagerConfig((Pointer)this).offsetAddress(i);
    }


    @MemberGetter public static native float kDefaultDeviceCachePercent();
    public static final float kDefaultDeviceCachePercent = kDefaultDeviceCachePercent();
    @MemberGetter public static native long kDefaultHostCacheSize();
    public static final long kDefaultHostCacheSize = kDefaultHostCacheSize();

    // [CPP-FIX] broken decl removed:
    // public PeftCacheManagerConfig(int numHostModuleLayer/*=0*/, int numDeviceModuleLayer/*=0*/,
    // int optimalAdapterSize/*=8*/, int maxAdapterSize/*=64*/, int numPutWorkers/*=1*/,
    // int numEnsureWorkers/*=1*/, int numCopyStreams/*=1*/, int maxPagesPerBlockHost/*=24*/,
    // // [CPP-FIX] int maxPagesPerBlockDevice/*=8*/, @Optional FloatPointer deviceCachePercent/*=std::nullopt*/,
    // // [CPP-FIX] @Optional LongPointer hostCacheSize/*=std::nullopt*/, @StdString @Optional BytePointer loraPrefetchDir/*=std::nullopt*/) { super((Pointer)null); allocate(numHostModuleLayer, numDeviceModuleLayer, optimalAdapterSize, maxAdapterSize, numPutWorkers, numEnsureWorkers, numCopyStreams, maxPagesPerBlockHost, maxPagesPerBlockDevice, deviceCachePercent, hostCacheSize, loraPrefetchDir); }
    // // [CPP-FIX] broken signature removed:
    // // private native void allocate(int numHostModuleLayer/*=0*/, int numDeviceModuleLayer/*=0*/,
    // // int optimalAdapterSize/*=8*/, int maxAdapterSize/*=64*/, int numPutWorkers/*=1*/,
    // // int numEnsureWorkers/*=1*/, int numCopyStreams/*=1*/, int maxPagesPerBlockHost/*=24*/,
    // // // [CPP-FIX] int maxPagesPerBlockDevice/*=8*/, @Optional FloatPointer deviceCachePercent/*=std::nullopt*/,
    // // // [CPP-FIX] @Optional LongPointer hostCacheSize/*=std::nullopt*/, @StdString @Optional BytePointer loraPrefetchDir/*=std::nullopt*/);
    public PeftCacheManagerConfig() { super((Pointer)null); allocate(); }
    private native void allocate();
    // [CPP-FIX] broken decl removed:
    // public PeftCacheManagerConfig(int numHostModuleLayer/*=0*/, int numDeviceModuleLayer/*=0*/,
    // int optimalAdapterSize/*=8*/, int maxAdapterSize/*=64*/, int numPutWorkers/*=1*/,
    // int numEnsureWorkers/*=1*/, int numCopyStreams/*=1*/, int maxPagesPerBlockHost/*=24*/,
    // // [CPP-FIX] int maxPagesPerBlockDevice/*=8*/, @Optional FloatBuffer deviceCachePercent/*=std::nullopt*/,
    // // [CPP-FIX] @Optional LongPointer hostCacheSize/*=std::nullopt*/, @StdString @Optional BytePointer loraPrefetchDir/*=std::nullopt*/) { super((Pointer)null); allocate(numHostModuleLayer, numDeviceModuleLayer, optimalAdapterSize, maxAdapterSize, numPutWorkers, numEnsureWorkers, numCopyStreams, maxPagesPerBlockHost, maxPagesPerBlockDevice, deviceCachePercent, hostCacheSize, loraPrefetchDir); }
    // // [CPP-FIX] broken signature removed:
    // // private native void allocate(int numHostModuleLayer/*=0*/, int numDeviceModuleLayer/*=0*/);
    // // int optimalAdapterSize/*=8*/, int maxAdapterSize/*=64*/, int numPutWorkers/*=1*/,
    // // int numEnsureWorkers/*=1*/, int numCopyStreams/*=1*/, int maxPagesPerBlockHost/*=24*/,
    // // // [CPP-FIX] int maxPagesPerBlockDevice/*=8*/, @Optional FloatBuffer deviceCachePercent/*=std::nullopt*/,
    // // // [CPP-FIX] @Optional LongPointer hostCacheSize/*=std::nullopt*/, @StdString @Optional BytePointer loraPrefetchDir/*=std::nullopt*/);
    // [CPP-FIX] broken decl removed:
    // public PeftCacheManagerConfig(int numHostModuleLayer/*=0*/, int numDeviceModuleLayer/*=0*/,
    // int optimalAdapterSize/*=8*/, int maxAdapterSize/*=64*/, int numPutWorkers/*=1*/,
    // int numEnsureWorkers/*=1*/, int numCopyStreams/*=1*/, int maxPagesPerBlockHost/*=24*/,
    // // [CPP-FIX] int maxPagesPerBlockDevice/*=8*/, @Optional float[] deviceCachePercent/*=std::nullopt*/,
    // // [CPP-FIX] @Optional LongPointer hostCacheSize/*=std::nullopt*/, @StdString @Optional BytePointer loraPrefetchDir/*=std::nullopt*/) { super((Pointer)null); allocate(numHostModuleLayer, numDeviceModuleLayer, optimalAdapterSize, maxAdapterSize, numPutWorkers, numEnsureWorkers, numCopyStreams, maxPagesPerBlockHost, maxPagesPerBlockDevice, deviceCachePercent, hostCacheSize, loraPrefetchDir); }
    // // [CPP-FIX] broken signature removed:
    // // private native void allocate(int numHostModuleLayer/*=0*/, int numDeviceModuleLayer/*=0*/);
    // // int optimalAdapterSize/*=8*/, int maxAdapterSize/*=64*/, int numPutWorkers/*=1*/,
    // // int numEnsureWorkers/*=1*/, int numCopyStreams/*=1*/, int maxPagesPerBlockHost/*=24*/,
    // // // [CPP-FIX] int maxPagesPerBlockDevice/*=8*/, @Optional float[] deviceCachePercent/*=std::nullopt*/,
    // // // [CPP-FIX] @Optional LongPointer hostCacheSize/*=std::nullopt*/, @StdString @Optional BytePointer loraPrefetchDir/*=std::nullopt*/);

    public PeftCacheManagerConfig(@ByVal PeftCacheConfig cfg) { super((Pointer)null); allocate(cfg); }
    private native void allocate(@ByVal PeftCacheConfig cfg);

    // number of max sized 1-layer 1-module adapterSize=1 sets of weights that can be stored in host cache
    public native int numHostModuleLayer(); public native PeftCacheManagerConfig numHostModuleLayer(int setter);
    // number of max sized 1-layer 1-module sets of weights that can be stored in host cache
    public native int numDeviceModuleLayer(); public native PeftCacheManagerConfig numDeviceModuleLayer(int setter);
    // optimal adapter size used to set page width
    public native int optimalAdapterSize(); public native PeftCacheManagerConfig optimalAdapterSize(int setter);
    // max supported adapter size. Used to compute minimum
    public native int maxAdapterSize(); public native PeftCacheManagerConfig maxAdapterSize(int setter);
    // number of worker threads used to put weights into host cache
    public native int numPutWorkers(); public native PeftCacheManagerConfig numPutWorkers(int setter);
    // number of worker threads used to copy weights from host to device
    public native int numEnsureWorkers(); public native PeftCacheManagerConfig numEnsureWorkers(int setter);
    // number of streams used to copy weights from host to device
    public native int numCopyStreams(); public native PeftCacheManagerConfig numCopyStreams(int setter);
    // Number of cache pages per allocation block (host)
    public native int maxPagesPerBlockHost(); public native PeftCacheManagerConfig maxPagesPerBlockHost(int setter);
    // Number of cache pages per allocation block (device)
    public native int maxPagesPerBlockDevice(); public native PeftCacheManagerConfig maxPagesPerBlockDevice(int setter);
    // percent of memory after engine load to use for cache
    public native @Optional FloatPointer deviceCachePercent(); public native PeftCacheManagerConfig deviceCachePercent(FloatPointer setter);
    // size in bytes to use for host cache
    public native @Optional LongPointer hostCacheSize(); public native PeftCacheManagerConfig hostCacheSize(LongPointer setter);
    // folder to store the LoRA weights we hope to load during engine initialization
    public native @StdString @Optional BytePointer loraPrefetchDir(); public native PeftCacheManagerConfig loraPrefetchDir(BytePointer setter);
}
