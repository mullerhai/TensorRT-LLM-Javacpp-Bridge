// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.batch_manager;


import tensorrt_llm.global.Batchmanager;
import tensorrt_llm.presets.BatchmanagerConfig;
import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Batchmanager.*;


@Namespace("tensorrt_llm::batch_manager") @NoOffset @Properties(inherit = tensorrt_llm.presets.BatchmanagerConfig.class)
public class ScheduledRequests extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ScheduledRequests(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public ScheduledRequests(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public ScheduledRequests position(long position) {
        return (ScheduledRequests)super.position(position);
    }
    @Override public ScheduledRequests getPointer(long i) {
        return new ScheduledRequests((Pointer)this).offsetAddress(i);
    }

    /** \brief context phase requests (for decoder-only models) or encoder phase requests (for encoder-decoder models
     *  and encoder-only models) */
    public native @SharedPtr LlmRequest contextRequests(); public native ScheduledRequests contextRequests(LlmRequest setter);

    /** \brief generation phase requests (for decoder-only models) or empty for others */
    public native @SharedPtr LlmRequest generationRequests(); public native ScheduledRequests generationRequests(LlmRequest setter);

    public ScheduledRequests() { super((Pointer)null); allocate(); }
    private native void allocate();

    public ScheduledRequests(@SharedPtr LlmRequest contextRequests, @SharedPtr LlmRequest generationRequests) { super((Pointer)null); allocate(contextRequests, generationRequests); }
    private native void allocate(@SharedPtr LlmRequest contextRequests, @SharedPtr LlmRequest generationRequests);

    public native boolean empty();

    public native long size();
}
