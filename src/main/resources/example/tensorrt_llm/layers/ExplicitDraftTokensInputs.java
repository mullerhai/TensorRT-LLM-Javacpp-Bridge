// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.layers;

import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;


// Explicit draft tokens inputs
@Namespace("tensorrt_llm::layers") @NoOffset @Properties(inherit = tensorrt_llm.presets.LayersConfig.class)
public class ExplicitDraftTokensInputs extends DecodingInputs {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ExplicitDraftTokensInputs(Pointer p) { super(p); }

    public ExplicitDraftTokensInputs(@ByVal @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr endIds, @ByVal @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr batchSlots, int batchSize) { super((Pointer)null); allocate(endIds, batchSlots, batchSize); }
    private native void allocate(@ByVal @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr endIds, @ByVal @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr batchSlots, int batchSize);

    /** Draft tokens for the next iteration. The first token in each path is the last accepted token at current
     *  iteration. E.g. if forwardBatchSize == 1, maxNumPaths == 2, maxPathLen== 3, [[[0, 1, 2], [0, 1, 10]]] */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr nextDraftTokens(); public native ExplicitDraftTokensInputs nextDraftTokens(SharedConstPtr setter); // [forwardBatchSize, maxNumPaths, maxPathLen], gpu
    /** Compressed form of {@code nextDraftTokens}, where common prefixes and collapsed.
     *  Using example above [0, 1, 2, 10] */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr nextFlatTokens(); public native ExplicitDraftTokensInputs nextFlatTokens(SharedConstPtr setter); // [forwardBatchSize * maxDecodingTokens], gpu
    /** Indices of draft tokens in the compressed {@code nextFlatTokens} for the next iteration.
     *  Using example above, [[[0, 1, 2], [0, 1, 3]]] */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr nextDraftIndices(); public native ExplicitDraftTokensInputs nextDraftIndices(SharedConstPtr setter); // [forwardBatchSize, maxNumPaths, maxPathLen], gpu
    /** Probabilities of the next draft tokens. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr nextDraftProbs(); public native ExplicitDraftTokensInputs nextDraftProbs(SharedConstPtr setter); // [forwardBatchSize, maxNumPaths, maxDraftPathLen, vocabSize], gpu
    /** Same as {@code nextDraftTokens}, but for current iteration.
     *  Current accepted tokens obtained as {@code lastDraftTokens[bi][bestPathIndices[bi]][1:bestPathLengths[bi]]}. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr lastDraftTokens(); public native ExplicitDraftTokensInputs lastDraftTokens(SharedConstPtr setter); // [forwardBatchSize, maxNumPaths, maxPathLen], gpu
    /** Same as {@code nextDraftIndices}, but for current iteration. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr lastDraftIndices(); public native ExplicitDraftTokensInputs lastDraftIndices(SharedConstPtr setter); // [forwardBatchSize, maxNumPaths, maxPathLen], gpu
    /** Boolean attention masks.
     *  maxDecodingTokens' = generationLengths.max() */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr masks(); public native ExplicitDraftTokensInputs masks(SharedConstPtr setter); // [forwardBatchSize, maxDecodingTokens', maxDecodingTokens'], gpu
    /** Relative to {@code positionIdsBase} position ids. Same as {@code nextFlatTokens} for next draft indices.
     *  Using example above, [0, 1, 2, 3] */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr packedPosIds(); public native ExplicitDraftTokensInputs packedPosIds(SharedConstPtr setter); // [forwardBatchSize * maxDecodingTokens], gpu
    /** Lengths of the accepted paths for each request. It is 1 for context phase (Only 1 primary tokens is accepted). */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr bestPathLengths(); public native ExplicitDraftTokensInputs bestPathLengths(SharedConstPtr setter); // [forwardBatchSize], gpu
    /** Indices of the accepted paths for each request. It is 0 for context phase. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr bestPathIndices(); public native ExplicitDraftTokensInputs bestPathIndices(SharedConstPtr setter); // [forwardBatchSize], gpu
    /** Number of the draft tokens for the next iteration. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr generationLengths(); public native ExplicitDraftTokensInputs generationLengths(SharedConstPtr setter); // [forwardBatchSize], gpu
    /** Baseline for the position ids. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr positionIdsBase(); public native ExplicitDraftTokensInputs positionIdsBase(SharedConstPtr setter); // [forwardBatchSize], gpu
    /** Generation length for the previous stage. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr lastGenerationLengths(); public native ExplicitDraftTokensInputs lastGenerationLengths(SharedConstPtr setter); // [forwardBatchSize], gpu
    /** Maximum number of generated tokens for the next step across whole batch */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr maxGenLengthDevice(); public native ExplicitDraftTokensInputs maxGenLengthDevice(SharedConstPtr setter); // [1], on gpu
    /** Address map to map from linear indices of the engine outputs to seqSlot.
     *  It is not the same as batchSlots because it maps the ordered engine outputs to the respective seqSlot,
     *  while batchSlots is just a a list of active seqSlots. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorConstPtr*") SharedConstPtr seqSlots(); public native ExplicitDraftTokensInputs seqSlots(SharedConstPtr setter); // [forwardBatchSize], on gpu
}
