// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.layers;

import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;


@Namespace("tensorrt_llm::layers") @NoOffset @Properties(inherit = tensorrt_llm.presets.LayersConfig.class)
public class BaseDecodingOutputs extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public BaseDecodingOutputs(Pointer p) { super(p); }

    public BaseDecodingOutputs(@ByVal @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr outputIds) { super((Pointer)null); allocate(outputIds); }
    private native void allocate(@ByVal @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr outputIds);

    /** Mandatory parameters */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr outputIds(); public native BaseDecodingOutputs outputIds(SharedPtr setter); // [maxBatchSize, maxSeqLen]

    /** Optional parameters */
    public native @Cast("tensorrt_llm::layers::TensorPtr*") @Optional SharedPtr finished(); public native BaseDecodingOutputs finished(SharedPtr setter);       // [maxBatchSize * maxBeamWidth], on pinned
    public native @Cast("tensorrt_llm::layers::TensorPtr*") @Optional SharedPtr sequenceLength(); public native BaseDecodingOutputs sequenceLength(SharedPtr setter); // [maxBatchSize * maxBeamWidth], on gpu
    public native @Cast("tensorrt_llm::layers::TensorPtr*") @Optional SharedPtr cumLogProbs(); public native BaseDecodingOutputs cumLogProbs(SharedPtr setter);    // [maxBatchSize * maxBeamWidth], on gpu, for Beam Search
    public native @Cast("tensorrt_llm::layers::TensorPtr*") @Optional SharedPtr outputLogProbs(); public native BaseDecodingOutputs outputLogProbs(SharedPtr setter); // [maxBatchSize, maxBeamWidth, maxSeqLen], on gpu
    public native @Cast("tensorrt_llm::layers::TensorPtr*") @Optional SharedPtr parentIds(); public native BaseDecodingOutputs parentIds(SharedPtr setter);      // [maxBatchSize, maxBeamWidth, maxSeqLen], on gpu, for Beam Search

    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr outputIdsPtr(); public native BaseDecodingOutputs outputIdsPtr(SharedPtr setter);     // [maxBatchSize][maxBeamWidth, maxSeqLen], on gpu and outputIdsPtr[i], on gpu
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr outputIdsPtrHost(); public native BaseDecodingOutputs outputIdsPtrHost(SharedPtr setter); // [maxBatchSize][maxBeamWidth, maxSeqLen], on cpu but outputIdsPtrHost[i], on gpu
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr parentIdsPtr(); public native BaseDecodingOutputs parentIdsPtr(SharedPtr setter);     // [maxBatchSize][maxBeamWidth, maxSeqLen], on cpu but parentIdsPtr[i], on gpu
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr newTokens(); public native BaseDecodingOutputs newTokens(SharedPtr setter);        // [maxBatchSize, maxBeamWidth], on gpu, tokens predicted at current iteration.

    // optional parameters
    public native @Cast("tensorrt_llm::layers::TensorPtr*") @Optional SharedPtr numNewTokens(); public native BaseDecodingOutputs numNewTokens(SharedPtr setter); // [maxBatchSize], on pinned, number of tokens predicted at current iteration
    public native @Cast("tensorrt_llm::layers::TensorPtr*") @Optional SharedPtr finishedSum(); public native BaseDecodingOutputs finishedSum(SharedPtr setter);  // [1], on pinned
    public native @Cast("tensorrt_llm::layers::TensorPtr*") @Optional SharedPtr outputLogProbsTiled(); public native BaseDecodingOutputs outputLogProbsTiled(SharedPtr setter); // [maxSeqLen, maxBatchSize, maxBeamWidth], on gpu

    // Beam width might change in Variable-Beam-Width-Search mode.
    // So the beam width is updated in beam search layer for the later layers.
    public native int beamWidth(); public native BaseDecodingOutputs beamWidth(int setter);
}
