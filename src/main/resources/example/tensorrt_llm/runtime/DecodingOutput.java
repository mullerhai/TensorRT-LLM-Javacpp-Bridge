// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.runtime;

import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

// namespace tensorrt_llm::batch_manager
@Namespace("tensorrt_llm::runtime") @NoOffset @Properties(inherit = tensorrt_llm.presets.RuntimeConfig.class)
public class DecodingOutput extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public DecodingOutput(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public DecodingOutput(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public DecodingOutput position(long position) {
        return (DecodingOutput)super.position(position);
    }
    @Override public DecodingOutput getPointer(long i) {
        return new DecodingOutput((Pointer)this).offsetAddress(i);
    }


    // BS: batch_size, BM: beam_width, MSL: max_seq_length
    // All TensorPtr without special comments are on gpu

    public static class BeamHypotheses extends Pointer {
        static { Loader.load(); }
        /** Default native constructor. */
        public BeamHypotheses() { super((Pointer)null); allocate(); }
        /** Native array allocator. Access with {@link Pointer#position(long)}. */
        public BeamHypotheses(long size) { super((Pointer)null); allocateArray(size); }
        /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
        public BeamHypotheses(Pointer p) { super(p); }
        private native void allocate();
        private native void allocateArray(long size);
        @Override public BeamHypotheses position(long position) {
            return (BeamHypotheses)super.position(position);
        }
        @Override public BeamHypotheses getPointer(long i) {
            return new BeamHypotheses((Pointer)this).offsetAddress(i);
        }
    
        // Keep same as cpp/tensorrt_llm/kernels/beamSearchKernels.h
        public native @SharedPtr ITensor outputIdsCBA(); public native BeamHypotheses outputIdsCBA(ITensor setter);       // [BS, BM*2, MSL]
        public native @SharedPtr ITensor logProbsCBA(); public native BeamHypotheses logProbsCBA(ITensor setter);        // [BS, BM*2, MSL]
        public native @SharedPtr ITensor sequenceLengthsCBA(); public native BeamHypotheses sequenceLengthsCBA(ITensor setter); // [BS, BM*2]
        public native @SharedPtr ITensor cumLogProbsCBA(); public native BeamHypotheses cumLogProbsCBA(ITensor setter);     // [BS, BM*2]
        public native @SharedPtr ITensor normedScoresCBA(); public native BeamHypotheses normedScoresCBA(ITensor setter);    // [BS, BM*2]
        public native @SharedPtr ITensor numBeamsCBA(); public native BeamHypotheses numBeamsCBA(ITensor setter);        // [BS]
        public native @SharedPtr ITensor minNormedScoresCBA(); public native BeamHypotheses minNormedScoresCBA(ITensor setter); // [BS]
        public native @SharedPtr ITensor batchDones(); public native BeamHypotheses batchDones(ITensor setter);         // [BS]

        public native void empty(@Const @ByRef BufferManager manager);

        public native void reshape(int batchSize, int beamWidth, int maxSequenceLength);

        public native void release();

        public native void init(@Const @ByRef BufferManager manager, int endId);

        public native @ByVal BeamHypotheses slice(int batchIndex, int size);
    }

    @MemberGetter public static native float kNegativeInfinity();
    public static final float kNegativeInfinity = kNegativeInfinity();

    public DecodingOutput() { super((Pointer)null); allocate(); }
    private native void allocate();

    /** Mandatory parameters
     *  Previously generated token ids for all steps before DecodingInput.step, [BS, BM, MSL] */
    public native @SharedPtr ITensor ids(); public native DecodingOutput ids(ITensor setter);
    /** The tokens computed during the gatherTree step, [BS, BM, MSL]
     *  Necessary for "Streaming + Beam Search" mode since beam search kernels store ungathered tokens in {@code ids}. */
    public native @SharedPtr ITensor gatheredIds(); public native DecodingOutput gatheredIds(ITensor setter);
    /** New tokens at each generated token of maxTokensPerStep, [maxTokensPerStep, BS, BM] */
    public native @SharedPtr ITensor newTokensSteps(); public native DecodingOutput newTokensSteps(ITensor setter);
    /** A view of newTokensSteps for the current token, [BS, BM] */
    public native @SharedPtr ITensor newTokens(); public native DecodingOutput newTokens(ITensor setter);
    /** A Vector of views on newTokensSteps for each token [BS, BM]. */
    public native @SharedPtr @StdVector PointerPointer newTokensVec(); public native DecodingOutput newTokensVec(PointerPointer setter);

    /** Optional parameters
     *  FinishedState by decoding if any of the stop conditions are met or if DecodingInput.finished is true, [BS, BM] */
    public native @SharedPtr ITensor finishReasons(); public native DecodingOutput finishReasons(ITensor setter);
    /** The sum of finished sequences per request, in pinned memory, [BS] */
    public native @SharedPtr ITensor finishedSum(); public native DecodingOutput finishedSum(ITensor setter);

    /** Mandatory parameters for Beam Search
     *  log-probility of generated tokens, [BS, BM, MSL], float */
    public native @SharedPtr ITensor logProbs(); public native DecodingOutput logProbs(ITensor setter);
    /** Sum log-probility of all generated tokens, [BS, BM] */
    public native @SharedPtr ITensor cumLogProbs(); public native DecodingOutput cumLogProbs(ITensor setter);
    /** Index of the beam where the previous token is, [BS, BM, MSL] */
    public native @SharedPtr ITensor parentIds(); public native DecodingOutput parentIds(ITensor setter);
    /** Total sequence lengths including padding, [BS, BM] */
    public native @SharedPtr ITensor lengths(); public native DecodingOutput lengths(ITensor setter);
    /** K/V indirection for next generation step, [BS, BM, MSL] */
    public native @SharedPtr ITensor cacheIndirection(); public native DecodingOutput cacheIndirection(ITensor setter);
    /** Buffer used to store the transpose of the logProbs, [MSL, BS, BM] */
    public native @SharedPtr ITensor logProbsTiled(); public native DecodingOutput logProbsTiled(ITensor setter);

    public native @ByRef BeamHypotheses beamHypotheses(); public native DecodingOutput beamHypotheses(BeamHypotheses setter);

    // Speculative decoding
    public static class SpeculativeDecodingOutputs extends Pointer {
        static { Loader.load(); }
        /** Default native constructor. */
        public SpeculativeDecodingOutputs() { super((Pointer)null); allocate(); }
        /** Native array allocator. Access with {@link Pointer#position(long)}. */
        public SpeculativeDecodingOutputs(long size) { super((Pointer)null); allocateArray(size); }
        /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
        public SpeculativeDecodingOutputs(Pointer p) { super(p); }
        private native void allocate();
        private native void allocateArray(long size);
        @Override public SpeculativeDecodingOutputs position(long position) {
            return (SpeculativeDecodingOutputs)super.position(position);
        }
        @Override public SpeculativeDecodingOutputs getPointer(long i) {
            return new SpeculativeDecodingOutputs((Pointer)this).offsetAddress(i);
        }
    
        public native @SharedPtr ITensor nextDraftTokens(); public native SpeculativeDecodingOutputs nextDraftTokens(ITensor setter);       // [maxBatchSize, maxDraftTokens]
        public native @SharedPtr ITensor nextDraftTokensLen(); public native SpeculativeDecodingOutputs nextDraftTokensLen(ITensor setter);    // [maxBatchSize]
        public native @SharedPtr ITensor prevDraftTokensLen(); public native SpeculativeDecodingOutputs prevDraftTokensLen(ITensor setter);    // [maxBatchSize]
        public native @SharedPtr ITensor acceptedTokensLen(); public native SpeculativeDecodingOutputs acceptedTokensLen(ITensor setter);     // [maxBatchSize]
        public native @SharedPtr ITensor acceptedLengthsCumSum(); public native SpeculativeDecodingOutputs acceptedLengthsCumSum(ITensor setter); // [maxBatchSize + 1]
        public native @SharedPtr ITensor pathsOffsets(); public native SpeculativeDecodingOutputs pathsOffsets(ITensor setter);          // [maxBatchSize, maxAcceptedDraftTokensPerStep]
    }

    public native @Optional SpeculativeDecodingOutputs speculativeDecodingOutputs(); public native DecodingOutput speculativeDecodingOutputs(SpeculativeDecodingOutputs setter);

    public native @Optional Inputs explicitDraftTokensBuffers(); public native DecodingOutput explicitDraftTokensBuffers(Inputs setter);

    public native @Optional LookaheadDecodingBuffers lookaheadOutputs(); public native DecodingOutput lookaheadOutputs(LookaheadDecodingBuffers setter);

    public native @Optional Inputs eagleBuffers(); public native DecodingOutput eagleBuffers(Inputs setter);
}
