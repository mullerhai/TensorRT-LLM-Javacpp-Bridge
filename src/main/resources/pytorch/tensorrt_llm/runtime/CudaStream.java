// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.runtime;

import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;


@Namespace("tensorrt_llm::runtime") @NoOffset @Properties(inherit = tensorrt_llm.presets.RuntimeConfig.class)
public class CudaStream extends Pointer {
    static { Loader.load(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public CudaStream(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public CudaStream position(long position) {
        return (CudaStream)super.position(position);
    }
    @Override public CudaStream getPointer(long i) {
        return new CudaStream((Pointer)this).offsetAddress(i);
    }

    /** Creates a new cuda stream on the current device. The stream will be destroyed in the destructor.
     * 
     *  @param flags Flags for stream creation. See ::cudaStreamCreateWithFlags for a list of valid flags that can be
     *  passed.
     *  @param priority Priority of the stream. Lower numbers represent higher priorities. See
     *  ::cudaDeviceGetStreamPriorityRange for more information about the meaningful stream priorities that can be
     *  passed. */
    
    //!
    public CudaStream(@Cast("unsigned int") int flags/*=cudaStreamNonBlocking*/, int priority/*=0*/) { super((Pointer)null); allocate(flags, priority); }
    private native void allocate(@Cast("unsigned int") int flags/*=cudaStreamNonBlocking*/, int priority/*=0*/);
    public CudaStream() { super((Pointer)null); allocate(); }
    private native void allocate();

    /** Pass an existing cuda stream to this object.
     * 
     *  @param stream The stream to pass to this object.
     *  @param device The device on which the stream was created.
     *  @param ownsStream Whether this object owns the stream and destroys it in the destructor. */
    public CudaStream(Pointer stream, int device, boolean ownsStream/*=true*/) { super((Pointer)null); allocate(stream, device, ownsStream); }
    private native void allocate(Pointer stream, int device, boolean ownsStream/*=true*/);
    public CudaStream(Pointer stream, int device) { super((Pointer)null); allocate(stream, device); }
    private native void allocate(Pointer stream, int device);

    /** Construct with an existing cuda stream or the default stream by passing nullptr. */
    public CudaStream(Pointer stream) { super((Pointer)null); allocate(stream); }
    private native void allocate(Pointer stream);

    /** Returns the device on which the stream was created. */
    public native int getDevice();

    /** Returns the stream associated with this object. */
    public native Pointer get();

    /** \brief Synchronizes the stream. */
    public native void synchronize();

    /** \brief Record an event on the stream. */
    public native void record(@Cast("tensorrt_llm::runtime::CudaEvent::pointer") Pointer event);

    /** \brief Record an event on the stream. */
    public native void record(@Const @ByRef CudaEvent event);

    /** \brief Wait for an event. */
    public native @Name("wait") void _wait(@Cast("tensorrt_llm::runtime::CudaEvent::pointer") Pointer event);

    /** \brief Wait for an event. */
    public native @Name("wait") void _wait(@Const @ByRef CudaEvent event);
}
