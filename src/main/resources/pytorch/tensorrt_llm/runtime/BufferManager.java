// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.runtime;

import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.TrtllmRuntime.*;


/** \brief A helper class for managing memory on host and device. */
@Namespace("tensorrt_llm::runtime") @NoOffset @Properties(inherit = tensorrt_llm.presets.RuntimeConfig.class)
public class BufferManager extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public BufferManager(Pointer p) { super(p); }

    
    //!

    /** \brief Construct a BufferManager.
     * 
     *  @param cudaStream [in] The cuda stream to use for all operations on GPU (allocation, de-allocation, copying,
     *  etc.). */
    public BufferManager(@SharedPtr CudaStream stream, boolean trimPool/*=false*/) { super((Pointer)null); allocate(stream, trimPool); }
    private native void allocate(@SharedPtr CudaStream stream, boolean trimPool/*=false*/);
    public BufferManager(@SharedPtr CudaStream stream) { super((Pointer)null); allocate(stream); }
    private native void allocate(@SharedPtr CudaStream stream);

    /** \brief Destructor. */

    @MemberGetter public static native int kBYTE_TYPE();
    public static final int kBYTE_TYPE = kBYTE_TYPE();

    /** \brief Allocates an {@code IBuffer} of the given size on the GPU, using cudaMallocAsync. */
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr gpu(long size, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr gpu(long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions on the GPU, using cudaMallocAsync. */
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr gpu(@ByVal Dims dims, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr gpu(@ByVal Dims dims);

    /** \brief Allocates an {@code IBuffer} of the given size on the GPU, using cudaMalloc. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr gpuSync(long size, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr gpuSync(long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions on the GPU, using cudaMalloc. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr gpuSync(@ByVal Dims dims, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr gpuSync(@ByVal Dims dims);

    /** \brief Allocates an {@code IBuffer} of the given size on the CPU. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr cpu(long size, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr cpu(long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions on the CPU. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr cpu(@ByVal Dims dims, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr cpu(@ByVal Dims dims);

    /** \brief Allocates a pinned {@code IBuffer} of the given size on the CPU. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr pinned(long size, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr pinned(long size);

    /** \brief Allocates a pinned {@code ITensor} of the given dimensions on the CPU. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr pinned(@ByVal Dims dims, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr pinned(@ByVal Dims dims);

    /** \brief Allocates a pinned {@code IBuffer} of the given size on the CPU in the default memory pool. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr pinnedPool(long size, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr pinnedPool(long size);

    /** \brief Allocates a pinned {@code ITensor} of the given dimensions on the CPU in the default memory pool. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr pinnedPool(@ByVal Dims dims, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr pinnedPool(@ByVal Dims dims);

    /** \brief Allocates an {@code IBuffer} of the given size in UVM. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr managed(long size, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr managed(long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions in UVM. */
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr managed(@ByVal Dims dims, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr managed(@ByVal Dims dims);

    /** \brief Allocates an {@code ITensor} of the given dimensions for NVLS */
    // [CPP-FIX] public static native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr ipcNvls(@ByVal std::set<int> ranks, @ByVal Dims dims, @ByVal DataType type);

    /** \brief Allocates an {@code IBuffer} of the given size and memory type. */
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") @Name("allocate") UniquePtr _allocate(
            @ByVal MemoryType memoryType, long size, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") @Name("allocate") UniquePtr _allocate(
            @ByVal MemoryType memoryType, long size);

    /** \brief Allocates an {@code ITensor} of the given dimensions and memory type. */
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") @Name("allocate") UniquePtr _allocate(
            @ByVal MemoryType memoryType, @ByVal Dims dims, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") @Name("allocate") UniquePtr _allocate(
            @ByVal MemoryType memoryType, @ByVal Dims dims);

    /** \brief Create an empty {@code IBuffer} of the given memory type. It may be resized later. */
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr emptyBuffer(@ByVal MemoryType memoryType, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr emptyBuffer(@ByVal MemoryType memoryType);

    /** \brief Create an empty {@code ITensor} of the given memory type. It may be reshaped later. */
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr emptyTensor(@ByVal MemoryType memoryType, @ByVal(nullValue = "nvinfer1::DataType(tensorrt_llm::runtime::BufferManager::kBYTE_TYPE)") DataType type);
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr emptyTensor(@ByVal MemoryType memoryType);

    /** \brief Set the contents of the given {@code buffer} to value. */
    public native void setMem(@ByRef IBuffer buffer, int value);

    /** \brief Set the contents of the given {@code buffer} to zero. */
    public native void setZero(@ByRef IBuffer buffer);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const Pointer src, @ByRef IBuffer dst, @ByVal MemoryType srcType);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const @ByRef IBuffer src, Pointer dst, @ByVal MemoryType dstType);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const Pointer src, @ByRef IBuffer dst);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const @ByRef IBuffer src, Pointer dst);

    /** \brief Copy {@code src} to {@code dst}. */
    public native void copy(@Const @ByRef IBuffer src, @ByRef IBuffer dst);

    /** \brief Copy {@code src} into a new {@code IBuffer} with a potentially different memory type. */
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::IBufferPtr*") UniquePtr copyFrom(@Const @ByRef IBuffer src, @ByVal MemoryType memoryType);

    /** \brief Copy {@code src} into a new {@code ITensor} with a potentially different memory type. */
    public native @ByVal @Cast("tensorrt_llm::runtime::BufferManager::ITensorPtr*") UniquePtr copyFrom(@Const @ByRef ITensor src, @ByVal MemoryType memoryType);

    /** \brief Copy {@code src} into a new {@code IBuffer} with a potentially different memory type. */

    /** \brief Copy {@code src} into a new {@code ITensor} with a potentially different memory type. */

    /** \brief Copy {@code src} into a new {@code ITensor} with a potentially different memory type. */

    /** \brief Get the underlying cuda stream. */
    public native @Const @ByRef CudaStream getStream();

    /** \brief The current size of the memory reserved by the memory pool. */
    public native long memoryPoolReserved();

    /** \brief The current size of the memory used by the memory pool. */
    public native long memoryPoolUsed();

    /** \brief The current size of the memory free in the memory pool. */
    public native long memoryPoolFree();

    /** \brief Try to trim the memory reserved by the pool to {@code size} bytes. This synchronizes implicitly with the
     *  stream. */
    public native void memoryPoolTrimTo(long size);
}
