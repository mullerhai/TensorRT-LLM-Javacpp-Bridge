// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.layers;

import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;


/** \brief A helper class for managing key-ngram pool. */
@Namespace("tensorrt_llm::layers") @NoOffset @Properties(inherit = tensorrt_llm.presets.LayersConfig.class)
public class LookaheadPoolManager extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public LookaheadPoolManager(Pointer p) { super(p); }


    public LookaheadPoolManager(int maxG) { super((Pointer)null); allocate(maxG); }
    private native void allocate(int maxG);

    /** \brief setup runtime resource
     *  @param guessSetSize the runtime guessSetSize. */
    public native void setup(int guessSetSize);

    /** \brief fill token map from accepted tokens, including prompt.
     *  @param prompt the user input prompt, [length] on cpu
     *  @param level the n-gram length */
    public native void accept(@Cast("const tensorrt_llm::layers::LookaheadPoolManager::TensorConstPtr*") @ByRef SharedConstPtr prompt, int level);

    /** \brief  get a list of guess tokens
     *  @param lastToken the newest golden token
     *  @param guessSize at most guessSize candidates returned
     *  @return the list guess tokens, with list size <= guessSize */
    // [CPP-FIX] public native @ByVal std::list<tensorrt_llm::layers::LookaheadPoolManager::TensorConstPtr> guess(@Cast("tensorrt_llm::layers::LookaheadPoolManager::Key") int lastToken, int guessSize);

    /** \brief update token map with new generated tokens
     *  @param keyTokens the new shifted out tokens from each window, as the key, [window] on cpu
     *  @param ngramTokens the new shifted lookahead window, as the ngrams, [window, ngramLen] on cpu */
    public native void update(@Cast("const tensorrt_llm::layers::LookaheadPoolManager::TensorConstPtr*") @ByRef SharedConstPtr keyTokens, @Cast("const tensorrt_llm::layers::LookaheadPoolManager::TensorConstPtr*") @ByRef SharedConstPtr ngramTokens);
}
