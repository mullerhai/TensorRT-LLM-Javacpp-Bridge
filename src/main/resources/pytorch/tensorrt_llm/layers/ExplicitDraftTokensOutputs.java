// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.layers;

import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;


@Namespace("tensorrt_llm::layers") @NoOffset @Properties(inherit = tensorrt_llm.presets.LayersConfig.class)
public class ExplicitDraftTokensOutputs extends SpeculativeDecodingOutputs {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public ExplicitDraftTokensOutputs(Pointer p) { super(p); }

    public ExplicitDraftTokensOutputs(@ByVal @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr outputIds) { super((Pointer)null); allocate(outputIds); }
    private native void allocate(@ByVal @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr outputIds);

    /** Draft tokens for the next iteration. The first token in each path is the last accepted token at current
     *  iteration. E.g. if batchSize == 1, maxNumPaths == 2, maxPathLen== 3, [[[0, 1, 2], [0, 1, 10]]] */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr unpackedNextDraftTokens(); public native ExplicitDraftTokensOutputs unpackedNextDraftTokens(SharedPtr setter); // [maxBatchSize, maxNumPaths, maxPathLen], on gpu
    /** Indices of draft tokens in the compressed {@code nextFlatTokens} for the next iteration.
     *  Using example above, [[[0, 1, 2], [0, 1, 3]]] */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr unpackedNextDraftIndices(); public native ExplicitDraftTokensOutputs unpackedNextDraftIndices(SharedPtr setter); // [maxBatchSize, maxNumPaths, maxPathLen], on gpu
    /** Probabilities of the next draft tokens. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr nextDraftProbs(); public native ExplicitDraftTokensOutputs nextDraftProbs(SharedPtr setter); // [maxBatchSize, maxNumPaths, maxPathDraftLen, vocabSize], on gpu
    /** Baseline for the position ids. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr positionIdsBase(); public native ExplicitDraftTokensOutputs positionIdsBase(SharedPtr setter); // [maxBatchSize], on gpu
    /** Randomly sampled data (between 0.f and 1.f) */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr randomDataSample(); public native ExplicitDraftTokensOutputs randomDataSample(SharedPtr setter); // [maxBatchSize], on gpu
    /** Randomly sampled data (between 0.f and 1.f) */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr randomDataValidation(); public native ExplicitDraftTokensOutputs randomDataValidation(SharedPtr setter); // [maxBatchSize, maxNumPaths, maxDraftPathLen], on gpu
    /** Sampling temperature. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr temperatures(); public native ExplicitDraftTokensOutputs temperatures(SharedPtr setter); // [maxBatchSize], on gpu
    /** Next generation lengths. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr generationLengths(); public native ExplicitDraftTokensOutputs generationLengths(SharedPtr setter); // [maxBatchSize], on gpu
    /** Next generation lengths on host. */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr generationLengthsHost(); public native ExplicitDraftTokensOutputs generationLengthsHost(SharedPtr setter); // [maxBatchSize], on pinned
    /** Maximum number of generated tokens for the next step across whole batch */
    public native @ByRef @Cast("tensorrt_llm::layers::TensorPtr*") SharedPtr maxGenLengthHost(); public native ExplicitDraftTokensOutputs maxGenLengthHost(SharedPtr setter); // [1], on pinned
}
