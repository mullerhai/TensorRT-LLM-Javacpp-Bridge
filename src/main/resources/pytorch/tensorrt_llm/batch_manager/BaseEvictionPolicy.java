// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.batch_manager;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static tensorrt_llm.global.Batchmanager.*;


@Namespace("tensorrt_llm::batch_manager::eviction_policy") @Properties(inherit = tensorrt_llm.presets.BatchmanagerConfig.class)
public class BaseEvictionPolicy extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public BaseEvictionPolicy(Pointer p) { super(p); }


    // TODO(TRTLLM-1564): Don't use a separate `initialize` function. Ensure eviction policies can't be in-between a
    // state of construction and initialization.
    public native void initialize(@StdVector BlockPtr mAllBlocksById, @Cast("tensorrt_llm::batch_manager::SizeType32*") @StdVector IntPointer sizes,
            @Optional RetentionPriority secondaryOffloadMinPriority);

    /** \brief Get a free block from the specified cache level
     *  @return The pointer to the free block, along with whether it can be offloaded */
    /** \brief Release a block. Prioritize the block for eviction if toFront=true */
    public native void releaseBlock(@ByVal BlockPtr block);
    public native void releaseBlock(@ByVal BlockPtr block, boolean toFront);
    /** \brief Get the amount of free blocks in the primary memory pool */
    public native @Cast("tensorrt_llm::batch_manager::SizeType32") int getNumFreeBlocks(@Cast("tensorrt_llm::batch_manager::SizeType32") int cacheLevel);
    /** \brief Claim a free block. Called when the cache manager allocates or reuses a new block */
    public native void claimBlock(@ByVal BlockPtr block);
    public native void claimBlock(@ByVal BlockPtr block, @Optional RetentionPriority priority,
            @Optional milliseconds durationMs);
    /** \brief Perform any per-iteration bookkeeping */
    public native void refresh();

    public native boolean verifyQueueIntegrity();
}
