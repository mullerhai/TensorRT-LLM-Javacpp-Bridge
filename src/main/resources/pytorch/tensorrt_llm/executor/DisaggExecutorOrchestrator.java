// Targeted by JavaCPP version 1.5.13: DO NOT EDIT THIS FILE

package tensorrt_llm.executor;

import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;


@Namespace("tensorrt_llm::executor::disagg_executor") @NoOffset @Properties(inherit = tensorrt_llm.presets.ExecutorConfig.class)
public class DisaggExecutorOrchestrator extends Pointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public DisaggExecutorOrchestrator(Pointer p) { super(p); }

    /** \brief Constructs a DisaggExecutorOrchestrator object.
     * 
     *  @param ctxEnginePaths A vector of file paths to context engine files.
     *  @param genEnginePaths A vector of file paths to generation engine files.
     *  @param ctxExecutorConfigs A vector of ExecutorConfig  for context executors.
     *  @param genExecutorConfigs A vector of ExecutorConfig  for generation executors.
     *  @param hasContextAwaitThreads Whether or not there are threads that receive response for each generation
     *  executor.
     *  @param hasGenAwaitThreads Whether or not there are threads that receive response for each generation executor. */

    public DisaggExecutorOrchestrator(@StdVector path ctxEnginePaths,
            @StdVector path genEnginePaths,
            @StdVector ExecutorConfig ctxExecutorConfigs,
            @StdVector ExecutorConfig genExecutorConfigs, boolean hasContextAwaitThreads,
            boolean hasGenAwaitThreads) { super((Pointer)null); allocate(ctxEnginePaths, genEnginePaths, ctxExecutorConfigs, genExecutorConfigs, hasContextAwaitThreads, hasGenAwaitThreads); }
    private native void allocate(@StdVector path ctxEnginePaths,
            @StdVector path genEnginePaths,
            @StdVector ExecutorConfig ctxExecutorConfigs,
            @StdVector ExecutorConfig genExecutorConfigs, boolean hasContextAwaitThreads,
            boolean hasGenAwaitThreads);

    /** \brief Enqueue context-only requests to context executors.
     *  @param requests A vector of context-only requests.
     *  @param selectContextId The index of the context executor to use. If {@code std::nullopt}, the executor that has the
     *  smallest number of inflight requests will be used.
     *  @param batch If true,enqueue requests in same context executor.If false, will try to use a different executor
     *  for each request.
     *  @return A vector of global request ids, corresponding to the order of the requests in {@code requests}, the id
     *  returned may be different from the request id in each executor. */
    // [CPP-FIX] public native @StdVector IdType enqueueContext(@StdVector Request requests,
    // [CPP-FIX] @Optional IntPointer selectContextId/*=std::nullopt*/, boolean batch/*=false*/);
    public native @StdVector IdType enqueueContext(@StdVector Request requests);
    // [CPP-FIX] public native @StdVector IdType enqueueContext(@StdVector Request requests,
    // [CPP-FIX] @Optional IntBuffer selectContextId/*=std::nullopt*/, boolean batch/*=false*/);
    // [CPP-FIX] public native @StdVector IdType enqueueContext(@StdVector Request requests,
    // [CPP-FIX] @Optional int[] selectContextId/*=std::nullopt*/, boolean batch/*=false*/);

    /** \brief Enqueue generation-only requests to generation executors.
     *  @param requests A vector of generation-only requests.
     *  @param globalRequestIds A vector of global request ids, corresponding to the order of the requests,and must be
     *  the ids returned by the enqueueContext function.
     *  @param selectGenIdx The index of the generation executor to use. If {@code std::nullopt}, the executor that has the
     *  smallest number of inflight requests will be used.
     *  @param batch If true,enqueue requests in same generation executor.If false, will try to use a different executor
     *  for each request. */

    // [CPP-FIX] public native void enqueueGeneration(@StdVector Request requests, @StdVector IdType globalRequestIds,
    // [CPP-FIX] @Optional IntPointer selectGenIdx/*=std::nullopt*/, boolean batch/*=false*/);
    public native void enqueueGeneration(@StdVector Request requests, @StdVector IdType globalRequestIds);
    // [CPP-FIX] public native void enqueueGeneration(@StdVector Request requests, @StdVector IdType globalRequestIds,
    // [CPP-FIX] @Optional IntBuffer selectGenIdx/*=std::nullopt*/, boolean batch/*=false*/);
    // [CPP-FIX] public native void enqueueGeneration(@StdVector Request requests, @StdVector IdType globalRequestIds,
    // [CPP-FIX] @Optional int[] selectGenIdx/*=std::nullopt*/, boolean batch/*=false*/);

    /** \brief Await for context responses
     *  @param timeout The maximum time to wait for new responses
     *  @param contextIdx The index of the context executor to use. If {@code std::nullopt}, return ready responses in all
     *  context executors,if {@code hasContextAwaitThreads} is true, then this parameter must be std::nullopt.
     *  @return A vector of responses with corresponding global request ids */

    // [CPP-FIX] public native @StdVector ResponseWithId awaitContextResponses(
    // [CPP-FIX] @Optional milliseconds timeout, @Optional IntPointer contextIdx/*=std::nullopt*/);
    public native @StdVector ResponseWithId awaitContextResponses(
            @Optional milliseconds timeout);
    // [CPP-FIX] public native @StdVector ResponseWithId awaitContextResponses(
    // [CPP-FIX] @Optional milliseconds timeout, @Optional IntBuffer contextIdx/*=std::nullopt*/);
    // [CPP-FIX] public native @StdVector ResponseWithId awaitContextResponses(
    // [CPP-FIX] @Optional milliseconds timeout, @Optional int[] contextIdx/*=std::nullopt*/);

    /** \brief Await for generation responses
     *  @param timeout The maximum time to wait for new responses.
     *  @param genIdx The index of the generation executor to use. If {@code std::nullopt}, return ready responses in all
     *  generation executors,if {@code hasGenAwaitThreads} is true, then this parameter must be std::nullopt.
     *  @return A vector of responses with corresponding global request ids. */
    // [CPP-FIX] public native @StdVector ResponseWithId awaitGenerationResponses(
    // [CPP-FIX] @Optional milliseconds timeout, @Optional IntPointer genIdx/*=std::nullopt*/);
    public native @StdVector ResponseWithId awaitGenerationResponses(
            @Optional milliseconds timeout);
    // [CPP-FIX] public native @StdVector ResponseWithId awaitGenerationResponses(
    // [CPP-FIX] @Optional milliseconds timeout, @Optional IntBuffer genIdx/*=std::nullopt*/);
    // [CPP-FIX] public native @StdVector ResponseWithId awaitGenerationResponses(
    // [CPP-FIX] @Optional milliseconds timeout, @Optional int[] genIdx/*=std::nullopt*/);

    /** \brief  Indicates if the current process is allowed to enqueueRequests */
    public native boolean canEnqueue();

    /** \brief Get context executors */
    // [CPP-FIX] public native @StdVector std::unique_ptr<tensorrt_llm::executor::Executor> getContextExecutors();

    /** \brief Get generation executors */
    // [CPP-FIX] public native @StdVector std::unique_ptr<tensorrt_llm::executor::Executor> getGenExecutors();
}
